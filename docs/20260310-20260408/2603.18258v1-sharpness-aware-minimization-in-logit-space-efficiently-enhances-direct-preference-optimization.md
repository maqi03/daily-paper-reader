---
title: Sharpness-Aware Minimization in Logit Space Efficiently Enhances Direct Preference Optimization
title_zh: Logit 空间中的锐度感知最小化有效增强直接偏好优化
authors: "Haocheng Luo, Zehang Deng, Thanh-Toan Do, Mehrtash Harandi, Dinh Phung, Trung Le"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18258v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 在Logit空间使用SAM增强DPO对齐
tldr: 本研究针对直接偏好优化（DPO）中存在的“挤压效应”（即首选回答概率意外下降的问题），提出了一种在 Logit 空间应用锐度感知最小化（SAM）的改进方法。通过理论分析揭示了负梯度更新在 Logit 空间高曲率方向的扩张导致了该效应，并引入了计算高效的 logits-SAM 变体，仅对输出层进行扰动。实验证明该方法在多个模型和基准测试上显著提升了 DPO 的性能及其泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.18258v1/fig-001.webp\", \"caption\": \"Table 6: Objectives and hyperparameters for SLiC-HF and CPO.\", \"page\": 25, \"index\": 1, \"width\": 826, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2603.18258v1/fig-002.webp\", \"caption\": \"Table 2: Evaluation results on AlpacaEval 2 (LC and WR), Arena-Hard v0.1 (WR), and MT-Bench using Mistral-7B-v0.1. Judges are GPT-4 Turbo for AlpacaEval 2, and GPT-4.1 for Arena-Hard v0.1 and MT-Bench. The highest value within each method group (baseline vs. logits-SAM) is bolded.\", \"page\": 8, \"index\": 2, \"width\": 677, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2603.18258v1/fig-003.webp\", \"caption\": \"Table 3: Performance on HH and TL;DR datasets under different ρ values. Each entry reports win rate vs SFT (left) and vs chosen (right).\", \"page\": 9, \"index\": 3, \"width\": 817, \"height\": 277}]"
motivation: 旨在解决 DPO 训练过程中出现的首选回答概率下降（挤压效应）这一不稳定性问题。
method: 提出 logits-SAM 方法，通过在 Logit 空间引入曲率正则化并仅扰动输出层，以抑制高曲率方向的残差扩张。
result: 在 Pythia、Mistral 和 Gemma 等多个模型上的实验表明，该方法能有效提升 DPO 及其变体的对齐效果。
conclusion: 在 Logit 空间应用 SAM 是一种高效且通用的策略，能显著增强大语言模型偏好对齐的稳定性和性能。
---

## 摘要
直接偏好优化（DPO）因其简单性和训练稳定性，已成为将预训练大语言模型与人类偏好对齐的流行算法。然而，DPO 受到近期发现的“挤压效应”（也称为似然位移）的影响，即在训练过程中，首选回答的概率会无意中降低。为了理解并缓解这一现象，我们开发了一个理论框架，对 Logit 空间中的逐坐标动态进行建模。我们的分析表明，负梯度更新会导致残差沿高曲率方向迅速扩张，这是挤压效应的根源，而锐度感知最小化（SAM）可以通过其曲率正则化效应抑制这种行为。基于这一见解，我们研究了 logits-SAM，这是一种计算效率高的变体，仅对输出层进行扰动，且开销可以忽略不计。在 Pythia-2.8B、Mistral-7B 和 Gemma-2B-IT 上跨多个数据集和基准测试进行的广泛实验表明，logits-SAM 持续提升了 DPO 的有效性，并能与其他 DPO 变体无缝集成。代码可在 https://github.com/RitianLuo/logits-sam-dpo 获取。

## Abstract
Direct Preference Optimization (DPO) has emerged as a popular algorithm for aligning pretrained large language models with human preferences, owing to its simplicity and training stability. However, DPO suffers from the recently identified squeezing effect (also known as likelihood displacement), where the probability of preferred responses decreases unintentionally during training. To understand and mitigate this phenomenon, we develop a theoretical framework that models the coordinate-wise dynamics in logit space. Our analysis reveals that negative-gradient updates cause residuals to expand rapidly along high-curvature directions, which underlies the squeezing effect, whereas Sharpness-Aware Minimization (SAM) can suppress this behavior through its curvature-regularization effect. Building on this insight, we investigate logits-SAM, a computationally efficient variant that perturbs only the output layer with negligible overhead. Extensive experiments on Pythia-2.8B, Mistral-7B, and Gemma-2B-IT across multiple datasets and benchmarks demonstrate that logits-SAM consistently improves the effectiveness of DPO and integrates seamlessly with other DPO variants. Code is available at https://github.com/RitianLuo/logits-sam-dpo.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）偏好对齐中的 **DPO（直接偏好优化）** 算法进行了深入的理论分析与改进，提出了一种在 Logit 空间应用锐度感知最小化（SAM）的高效优化方案。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：DPO 虽然简单稳定，但存在**“挤压效应”（Squeezing Effect）**，即在优化过程中，模型对“首选回答”（preferred responses）的生成概率有时会无意中下降，这会导致对齐失败、性能下降或安全风险。
*   **研究动机**：作者试图从理论上解释为什么会出现这种概率下降现象，并寻找一种能够抑制这种不稳定动态的优化手段。他们发现这种效应与 Logit 空间中 Hessian 矩阵的高曲率方向有关。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用 **SAM（锐度感知最小化）** 的曲率正则化特性来抑制残差在高曲率方向的过度扩张。
*   **理论发现**：
    *   通过对 Logit 空间的坐标动态建模，证明了 DPO 中的负梯度更新（针对被拒绝的回答）会导致残差沿 Hessian 矩阵特征值较大的方向迅速膨胀。
    *   证明了 SAM 引入的二阶修正项可以作为一种预处理机制，有效抵消这种扩张。
*   **关键技术：Logits-SAM**：
    *   **局部扰动**：传统的 SAM 需要对全量参数进行两次前向/后向传播，开销巨大。作者提出仅对模型的**输出层（lm_head）**参数进行扰动。
    *   **算法流程**：
        1.  计算输出层参数 $W$ 的梯度。
        2.  在梯度方向上对 $W$ 施加一个微小的扰动 $\epsilon$。
        3.  使用扰动后的参数计算损失并进行最终的参数更新。
    *   **高效性**：由于只涉及输出层，其计算开销相比标准 DPO 几乎可以忽略不计。

### 3. 实验设计
*   **数据集/场景**：
    *   **对话与摘要**：Anthropic-HH, Reddit TL;DR, UltraFeedback Binarized。
    *   **安全对齐**：SorryBench（评估有害请求的拒绝率）。
*   **基准测试（Benchmarks）**：
    *   使用 GPT-5-mini 作为裁判评估胜率（Win Rate）。
    *   主流榜单：AlpacaEval 2 (LC), Arena-Hard v0.1, MT-Bench。
*   **对比方法**：
    *   基础方法：DPO, SLiC-HF, CPO。
    *   安全增强方法：CHES。
    *   模型涵盖：Pythia-2.8B, Mistral-7B, Gemma-2B-IT。

### 4. 资源与算力
*   **硬件配置**：
    *   **Pythia-2.8B**：使用 2 台 NVIDIA **A100** GPU，采用 DDP（分布式数据并行）模式。
    *   **Mistral-7B**：使用 4 台 NVIDIA **A100** GPU，采用 DeepSpeed ZeRO-3 优化。
*   **训练时长与开销**：
    *   论文明确指出，Logits-SAM 相比于 AdamW 仅增加了约 **2-3%** 的额外训练时间。
    *   显存占用方面，Logits-SAM 的峰值显存增加几乎可以忽略不计（例如从 69.36GB 微增至 69.39GB）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2B 到 7B 不同规模的模型，并在三个主流对齐任务和三个权威 Benchmark 上进行了验证。
*   **消融与分析**：
    *   进行了超参数 $\rho$（扰动半径）的敏感性分析。
    *   对比了 Logit 空间与参数空间的 Hessian 迹（Trace），验证了方法确实能找到更平滑的解。
    *   包含了对训练动态（Loss、Accuracy）的实时追踪。
*   **客观性**：实验采用了业界公认的开源 Checkpoint 和超参数设置（如 Alignment Handbook），对比实验在相同硬件和超参数条件下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **性能提升**：Logits-SAM 在所有测试的 DPO 变体（DPO, SLiC-HF, CPO）上均实现了持续的性能增长。
*   **缓解挤压效应**：实验观察到，加入 SAM 后，首选回答的概率下降速度减缓，而模型最自信的错误回答概率增长受到抑制。
*   **泛化与安全**：该方法不仅提升了通用能力（如 MT-Bench 分数），还在安全对齐场景下显著提高了模型对有害指令的拒绝率（SorryBench 提升约 9%）。
*   **平滑性**：实证测量证实，Logits-SAM 确实降低了参数空间和 Logit 空间的锐度（Sharpness）。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还通过严谨的数学推导揭示了 DPO 挤压效应的本质。
*   **极高的实用性**：Logits-SAM 几乎不增加计算负担，却能带来稳定的性能增益，非常适合大规模模型训练。
*   **良好的兼容性**：作为一个优化层面的改进，它可以无缝集成到现有的各种 DPO 变体中。

### 8. 不足与局限
*   **超参数敏感性**：虽然 $\rho$ 在一定范围内有效，但其最优值通常比标准 SAM 小得多（1e-4 左右），且在不同任务间仍需微调。
*   **实验覆盖范围**：虽然测试了 7B 模型，但尚未在更大规模（如 70B+）或更复杂的混合专家模型（MoE）上验证其效果。
*   **离线限制**：目前的研究主要集中在离线偏好对齐，对于在线（Online）PPO 等算法的适用性讨论较少。

（完）
