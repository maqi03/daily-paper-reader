---
title: An Empirical Study of SFT-DPO Interaction and Parameterization in Small Language Models
title_zh: 小语言模型中 SFT-DPO 交互与参数化的实证研究
authors: "Yuming Feng, Christy Yang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20100v1"
tags: ["query:rm-opt"]
score: 9.0
evidence: DPO交互与参数化的实证研究
tldr: 本研究针对小规模语言模型（GPT-2规模），系统探讨了监督微调（SFT）与直接偏好优化（DPO）的交互作用，并对比了全参数微调（FFT）与LoRA的表现。研究发现，在小模型场景下，全参数微调对性能的提升远超参数高效微调，而DPO相比强SFT基线的提升有限。该研究为资源受限下的模型对齐和参数化策略提供了实证参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20100v1/fig-001.webp\", \"caption\": \"Table 13: Hyperparameters for the Sonnet Generation task (SFT and DPO phases).\", \"page\": 10, \"index\": 1, \"width\": 669, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2603.20100v1/fig-002.webp\", \"caption\": \"Table 12: DPO hyperparameters: search space (left) and hand-off phase (right).\", \"page\": 10, \"index\": 2, \"width\": 748, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2603.20100v1/fig-003.webp\", \"caption\": \"Table 9: Paraphrase detection Case Study\", \"page\": 8, \"index\": 3, \"width\": 833, \"height\": 524}, {\"url\": \"assets/figures/arxiv/2603.20100v1/fig-004.webp\", \"caption\": \"Figure 1: Training curves for FFT and LoRA (r = 8) on paraphrase detection. Plot train and dev accuracy/F1 across epochs.\", \"page\": 5, \"index\": 4, \"width\": 762, \"height\": 302}]"
motivation: 旨在探究在小规模模型和有限数据场景下，SFT与DPO的交互效应以及不同参数化方法（FFT与LoRA）的实际表现。
method: 在GPT-2规模的解码器上，通过释义检测和十四行诗续写任务，对比了仅SFT、仅DPO、SFT后接DPO以及FFT与LoRA的不同组合。
result: 实验表明FFT在性能上始终优于LoRA，且LoRA并未减少训练耗时；DPO在特定条件下能匹配SFT效果，但整体增益较小。
conclusion: 在小规模模型领域，全参数监督适配仍是提升性能的核心手段，而偏好优化和低秩适配带来的边际收益相对有限。
---

## 摘要
直接偏好优化 (DPO) 在监督微调 (SFT) 之后被广泛用于对齐语言模型，但在小规模骨干网络和适量数据下的实证表现尚不明确。我们在 GPT-2 规模的解码器上系统地比较了仅 SFT、仅 DPO 以及阶段性 SFT 到 DPO 的训练方式，并对比了全参数微调 (FFT) 与 LoRA 的效果，评估任务包括释义检测和莎士比亚十四行诗续写。结果显示，相比于强 SFT，DPO 带来的提升较小且依赖于具体任务；当偏好构建与监督目标高度平行时，DPO 无需预热启动即可达到具有竞争力的 SFT 准确率。相比之下，参数化方式起主导作用：在相同的训练深度下，FFT 的表现始终优于 LoRA，且 LoRA 在我们的硬件上并未减少实际运行时间。这些发现表明，在小规模场景下，监督式全参数适配仍然是提升性能的主要手段，而偏好优化和低秩适配带来的边际收益有限。

## Abstract
Direct Preference Optimization (DPO) is widely used after supervised fine-tuning (SFT) to align language models, yet empirical behavior under small backbones and modest data is under-specified. We systematically compare SFT-only, DPO-only, and staged SFT-to-DPO training alongside full fine-tuning (FFT) versus LoRA on a GPT-2-scale decoder, evaluating paraphrase detection and Shakespearean sonnet continuation. DPO yields small, task-dependent gains over strong SFT and can match competitive SFT accuracy without a warm start when the preference construction closely parallels the supervised objective. In contrast, parameterization dominates: FFT consistently outperforms LoRA at matched training depth, and LoRA does not reduce wall-clock time on our hardware. These findings indicate that, in this small-scale regime, supervised full-parameter adaptation remains the primary performance lever, while preference optimization and low-rank adaptation provide limited marginal returns.

---

## 论文详细总结（自动生成）

以下是对论文《An Empirical Study of SFT-DPO Interaction and Parameterization in Small Language Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
本研究聚焦于**小语言模型（SLM，如 GPT-2 规模）**在特定任务适配中的表现。虽然监督微调（SFT）、直接偏好优化（DPO）和低秩适配（LoRA）在大规模模型中已成为标准流程，但在参数量较小（约 1.24 亿）且数据量适中的情况下，这些技术的交互作用和实际收益尚不明确。
论文旨在回答两个核心问题：
1.  在适配小模型时，LoRA 与全参数微调（FFT）的性能差异如何？
2.  SFT 与 DPO 如何相互影响？在训练过程中何时引入 DPO 最为有效？

### 2. 论文提出的方法论
研究者在 GPT-2（124M 参数）骨干网络上构建了一个系统的实验框架：
*   **核心思想**：通过控制变量法，对比不同参数化策略（FFT vs. LoRA）和不同训练阶段（仅 SFT、仅 DPO、SFT $\rightarrow$ DPO 阶段性训练）的组合效果。
*   **关键技术细节**：
    *   **SFT**：使用标准交叉熵损失进行监督学习。
    *   **DPO**：通过优化偏好对（Chosen vs. Rejected）的对数概率差值来对齐模型。
    *   **参数化策略**：对比了更新全部参数的 FFT 和通过低秩矩阵 $W = W_0 + \frac{\alpha}{r} BA$ 更新部分参数的 LoRA。
    *   **偏好构建**：在分类任务中，正确标签为 Chosen，错误为 Rejected；在生成任务中，原诗为 Chosen，模型生成的低质量文本为 Rejected。

### 3. 实验设计
*   **数据集与场景**：
    1.  **释义检测（分类）**：使用 Quora Question Pairs (QQP) 数据集（约 28.3 万条数据），判断两句话意思是否相同。
    2.  **十四行诗续写（生成）**：使用莎士比亚十四行诗（155 首），给定前 3 行续写后 11 行。
*   **对比方法与 Benchmark**：
    *   基准线为仅进行 SFT 的模型。
    *   对比了不同数据规模（2.83k 到 283k）。
    *   对比了 LoRA 的不同秩（$r=4, 8, 16$）。
    *   对比了 DPO 的超参数（$\beta$ 和学习率）以及从 SFT 切换到 DPO 的时机（SFT@3 轮 vs. SFT@9 轮）。

### 4. 资源与算力
*   **硬件**：使用 Modal 云平台上的 **NVIDIA H100 GPU**（每张显卡 80GB VRAM）。
*   **训练时长**：
    *   释义检测任务：全量数据下每轮（Epoch）约 7 分钟。
    *   十四行诗任务：规模极小，每轮约 0.7 秒。
*   **重要发现**：在 H100 这种算力受限而非显存受限的硬件上，LoRA 并没有比 FFT 减少实际运行时间（Wall-clock time）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括数据规模实验、LoRA 秩扫描、DPO 超参数扫描、SFT-DPO 切换时机实验、生成任务的温度系数实验以及三种不同的偏好对构建策略。
*   **充分性与客观性**：实验设计较为严谨，特别是在生成任务中引入了 20 个不同随机种子的稳定性评估。作者坦诚地指出，由于部分实验仅运行了一次，某些微小差异（如 0.6% 的准确率波动）应视为描述性结果而非绝对统计显著。

### 6. 论文的主要结论与发现
1.  **参数化方式主导性能**：在小模型上，**FFT 的表现始终优于 LoRA**。LoRA 的秩增加并不一定带来性能提升（存在“秩悖论”）。
2.  **DPO 收益有限**：相比于调优良好的强 SFT 基线，DPO 带来的提升非常微小且依赖于任务。
3.  **DPO 无需预热的条件**：当偏好对的构建逻辑与监督目标高度一致时（如分类任务的对错标签），DPO 甚至可以脱离 SFT 独立训练并取得不错效果。
4.  **效率误区**：在高性能硬件（H100）处理小模型时，LoRA 的内存优势无法转化为训练速度优势。
5.  **数据多样性胜过重复**：在相同时间内，在大数据集上训练较少轮次的效果优于在小数据集上多次迭代。

### 7. 优点
*   **实证价值高**：针对被忽视的“小模型+小数据”场景提供了实用的工程指导。
*   **揭示了硬件与算法的解耦**：明确指出 LoRA 在高性能 GPU 上的时间效率瓶颈。
*   **任务覆盖全面**：同时涵盖了判别式（分类）和生成式任务，增强了结论的普适性。

### 8. 不足与局限
*   **模型规模局限**：结论仅基于 GPT-2 (124M)，可能无法直接推广到 7B 或更大规模的模型。
*   **任务数量有限**：仅测试了两个特定任务，对于更复杂的推理或多轮对话场景未做探讨。
*   **偏好构建单一**：生成任务的偏好对构建主要基于字符级重叠（chrF），可能无法完全捕捉语义层面的“好坏”。

（完）
