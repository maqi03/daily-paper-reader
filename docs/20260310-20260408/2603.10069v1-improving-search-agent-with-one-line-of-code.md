---
title: Improving Search Agent with One Line of Code
title_zh: 仅需一行代码改进搜索智能体
authors: "Jian Li, Dongsheng Chen, Zhenhua Xu, Yizhang Jin, Jiafu Wu, Chengjie Wang, Xiaotong Yuan, Yabiao Wang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10069v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 通过条件Token级KL约束稳定GRPO训练
tldr: "本研究针对工具增强型强化学习（TARL）中搜索智能体训练不稳定的问题，识别出重要性采样分布偏移（ISDD）是导致模型崩溃的关键原因。为此提出了SAPO算法，通过在GRPO基础上引入条件式Token级KL约束，仅对概率过低的正向Token进行惩罚，有效防止了分布偏移并保持梯度流动。该方法仅需一行代码修改，在多个QA基准测试中显著提升了性能，相比Search-R1平均提升10.6%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.10069v1/fig-001.webp\", \"caption\": \"Table 1: Accuracy comparison of SAPO-3B against baseline methods using Qwen2.5-3B Qwen et al. (2025) across multiple QA benchmarks. Bold indicates best results, and underline denotes second best results.\", \"page\": 6, \"index\": 1, \"width\": 827, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-002.webp\", \"caption\": \"Table 5: Scaling trends of Qwen2.5-Instruct with SAPO across different model sizes (1.5B to 14B). We report both EM and F1 scores. The best results are marked in bold.\", \"page\": 15, \"index\": 2, \"width\": 826, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-003.webp\", \"caption\": \"Table 2: Ablation results on different KL terms in SAPO. The values in parentheses denote the absolute performance change compared to the previous row.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-004.webp\", \"caption\": \"Table 4: Hyperparameters used in SAPO.\", \"page\": 14, \"index\": 4, \"width\": 377, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-005.webp\", \"caption\": \"Figure 3: Prompt template for SAPO.\", \"page\": 14, \"index\": 5, \"width\": 829, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-006.webp\", \"caption\": \"Table 7: Case Study illustrating SAPO from the Musique benchmark. Key evidence is extracted from the original searched documents.\", \"page\": 17, \"index\": 6, \"width\": 827, \"height\": 1025}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-007.webp\", \"caption\": \"Figure 4: Evolution of EM accuracy over training steps for SAPO across seven benchmarks.\", \"page\": 16, \"index\": 7, \"width\": 801, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2603.10069v1/fig-008.webp\", \"caption\": \"Table 6: Case Studies illustrating SAPO trajectories from the 2wikimultihopqa benchmark. Key evidence is extracted from the original searched documents.\", \"page\": 16, \"index\": 8, \"width\": 827, \"height\": 754}]"
motivation: 旨在解决GRPO算法在训练搜索智能体时，因重要性采样分布偏移（ISDD）导致梯度消失和模型训练崩溃的稳定性难题。
method: 提出SAPO算法，通过引入条件式Token级KL约束，仅针对偏移过大的正向Token进行选择性惩罚，以稳定策略分布。
result: "在七个QA基准测试中，SAPO相比Search-R1实现了10.6%的绝对性能提升，并在不同规模（1.5B、14B）和系列（Qwen、LLaMA）的模型上表现出一致的优越性。"
conclusion: SAPO通过极简的代码修改有效解决了强化学习中的分布偏移问题，为构建稳定且高性能的自主搜索智能体提供了高效方案。
---

## 摘要
基于工具的智能体强化学习（TARL）已成为训练搜索智能体自主与外部工具交互以进行多轮信息检索过程的一种极具前景的范式。然而，我们发现了一个导致灾难性模型崩溃的关键训练不稳定性问题：重要性采样分布偏移（ISDD）。在广泛采用的 TARL 算法——群体相对策略优化（GRPO）中，ISDD 表现为重要性采样比率的急剧下降，这使得梯度更新失效并引发不可逆的训练失败。为解决这一问题，我们提出了搜索智能体策略优化（SAPO），通过条件性的 token 级 KL 约束来稳定训练。与忽略分布差异的硬截断（hard clipping）不同，SAPO 选择性地惩罚当前策略与旧策略之间的 KL 散度。至关重要的是，该惩罚仅应用于策略发生过度偏移的低概率正向 token，从而在防止分布偏移的同时保留梯度流。值得注意的是，SAPO 仅需对标准 GRPO 进行一行代码修改，确保了即时部署能力。在七个问答基准测试上的广泛实验表明，SAPO 相比 Search-R1 实现了 +10.6% 的绝对提升（+31.5% 相对提升），并在不同模型规模（1.5B、14B）和系列（Qwen、LLaMA）中均获得了一致的增益。

## Abstract
Tool-based Agentic Reinforcement Learning (TARL) has emerged as a promising paradigm for training search agents to interact with external tools for a multi-turn information-seeking process autonomously. However, we identify a critical training instability that leads to catastrophic model collapse: Importance Sampling Distribution Drift(ISDD). In Group Relative Policy Optimization(GRPO), a widely adopted TARL algorithm, ISDD manifests as a precipitous decline in the importance sampling ratios, which nullifies gradient updates and triggers irreversible training failure. To address this, we propose \textbf{S}earch \textbf{A}gent \textbf{P}olicy \textbf{O}ptimization (\textbf{SAPO}), which stabilizes training via a conditional token-level KL constraint. Unlike hard clipping, which ignores distributional divergence, SAPO selectively penalizes the KL divergence between the current and old policies. Crucially, this penalty is applied only to positive tokens with low probabilities where the policy has shifted excessively, thereby preventing distribution drift while preserving gradient flow. Remarkably, SAPO requires only one-line code modification to standard GRPO, ensuring immediate deployability. Extensive experiments across seven QA benchmarks demonstrate that SAPO achieves \textbf{+10.6\% absolute improvement} (+31.5\% relative) over Search-R1, yielding consistent gains across varying model scales (1.5B, 14B) and families (Qwen, LLaMA).

---

## 论文详细总结（自动生成）

这是一份关于论文《Improving Search Agent with One Line of Code》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**工具增强型智能体强化学习（TARL）**，特别是针对搜索智能体（Search Agent）的训练稳定性问题。
*   **背景**：目前主流方法（如 Search-R1）采用 GRPO 算法进行后训练，使模型能自主调用搜索工具。
*   **核心问题**：研究者发现训练中存在**重要性采样分布偏移（ISDD）**现象。在多轮搜索任务中，当当前策略与旧策略稍有偏差，重要性采样比率（IS Ratio）会呈指数级下降趋近于零。这导致梯度消失，模型无法从高奖励路径中学习，最终引发灾难性的模型崩溃（表现为奖励下降、熵值异常）。

### 2. 方法论：SAPO (Search Agent Policy Optimization)
SAPO 的核心思想是通过**条件 Token 级 KL 约束**来替代或增强传统的硬截断（Hard Clipping）机制。
*   **核心思想**：针对性地惩罚那些“概率大幅下降但具有正向优势（Advantage > 0）”的 Token。
*   **关键技术细节**：
    *   **条件触发**：不同于传统的全局 KL 惩罚，SAPO 引入了一个指示函数。只有当 $IS Ratio < \tau$（通常 $\tau=1.0$）且该动作的优势函数 $\hat{A} > 0$ 时，才会激活 KL 惩罚。
    *   **Token 级粒度**：在 Token 级别计算 $\log(ratio)$ 惩罚，确保对搜索动作（Action）和推理步骤（Reasoning）进行精细化约束。
    *   **极简实现**：该方法在代码实现上非常简单，仅需在 GRPO 的损失函数中增加一行关于条件 KL 项的计算。
*   **算法逻辑**：通过软性惩罚防止策略在探索过程中过快地偏离旧策略的“信任区域”，从而维持梯度流的连续性，避免训练停滞。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流 QA 基准测试，分为两类：
    *   **单跳（Single-hop）**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳（Multi-hop）**：HotpotQA, 2WikiMultihopQA, Musique, Bamboogle。
*   **Benchmark 与对比方法**：
    *   **检索无关方法**：直接生成、SFT、R1-Instruct。
    *   **静态 RAG 方法**：Naive RAG, IRCoT。
    *   **搜索智能体方法**：Search-o1, Search-R1 (GRPO 基准), ReSearch, ZeroSearch, O2-Searcher, AutoRefine, SE-Search 等。
*   **模型底座**：主要使用 Qwen2.5 系列（1.5B 到 14B），并扩展到了 LLaMA-3.2-3B 以验证通用性。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **实现框架**：提到使用了 **VeRL** 强化学习框架进行开发。
*   **超参数**：训练批次大小为 256，微批次 64，学习率 $1 \times 10^{-6}$，最大搜索轮数 $T_{max}=5$。

### 5. 实验数量与充分性
*   **实验规模**：实验设计相当充分。
    *   **主实验**：在 7 个数据集上对比了超过 15 种基线方法。
    *   **消融实验**：对比了标准 GRPO、带全局 KL 的 GRPO、仅带比例约束的 KL 以及完整的 SAPO，验证了各组件的有效性。
    *   **扩展性实验**：测试了从 1.5B 到 14B 不同参数规模的模型，验证了 Scaling Law。
    *   **鲁棒性实验**：在 Qwen 和 LLaMA 两个完全不同的模型家族上均取得了提升。
*   **客观性**：采用了 Exact Match (EM) 和 F1 分数作为硬性指标，评价标准客观。

### 6. 主要结论与发现
*   **性能飞跃**：SAPO 在 7 个基准测试中平均比 Search-R1 提升了 **10.6% 的绝对精度**（相对提升 31.5%）。
*   **多跳任务优势明显**：在复杂的长链条推理（多跳 QA）中提升尤为显著，例如在 Bamboogle 上提升了约 30%。
*   **解决崩溃问题**：实验曲线证明 SAPO 有效抑制了训练后期的 IS Ratio 崩溃，使模型能够持续从搜索反馈中进化。
*   **模型无关性**：无论底座是 Base 还是 Instruct 版本，无论模型架构如何，该方法均表现出一致的增益。

### 7. 优点（亮点）
*   **大道至简**：通过“一行代码”的微小改动解决了强化学习中深层次的分布偏移问题，具有极高的工程实用价值。
*   **理论支撑**：对 ISDD 现象进行了数学定义和梯度分析，解释了为什么多轮交互会放大这种不稳定性。
*   **即插即用**：不需要额外的 Critic 网络或复杂的奖励建模，保持了 GRPO 的轻量化特性。

### 8. 不足与局限
*   **算力细节缺失**：缺乏具体的训练资源消耗报告，难以评估其在大规模集群上的训练效率。
*   **超参数敏感度**：虽然做了 $\tau$ 的敏感度分析，但在不同任务或不同工具调用场景下，该阈值是否需要精细调优尚不明确。
*   **工具多样性**：实验主要集中在“搜索”这一种工具上，对于计算器、代码执行等其他类型的工具智能体是否同样有效，仍需进一步验证。

（完）
