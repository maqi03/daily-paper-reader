---
title: Efficient Exploration at Scale
title_zh: 大规模高效探索
authors: "Seyed Mohammad Asghari, Chris Chute, Vikranth Dwaracherla, Xiuyuan Lu, Mehdi Jafarnia, Victor Minden, Zheng Wen, Benjamin Van Roy"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17378v1"
tags: ["query:rm-opt"]
score: 9.0
evidence: 增量更新奖励模型以提高RLHF的数据效率
tldr: 本研究针对人类反馈强化学习（RLHF）中数据效率低下的问题，提出了一种在线学习算法。该算法通过增量更新奖励模型和语言模型，并引入了肯定性微调、认知神经网络建模奖励不确定性以及信息导向探索（IDS）等关键技术。实验表明，该方法在Gemma大模型上仅需不到2万条标签即可达到传统离线RLHF 20万条标签的效果，实现了超过10倍的数据效率提升，并预测在大规模应用下可实现千倍增益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17378v1/fig-001.webp\", \"caption\": \"Figure 1 | The plots are of performance, in terms of the win rate over a baseline policy, as functions of the amount of human feedback, in terms of the number of choices observed. Efficient exploration shifts the scaling law.\", \"page\": 2, \"index\": 1, \"width\": 674, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.17378v1/fig-002.webp\", \"caption\": \"Figure 4(right) demonstrates the benefit.\", \"page\": 7, \"index\": 2, \"width\": 968, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2603.17378v1/fig-003.webp\", \"caption\": \"Figure 8 | Win rate against the baseline policy as a function of the amount of feedback collected to adapt the language model.\", \"page\": 10, \"index\": 3, \"width\": 674, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.17378v1/fig-004.webp\", \"caption\": \"Figure 9 | (a) Extrapolation of win rates attained by efficient exploration and offline RLHF. We fit a function of the form 𝑤(𝑛) = 1 − 0.5(𝑛/𝑎)−𝑏, with parameters 𝑎 and 𝑏. (b) Projected gain in data efficiency relative to offline RLHF. The projected gain is computed using extrapolations from (a). With 1M labels, the efficient exploration algorithm is predicted to attain a 1,000x gain in data efficiency.\", \"page\": 10, \"index\": 4, \"width\": 948, \"height\": 317}]"
motivation: 旨在解决当前RLHF方法在训练大规模语言模型时对人类标注数据需求量巨大且效率低下的核心挑战。
method: 提出一种在线学习框架，结合认知神经网络建模不确定性，并利用信息导向探索策略来优化奖励和语言模型的增量更新。
result: 在Gemma模型上的实验显示，该算法仅用不到2万个标签就达到了离线RLHF使用20万个标签的性能，效率提升逾10倍。
conclusion: 该研究证明了通过高效的在线探索机制，可以极大地降低RLHF对标注数据的依赖，为大规模模型训练提供了极具潜力的新路径。
---

## 摘要
我们开发了一种在线学习算法，显著提高了基于人类反馈的强化学习（RLHF）的数据效率。我们的算法在接收到选择数据时增量更新奖励模型和语言模型。奖励模型拟合选择数据，而语言模型通过 REINFORCE 算法的一种变体进行更新，其中强化信号由奖励模型提供。若干特性实现了效率提升：在每个强化信号中加入微小的肯定性微调（affirmative nudge）、一个建模奖励不确定性的认识神经网络（epistemic neural network），以及信息导向探索（information-directed exploration）。使用 Gemma 大语言模型（LLMs），我们的算法仅需不到 2 万条标签即可达到离线 RLHF 在 20 万条标签上训练的性能，这意味着数据效率提升了 10 倍以上。根据我们的结果推断，我们预计在 100 万条标签上训练的算法可以达到离线 RLHF 在 10 亿条标签上训练的性能。这代表了 1000 倍的提升。据我们所知，这是首个证明如此大幅度提升是可行性的结果。

## 速览
**TLDR**：本研究针对人类反馈强化学习（RLHF）中数据效率低下的问题，提出了一种在线学习算法。该算法通过增量更新奖励模型和语言模型，并引入肯定性微调、认识神经网络建模奖励不确定性以及信息导向探索等核心技术。实验表明，该方法在Gemma大模型上仅需不到2万条标签即可达到传统离线RLHF需20万条标签的效果，实现了超过10倍的数据效率提升，且预测在大规模应用下可达1000倍提升。 \
**Motivation**：旨在解决当前人类反馈强化学习（RLHF）在训练过程中对海量标注数据过度依赖且效率低下的核心挑战。 \
**Method**：提出一种在线学习框架，结合了增量模型更新、奖励不确定性建模以及基于信息导向的探索策略。 \
**Result**：在Gemma模型上，该算法仅用不到2万个标签就达到了离线RLHF使用20万个标签的性能，实现了10倍以上的数据效率提升。 \
**Conclusion**：该研究证明了通过高效的在线探索机制，可以极大幅度地降低大语言模型对人工标注数据的需求。

---

## Abstract
We develop an online learning algorithm that dramatically improves the data efficiency of reinforcement learning from human feedback (RLHF). Our algorithm incrementally updates reward and language models as choice data is received. The reward model is fit to the choice data, while the language model is updated by a variation of reinforce, with reinforcement signals provided by the reward model. Several features enable the efficiency gains: a small affirmative nudge added to each reinforcement signal, an epistemic neural network that models reward uncertainty, and information-directed exploration. With Gemma large language models (LLMs), our algorithm matches the performance of offline RLHF trained on 200K labels using fewer than 20K labels, representing more than a 10x gain in data efficiency. Extrapolating from our results, we expect our algorithm trained on 1M labels to match offline RLHF trained on 1B labels. This represents a 1,000x gain. To our knowledge, these are the first results to demonstrate that such large improvements are possible.

---

## 论文详细总结（自动生成）

这是一份关于 Google DeepMind 团队发表的论文《Efficient Exploration at Scale》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在预训练阶段使用了海量数据，但在对齐阶段（RLHF），获取高质量的人类偏好反馈数据成本极高且效率低下。
*   **核心问题**：如何通过主动探索（Active Exploration）和在线学习（Online Learning）机制，减少对人类标注数据的依赖，同时提升模型性能。
*   **整体含义**：该研究试图打破 RLHF 的现有缩放定律（Scaling Laws），证明通过智能地选择最具信息量的样本进行标注，可以实现数据效率的指数级提升。

### 2. 论文提出的方法论
该论文提出了一种**在线信息导向探索算法**，其核心由三个关键技术支柱组成：

*   **在线增量更新（Online Incremental Updates）**：
    *   不同于传统的离线 RLHF（先收集数据再训练），该算法在接收到每一批次（Batch）选择数据后，立即增量更新奖励模型（RM）和语言模型（LM）。
    *   LM 的更新采用了一种 REINFORCE 的变体（类似于 PMPO），利用 RM 提供的信号进行实时调整。
*   **肯定性微调（Affirmative Nudge）**：
    *   **技术细节**：在策略梯度的强化信号中加入一个微小的正标量 $\epsilon$。
    *   **作用**：解决了在线 RLHF 中常见的“性能崩塌（Tanking）”问题，使模型在持续学习过程中保持稳定，无需大幅降低学习率。
*   **信息导向探索（Information-Directed Exploration, IDS）**：
    *   **核心思想**：利用**认知神经网络（Epistemic Neural Network, ENN）**来建模奖励的不确定性。
    *   **实现**：RM 包含一个点估计头和一组由 100 个粒子组成的集成头（Ensemble MLPs）。
    *   **流程**：对于每个提示词（Prompt），模型生成 16 个候选回复，计算所有回复对在集成模型中的胜率方差，选择**方差最大（即不确定性最高、信息量最大）**的回复对提交给人类（或模拟器）进行标注。

### 3. 实验设计
*   **模型基础**：使用 **Gemma 9B** 作为基础策略模型。
*   **数据集**：使用了 20.2 万个内部 Prompt，涵盖写作、代码、摘要、数学、科学等多种任务。其中 20 万用于训练，1000 用于测试，1000 用于最终评估。
*   **Benchmark（基准）**：以未经 RLHF 的 SFT 模型（Top-1 采样）作为基准线。
*   **对比方法**：
    1.  **Offline RLHF**：传统的离线训练模式。
    2.  **Periodic RLHF**：每隔 400 个 Batch 重新训练一次的周期性在线模式。
    3.  **Online RLHF**：不带不确定性建模的纯在线增量更新。
    4.  **Information-Directed Exploration**：本文提出的完整算法。
*   **反馈模拟器**：使用基于 **Gemini 1.5 Pro** 训练的奖励模型来模拟人类真实偏好。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量或总训练时长。
*   **参数规模**：提到模型约为 90 亿参数（Gemma 9B），ENN 引入的额外参数量极小（占比小于 5%）。
*   **计算效率**：由于是在线增量更新，避免了离线 RLHF 中多次从头训练模型的巨大开销。

### 5. 实验数量与充分性
*   **实验规模**：在 20 万个 Prompt 的量级上进行了完整的训练曲线对比。
*   **消融实验**：针对“有无奖励模型”、“有无肯定性微调（Affirmative Nudge）”以及“不同学习率策略”进行了对比实验。
*   **客观性与公平性**：
    *   实验对比了不同算法在相同标注数量下的胜率。
    *   使用了对数坐标轴展示缩放定律，这比线性坐标轴更能客观反映长期的效率差异。
    *   通过展示“信息量最大（Infomax）”和“信息量最小（Infomin）”的回复对示例，直观证明了探索机制的有效性。

### 6. 论文的主要结论与发现
*   **效率飞跃**：在 2 万条标签时，该算法达到了离线 RLHF 使用 20 万条标签的性能（**10 倍提升**）。
*   **缩放定律偏移**：根据实验数据外推，当标签达到 100 万条时，该算法预计能匹配离线 RLHF 在 10 亿条标签下的表现（**1000 倍提升**）。
*   **稳定性发现**：引入“肯定性微调”是实现在线 RLHF 持续性能增长的关键，有效防止了模型在训练后期的退化。

### 7. 优点
*   **理论与实践结合**：将强化学习中的主动探索理论（IDS/ENN）成功应用于超大规模语言模型的对齐。
*   **极高的数据效率**：为解决人类反馈稀缺问题提供了极具前景的路径。
*   **稳定性创新**：提出的 Affirmative Nudge 简单且有效，解决了在线强化学习中的一个长期痛点。

### 8. 不足与局限
*   **模拟器依赖**：实验中的“人类反馈”是由 Gemini 1.5 Pro 模拟的，虽然模拟器很强大，但与真实人类的多样化、噪声偏好可能仍有偏差。
*   **计算复杂度**：虽然数据效率高，但在推理阶段需要为每个 Prompt 生成 16 个样本并进行多次 RM 推理来计算方差，这增加了训练过程中的实时计算开销。
*   **多轮对话覆盖不足**：目前的实验主要集中在单轮 Prompt-Response 模式，尚未在复杂的多轮对话或长期任务（Agent 场景）中完全验证。

（完）
