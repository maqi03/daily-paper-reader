---
title: "PLOT: Enhancing Preference Learning via Optimal Transport"
title_zh: PLOT：通过最优传输增强偏好学习
authors: "Liang Zhu, Yuelin Bai, Xiankun Ren, Jiaxi Yang, Lei Zhang, Feiteng Fang, Hamid Alinejad-Rokny, Minghuan Tan, Min Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01837v1"
tags: ["query:rm-opt"]
score: 9.0
evidence: 通过基于最优传输的标记级损失进行偏好学习
tldr: 针对大语言模型偏好学习中性能提升有限、计算成本高及缺乏全局特征建模等问题，本文提出 PLOT 框架。该方法将偏好学习建模为最优传输（Optimal Transport）问题，通过引入基于 Token 级别的损失函数，在对齐人类偏好的同时保留模型原始分布。PLOT 利用 Token 嵌入捕捉语义关系，实现了全局信息的优化。实验证明，该方法在人类价值观和逻辑解题等多个维度上均显著提升了对齐效果，并保持了生成的流畅性与连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01837v1/fig-001.webp\", \"caption\": \"Table 1: The Attack Success Rate (ASR) results for various models and preference learning methods under multiple red teaming attack scenarios consistently demonstrate that integrating LPLOT leads to a significant reduction in ASR across all evaluated models (Llama3.2-3B, Llama3.1-8B, and Qwen2.5-7B) and alignment techniques (SFT, DPO, PRO, and AOT). This highlights the universal effectiveness and robustness of PLOT in enhancing the preference learning capabilities of LLMs.\", \"page\": 6, \"index\": 1, \"width\": 941, \"height\": 804}, {\"url\": \"assets/figures/arxiv/2604.01837v1/fig-002.webp\", \"caption\": \"Table 2: Comparative evaluation of Instruct, DPO, and PLOT models across Human Values and Logic & Problem Solving, demonstrating consistent superiority of PLOT in multifaceted preference learning domains.\", \"page\": 7, \"index\": 2, \"width\": 904, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2604.01837v1/fig-003.webp\", \"caption\": \"Table 8: Detailed experimental results of Llama3.2-3B-Instruct for the Harmlessness preference learning. Attack Success Rate (ASR) is used to assess the defense capability of the model under various attack methods. PLOT demonstrates significant performance enhancements over DPO across all attack methods, showing improvements in each test data subset as well as the overall performance with better stability.\", \"page\": 14, \"index\": 3, \"width\": 887, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2604.01837v1/fig-004.webp\", \"caption\": \"Figure 2: The ASR curves of three models under different case counts for the Zero-Shot method (Left) and varying update steps of GCG (Right). PLOT consistently demonstrates superior defense capabilities and stability compared to DPO.\", \"page\": 13, \"index\": 4, \"width\": 936, \"height\": 354}]"
motivation: 现有的偏好学习方法存在计算成本高、超参数敏感以及难以捕捉全局 Token 级别语义关系等局限性。
method: 提出 PLOT 框架，利用最优传输理论构建 Token 级别的损失函数，通过 Token 嵌入实现模型输出与人类偏好的精准对齐。
result: 在人类价值观和逻辑问题解决等七个子偏好领域的实验表明，PLOT 在提升对齐性能的同时，确保了文本的流畅度与连贯性。
conclusion: 最优传输为大语言模型偏好学习提供了一个理论严密且行之有效的框架，为该领域的优化提供了新视角。
---

## 摘要
大语言模型（LLMs）中的偏好学习已取得显著进展，但现有方法仍受限于性能提升有限、计算成本高、超参数敏感以及对全局 token 级关系建模不足等问题。我们提出了 PLOT，通过源自最优传输（Optimal Transport）的 token 级损失函数，增强了基于微调的对齐中的偏好学习。通过将偏好学习建模为最优传输问题，PLOT 在使模型输出与人类偏好对齐的同时，保留了 LLMs 的原始分布，确保了稳定性和鲁棒性。此外，PLOT 利用 token 嵌入来捕捉语义关系，从而实现全局信息的优化。在涵盖人类价值观（Human Values）以及逻辑与问题解决（Logic & Problem Solving）两个偏好类别、共七个子偏好的实验中，结果表明 PLOT 在保持流畅性和连贯性的同时，持续提升了对齐性能。这些结果证实了最优传输作为偏好学习的一种原则性方法，建立了一个具有理论基础的框架，为 LLMs 的偏好学习提供了新的见解。

## Abstract
Preference learning in Large Language Models (LLMs) has advanced significantly, yet existing methods remain limited by modest performance gains, high computational costs, hyperparameter sensitivity, and insufficient modeling of global token-level relationships. We introduce PLOT, which enhances Preference Learning in fine-tuning-based alignment through a token-level loss derived from Optimal Transport. By formulating preference learning as an Optimal Transport Problem, PLOT aligns model outputs with human preferences while preserving the original distribution of LLMs, ensuring stability and robustness. Furthermore, PLOT leverages token embeddings to capture semantic relationships, enabling globally informed optimization. Experiments across two preference categories - Human Values and Logic & Problem Solving - spanning seven subpreferences demonstrate that PLOT consistently improves alignment performance while maintaining fluency and coherence. These results substantiate optimal transport as a principled methodology for preference learning, establishing a theoretically grounded framework that provides new insights for preference learning of LLMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PLOT (Preference Learning via Optimal Transport)** 的新框架，旨在通过最优传输理论改进大语言模型（LLM）的偏好对齐。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的 LLM 偏好学习方法（如 DPO、PPO 等）虽然取得了进展，但仍面临以下挑战：
*   **性能增益有限**：许多方法仅关注局部 token 的修改，缺乏对输出分布全局结构的优化。
*   **计算成本与敏感性**：复杂的损失函数导致资源消耗大，且模型对超参数（如 KL 散度系数）高度敏感。
*   **语义缺失**：现有 token 级对齐往往忽略了 token 之间的语义关联。
**研究动机**：引入**最优传输（Optimal Transport, OT）**理论，将偏好学习视为两个概率分布之间的最小代价变换问题，从而在保持模型原始分布稳定性的同时，利用语义信息实现全局优化。

### 2. 方法论：核心思想与技术细节
PLOT 的核心是将模型当前的输出分布 $Q_\theta$ 转化为目标偏好分布 $P$ 的过程建模为一个 OT 问题。
*   **核心思想**：计算将 $Q_\theta$ 移动到 $P$ 所需的最小“工作量”（传输成本），并将其作为附加损失函数。
*   **目标偏好分布 ($P$)**：通过计算正向样本和负向样本之间的 token 频率差异（$Q_{diff}$）并进行非负归一化处理，构建出代表人类偏好的目标分布。
*   **语义感知成本矩阵 ($C$)**：不同于传统的 0-1 代价矩阵，PLOT 利用模型的 **Token Embedding** 计算 token 之间的欧几里得距离，使得语义相近的 token 之间传输代价更小。
*   **算法简化**：为了解决大规模词表下 OT 计算复杂度高的问题，论文将问题简化为**一维 Wasserstein 距离**的计算，通过累积分布函数（CDF）的差值实现高效求解。
*   **总损失函数**：$L = L_{vanilla} + \alpha L_{PLOT}$，其中 $L_{vanilla}$ 是基础对齐算法（如 DPO）的损失，$\alpha$ 是调节权重。

### 3. 实验设计
*   **评估领域**：
    1.  **人类价值观**：无害性（Harmlessness）、有用性（Helpfulness）、人性化（Humanity）。
    2.  **逻辑与问题解决**：数学（GSM8K, MATH）、推理、代码、STEM。
*   **数据集**：HH-RLHF（用于无害性和有用性）、Magpie 序列子集（用于逻辑推理）、HarmBench（用于红队攻击测试）。
*   **Benchmark 与对比方法**：
    *   **对比基准**：SFT、DPO、PRO、AOT（基于 Batch 级的 OT 方法）。
    *   **评估工具**：GPT-4 评分、Reward Model 评分、攻击成功率（ASR）、AlpacaEval 2.0（通用能力）。
*   **模型架构**：Llama 3.2-3B-Instruct、Llama 3.1-8B-Instruct、Qwen 2.5-7B-Instruct。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 80GB GPU**。
*   **训练细节**：总 Batch Size 为 4，训练 1 个 epoch。
*   **时长**：论文提到 PLOT 相比 DPO 仅增加了约 **2.67%** 的训练时间（从 487 秒增至 500 秒左右，针对 4000 样本的实验），计算开销极小。

### 5. 实验数量与充分性
*   **实验规模**：在 3 种不同架构和规模的模型上进行了验证，涵盖了 7 个子偏好维度。
*   **红队攻击测试**：针对 6 种主流攻击方法（ZS, PEZ, GBDA, UAT, SFS, GCG）进行了详尽测试，每项实验重复 3 次并计算标准差。
*   **消融实验**：对比了有无 Embedding 引导、不同分布度量方法（KL, JS, OT）的效果，并验证了超参数 $\alpha$ 的稳定性。
*   **客观性**：实验设计较为全面，既包含了自动化的 Benchmark 评分，也引入了 GPT-4 作为裁判，并关注了对齐后模型通用能力的保持情况。

### 6. 主要结论与发现
*   **显著提升安全性**：在所有模型和基准方法上，加入 PLOT 损失后，红队攻击的成功率（ASR）均显著下降。
*   **多维度增强**：不仅在安全对齐上有效，在数学、代码和逻辑推理任务上也优于传统的 DPO 方法。
*   **高稳定性**：PLOT 对超参数的变化不敏感，表现出比 DEFT 等方法更强的鲁棒性。
*   **能力保留**：在 AlpacaEval 2.0 测试中，PLOT 有效缓解了对齐过程中常见的“对齐税”（即通用能力下降的问题）。

### 7. 优点（亮点）
*   **理论严密性**：将偏好学习提升到分布对齐的高度，利用最优传输理论提供了坚实的数学基础。
*   **语义感知**：通过引入 Token Embedding 构建成本矩阵，使模型能够理解 token 间的语义距离，而非简单的概率匹配。
*   **高效性**：通过一维 Wasserstein 距离简化计算，使得该方法在几乎不增加计算负担的情况下显著提升效果。
*   **通用性**：作为一个插件式的损失项，可以轻松集成到现有的各种微调对齐算法中。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 3B 到 8B 规模的模型，尚未在 70B 或更大规模的模型上验证其扩展性。
*   **数据量限制**：训练时每个任务仅随机采样了 4000 条数据，未深入探讨数据规模对 OT 学习效果的长尾影响。
*   **目标分布构建**：目标偏好分布 $P$ 的构建仍依赖于正负样本的频率差异，如果原始偏好数据质量不高，可能会限制 PLOT 的上限。

（完）
