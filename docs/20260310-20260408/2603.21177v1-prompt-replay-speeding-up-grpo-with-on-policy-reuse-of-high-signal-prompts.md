---
title: "Prompt replay: speeding up grpo with on-policy reuse of high-signal prompts"
title_zh: 提示重放：通过高信号提示的同策略重用加速 GRPO
authors: "Andrei Baroian, Rutger Berger"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21177v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 通过在线提示词复用加速GRPO训练
tldr: 本研究针对大语言模型推理任务中 GRPO 训练开销大、低信号 Prompt 浪费计算资源的问题，提出了 Prompt Replay 方法。该方法通过在线重用具有中等难度（通过率接近 0.5）的高信号 Prompt，在保持策略内优化的同时，显著减少了零方差样本，提高了优势函数的绝对值。实验表明，该方法在 Llama 和 Qwen 模型上能加快初期收敛速度，特别适用于计算资源受限且数据集较难的场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21177v1/fig-001.webp\", \"caption\": \"Figure 1: Prompt Replay visualization. In each step, we insert prompts with high learnability into a buffer. Training batches are formed by mixing reused prompts with fresh samples, with cooldown steps and max reuse times are controlling aggressiveness vs risk of overfitting.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2603.21177v1/fig-002.webp\", \"caption\": \"Figure 2: Prompt Replay vs Baseline (OLMo-RL). Prompt Replay shows: higher mean |Adv| (3rd row), resulting in more signal from the data; lower number of prompts (2nd row) with pass rate = 0, wasting less compute on unusable prompts; earlier gains in the average accuracy over 6 benchmarks (1st row), but plateaus and converges with the baseline; benchmark score is max at 10 instead of 100\", \"page\": 7, \"index\": 2, \"width\": 922, \"height\": 732}, {\"url\": \"assets/figures/arxiv/2603.21177v1/fig-003.webp\", \"caption\": \"Figure 5: Training dynamics for the main results: (row 1) steps vs. time, (row 2) sequence length, (row 3) verifiable reward, (row 4) policy entropy. Columns correspond to Llama 3.1 3B (Dolci), Qwen 3 8B (Dolci), and Qwen 3 8B (Polaris).\", \"page\": 17, \"index\": 3, \"width\": 1076, \"height\": 1286}, {\"url\": \"assets/figures/arxiv/2603.21177v1/fig-004.webp\", \"caption\": \"Table 1: Experiment Configuration Summary\", \"page\": 16, \"index\": 4, \"width\": 818, \"height\": 463}]"
motivation: 解决 GRPO 训练中昂贵的采样开销以及大量 Prompt 因缺乏学习信号而导致的计算资源浪费问题。
method: 提出一种无额外开销的在线数据选择方法，通过缓冲区重用通过率接近 0.5 的 Prompt，并结合冷却机制和最大重用次数防止过拟合。
result: 实验显示该方法有效减少了零方差 Prompt，提升了平均绝对优势值，并在多个数学基准测试中实现了更快的初期准确率增长。
conclusion: Prompt Replay 在采样为主要瓶颈且任务难度较高时最为高效，但需警惕过度重用导致的收敛平台期及特定模型的伪奖励效应。
---

## 摘要
具有可验证奖励的强化学习（RLVR）在扩展大语言模型（LLM）推理能力方面起着至关重要的作用，但 GRPO 风格的训练受限于昂贵的采样过程，且在无效提示上浪费了计算资源。我们提出了“提示重放”（Prompt Replay），这是一种针对 GRPO 的无额外开销在线数据选择方法，该方法仅重用提示（而非轨迹），以保持同策略优化。在每一步之后，我们将中等难度的提示插入缓冲区，并优先选择通过率接近 0.5（即一半答案正确，一半错误）的提示，以最大化优势函数，从而增强学习信号。训练批次通过混合重用提示与新样本形成，并利用冷却步数和最大重用次数来平衡激进程度与过拟合风险。在多个模型系列（Llama-3.2-3B、Qwen3-8B）和训练数据集（Dolci、Polaris）上，通过六个标准数学基准测试的平均准确率进行评估，结果表明提示重放减少了零方差提示，增加了平均绝对优势，并显示出更快的初始准确率增长。然而，由于使用了过于激进的配置，其表现最终会进入平台期并与基准线收敛。当采样是主要瓶颈且数据集对模型具有挑战性时，该方法效率最高。此外，我们观察到 Qwen2.5-Math 可能会表现出伪奖励效应，从而导致消融实验失效，这为将其作为 GRPO 方法研究的唯一测试平台发出了警告信号。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) plays a crucial role in expanding the capacities of LLM reasoning, but GRPO-style training is dominated by expensive rollouts and wastes compute on unusable prompts. We propose Prompt Replay, an overhead-free online data selection method for GRPO that reuses prompts only (not trajectories), to preserve on-policy optimization. After each step, we insert prompts with medium difficulty into a buffer, and prioritize prompts closer to a pass rate of 0.5 (half answers correct, half wrong) to maximize the advantage, thus learning signal. Training batches are formed by mixing reused prompts with fresh samples, with cooldown steps and max reuse times controlling aggressiveness vs risk of overfitting. Across multiple model families (Llama-3.2- 3B, Qwen3-8B) and training datasets (Dolci, Polaris), evaluated using average accuracy on six standard math benchmarks, Prompt Replay reduces zero-variance prompts, increases mean absolute advantage and shows faster initial accuracy gains. Yet, it plateaus and converges with the baseline, as too aggressive configuration was used. The method is most efficient when the rollouts are the primary bottleneck and the dataset is difficult for the model. We additionally observe that Qwen2.5-Math can exhibit spurious-reward effects that invalidates ablations, raising a warning signal for using it as a sole testbed for GRPO method research.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prompt Replay: Speeding Up GRPO with On-Policy Reuse of High-Signal Prompts》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于大语言模型（LLM）推理任务中的**强化学习（RLVR）效率问题**。目前主流的 GRPO（群体相对策略优化）算法虽然省去了复杂的评论家模型（Critic），但其计算成本依然被昂贵的**采样（Rollouts）**过程主导。
*   **痛点**：在训练过程中，大量计算资源被浪费在“零方差”提示词（Prompts）上——即模型要么全对、要么全错的题目，这些题目无法提供有效的学习信号（优势函数为零）。
*   **目标**：提出一种动态数据选择机制，通过重用那些能产生高学习信号的提示词，在不增加额外计算开销的前提下加速 GRPO 训练。

### 2. 方法论：Prompt Replay
核心思想是**“只重用题目，不重用答案”**，以确保训练过程严格遵循**同策略（On-policy）**优化。
*   **高信号筛选**：定义“通过率”（Pass Rate, $p_\theta(x)$）为一组采样中正确答案的比例。理论证明，当 $p_\theta(x) \approx 0.5$ 时，奖励方差最大，提供的梯度信号最强。
*   **重用缓冲区（Replay Buffer）**：
    1.  **插入**：每步训练后，将通过率在 $[0.25, 0.75]$ 之间的提示词存入缓冲区。
    2.  **排序与优先级**：优先选择通过率最接近 0.5 的提示词。
    3.  **混合采样**：训练批次由 $\epsilon$ 比例的重用提示词和 $(1-\epsilon)$ 比例的新提示词组成。
*   **正则化机制**：引入**冷却步数（Cooldown）**限制同一题目被连续使用的频率，以及**最大重用次数（Max Reuse）**限制总重用次数，以防止模型对特定题目过拟合。

### 3. 实验设计
*   **模型**：Llama-3.2-3B、Qwen3-8B，以及用于消融实验的 Qwen2.5-Math-1.5B。
*   **训练数据集**：Dolci RL-Zero Math (13k 样本) 和 Polaris (53k 样本，难度更高)。
*   **基准测试（Benchmarks）**：使用 6 个标准数学评测集的平均准确率：AIME24、AIME25、AMC、MATH500、OlympiadBench、MinervaMath。
*   **对比方法**：以最新的开源强化学习框架 **OLMo-RL** 作为强基准线（Baseline）。

### 4. 资源与算力
论文详细列出了硬件配置：
*   **硬件**：使用了 2 到 3 个节点，每个节点配备 **4 张 NVIDIA H100 (95GB)** GPU。
*   **分配**：通常 1 个节点用于学习器（权重更新），1-2 个节点用于采样器（vLLM 引擎）。
*   **时长与规模**：总计算预算约为 **3000 GPU 小时**。每步训练包含 32 个提示词，每个提示词采样 16 次（共 512 个采样/步）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流模型家族、两种不同规模和难度的训练集，以及针对冷却步数的消融实验。
*   **充分性**：实验设计较为客观，通过对比“平均绝对优势值”和“零方差提示词数量”验证了方法论的底层逻辑。
*   **局限性**：由于算力限制，未进行多随机种子运行以获取统计显著性（虽然作者引用文献说明 GRPO 变体方差通常在 2% 以内）。

### 6. 主要结论与发现
*   **效率提升**：Prompt Replay 显著减少了零方差提示词，提高了平均绝对优势值，使模型在训练初期能更快达到较高的准确率。
*   **适用场景**：当**采样过程是主要瓶颈**且**数据集对模型而言较难**时，该方法效果最明显。
*   **收敛特性**：虽然初期增长快，但最终会进入平台期并与基准线收敛。这可能是由于配置过于激进导致了局部过拟合。
*   **警示发现**：发现 Qwen2.5-Math 模型存在**“伪奖励效应”**（即使给随机奖励准确率也会提升），这可能导致相关消融实验失效，建议研究者不要仅在该系列模型上测试算法。

### 7. 优点
*   **零额外开销**：不需要额外的奖励模型或复杂的难度预测模型，直接利用训练过程中的统计数据。
*   **保持同策略性**：通过重新采样答案而非存储旧轨迹，避免了离策略（Off-policy）带来的噪声。
*   **理论支撑**：基于梯度信号能量（Gradient Signal Energy）提供了严谨的数学解释。

### 8. 不足与局限
*   **超参数敏感**：冷却步数、重用比例等超参数对防止过拟合至关重要，但论文尚未给出自动化的最优配置方案。
*   **后期平台期**：在训练后期，该方法未能表现出超越基准线的最终性能，甚至可能因为重用过多而提前陷入停滞。
*   **模型依赖性**：在已经非常强大的模型（如 Qwen3-8B）上，由于其本身产生的零方差提示词较少，该方法的加速收益会有所稀释。

（完）
