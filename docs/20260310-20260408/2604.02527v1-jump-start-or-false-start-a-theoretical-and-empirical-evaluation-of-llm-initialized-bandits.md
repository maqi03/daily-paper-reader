---
title: Jump Start or False Start? A Theoretical and Empirical Evaluation of LLM-initialized Bandits
title_zh: 抢跑还是误跑？LLM 初始化的多臂老虎机算法的理论与实证评估
authors: "Adam Bayley, Xiaodan Zhu, Raquel Aoki, Yanshuai Cao, Kevin H. Wilson"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02527v1"
tags: ["query:rm-trn"]
score: 9.0
evidence: 评估带有随机和标签翻转噪声的LLM生成偏好数据
tldr: 本研究探讨了利用大语言模型（LLM）生成的合成偏好数据为上下文多臂老虎机（Bandit）提供热启动的有效性。通过在合成数据中注入随机和标签翻转噪声，并结合理论分析，研究了LLM先验在不同对齐程度下的表现。结果显示，虽然LLM能显著降低早期遗憾，但在高噪声或系统性偏差下可能适得其反。本文提出了LLM热启动优于冷启动的理论充分条件，并跨多个数据集验证了对齐度与推荐质量之间的可靠关联。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02527v1/fig-001.webp\", \"caption\": \"Table 1: Percentage reduction in cumulative regret (%∆ Regret) for the COVID-19 Vaccine dataset using GPT-4o priors. Reported across three synthetic dataset sizes (N). Mean over G = 10 seeds ± 95% CI.\", \"page\": 11, \"index\": 1, \"width\": 518, \"height\": 653}, {\"url\": \"assets/figures/arxiv/2604.02527v1/fig-002.webp\", \"caption\": \"Figure 3: Cumulative regret on the COVID-19 Vaccine dataset under random-response noise.\", \"page\": 11, \"index\": 2, \"width\": 929, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2604.02527v1/fig-003.webp\", \"caption\": \"Figure 2: Cumulative regret on the COVID-19 Vaccine dataset under preference-flipping noise. “10k_fX” indicates X times 10% of LLM-generated labels flipped. Shaded regions are 95 percent CI over G = 10 runs.\", \"page\": 10, \"index\": 3, \"width\": 490, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.02527v1/fig-004.webp\", \"caption\": \"Table 2: Percentage reduction in cumulative regret (%∆ Regret) compared to cold-start. Reported across 4 models, 3 datasets, and 3 representative noise levels (p ∈ {0, 0.3, 0.5}). Mean over G = 10 seeds.\", \"page\": 13, \"index\": 4, \"width\": 772, \"height\": 982}, {\"url\": \"assets/figures/arxiv/2604.02527v1/fig-005.webp\", \"caption\": \"Table 5: Inference parameters.\", \"page\": 24, \"index\": 5, \"width\": 632, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2604.02527v1/fig-006.webp\", \"caption\": \"Table 6: GPT-3.5 Turbo results from older snapshot under preference flipping; percentage reduction in cumulative regret (%∆ Regret) versus a cold-start LinUCB baseline. Mean over G = 10 seeds ± 95 % CI.\", \"page\": 24, \"index\": 6, \"width\": 660, \"height\": 254}]"
motivation: 旨在评估利用LLM生成的合成数据进行Bandit算法热启动时，模型对齐程度和数据噪声对算法性能的具体影响。
method: 通过系统性注入随机和标签翻转噪声，并结合理论分析分解先验误差，推导LLM热启动优于冷启动的充分条件。
result: "实验发现热启动在噪声低于30%时有效，但在噪声超过50%或存在系统性偏差时，其表现会劣于传统的冷启动Bandit。"
conclusion: LLM初始化是一把双刃剑，其有效性高度依赖于合成数据与真实偏好的对齐度，且该对齐度可作为预测推荐质量的可靠指标。
---

## 摘要
大语言模型（LLMs）的最新进展为生成用户偏好数据以实现多臂老虎机（bandits）的热启动提供了新机遇。最近关于 LLM 初始化上下文多臂老虎机（CBLI）的研究表明，这些合成先验可以显著降低早期遗憾值（regret）。然而，这些发现假设 LLM 生成的选择与实际用户偏好具有合理的对齐性。在本文中，我们系统地研究了在合成训练数据中注入随机噪声和标签翻转噪声时，LLM 生成的偏好表现如何。对于已对齐的领域，我们发现热启动在损坏率达到 30% 时仍然有效，在 40% 左右失去优势，并在超过 50% 时导致性能下降。当存在系统性失配（misalignment）时，即使没有添加噪声，LLM 生成的先验也可能导致比冷启动老虎机更高的遗憾值。为了解释这些行为，我们开发了一种理论分析方法，将随机标签噪声和系统性失配对驱动老虎机遗憾值的先验误差的影响进行分解，并推导出了基于 LLM 的热启动在证明上优于冷启动老虎机的充分条件。我们在多个联合分析（conjoint）数据集和 LLM 上验证了这些结果，表明估计的对齐程度能够可靠地追踪热启动何时会提高或降低推荐质量。

## Abstract
The recent advancement of Large Language Models (LLMs) offers new opportunities to generate user preference data to warm-start bandits. Recent studies on contextual bandits with LLM initialization (CBLI) have shown that these synthetic priors can significantly lower early regret. However, these findings assume that LLM-generated choices are reasonably aligned with actual user preferences. In this paper, we systematically examine how LLM-generated preferences perform when random and label-flipping noise is injected into the synthetic training data. For aligned domains, we find that warm-starting remains effective up to 30% corruption, loses its advantage around 40%, and degrades performance beyond 50%. When there is systematic misalignment, even without added noise, LLM-generated priors can lead to higher regret than a cold-start bandit. To explain these behaviors, we develop a theoretical analysis that decomposes the effect of random label noise and systematic misalignment on the prior error driving the bandit's regret, and derive a sufficient condition under which LLM-based warm starts are provably better than a cold-start bandit. We validate these results across multiple conjoint datasets and LLMs, showing that estimated alignment reliably tracks when warm-starting improves or degrades recommendation quality.

---

## 论文详细总结（自动生成）

这篇论文《Jump Start or False Start? A Theoretical and Empirical Evaluation of LLM-initialized Bandits》对利用大语言模型（LLM）初始化上下文多臂老虎机（Contextual Bandits）的可靠性进行了深入探讨。以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在在线学习中，多臂老虎机算法面临“冷启动”挑战，即在初期因缺乏数据而进行随机探索，导致高昂的“遗憾值”（Regret）。
*   **背景**：近期研究提出利用 LLM 模拟用户偏好生成合成数据来“热启动”算法（CBLI 框架）。然而，这一做法隐含了一个假设：LLM 的偏好与真实人类偏好是高度对齐的。
*   **核心问题**：如果 LLM 生成的先验知识存在噪声或系统性偏差，热启动是否会变成“误导性启动”？论文旨在量化这种鲁棒性边界。

### 2. 方法论
*   **Noisy-CBLI 框架**：在 LLM 生成的合成数据中主动注入两种噪声：
    1.  **随机响应替换（Random Replacement）**：模拟 LLM 的无意义错误或不确定性。
    2.  **偏好翻转（Preference Flipping）**：将 LLM 的选择反转，模拟系统性偏差或错误对齐。
*   **理论分析**：
    *   引入**先验误差项 $B_0 = \|\theta_0 - \theta^*\|_{A_0}$**，将预训练效果浓缩为一个标量。
    *   推导了**先验中心置信边界（Prior-centered confidence bound）**，证明了预训练如何通过改变探索半径来影响遗憾值。
    *   提出了热启动优于冷启动的**充分条件**：即合成先验带来的方差减少必须大于其系统性偏差带来的误差增加。

### 3. 实验设计
*   **数据集**：使用了三个真实的人类联合分析（Conjoint）调查数据集：
    1.  **COVID-19 疫苗选择**：美国成年人对疫苗属性的偏好。
    2.  **移民态度**：对不同背景移民申请人的接纳倾向。
    3.  **休闲旅游**：对旅游目的地的选择偏好。
*   **Benchmark**：以“冷启动”（Cold-start）的 LinUCB 算法（无预训练）作为基准。
*   **对比方法**：对比了不同 LLM（GPT-4o, GPT-3.5 Turbo, Llama 3.1, Qwen 3）在不同噪声水平（0% 到 70%）下的热启动表现。

### 4. 资源与算力
*   **硬件环境**：20 核 Intel Core i7-14700F CPU，32GB RAM，单张 **NVIDIA GeForce RTX 4070 Ti SUPER (16GB)** GPU。
*   **训练时长**：生成 10k 条合成数据并运行 10 轮独立实验的完整流程在 **2 小时内**完成。
*   **API 使用**：调用了 OpenAI 的 GPT 系列 API。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种模型、3 个数据集、2 种噪声类型、8 个噪声等级（0%-70%）以及 3 种合成数据规模（1k, 3k, 10k）。
*   **充分性**：每组实验均运行了 10 个独立随机种子并计算 95% 置信区间。实验设计考虑了模型版本漂移（对比了 GPT-3.5 的新旧版本），体现了较高的客观性和严谨性。

### 6. 主要结论与发现
*   **噪声容忍度**：在对齐良好的领域，热启动在**噪声低于 30%** 时依然有效；在 40% 左右优势消失；**超过 50% 时会产生负迁移**，表现劣于冷启动。
*   **噪声类型差异**：算法对“随机噪声”较为鲁棒，但对“偏好翻转”（系统性偏差）极其敏感。
*   **对齐是关键**：在某些任务（如旅游数据集）中，LLM 原生偏好就与人类不一致，此时即使不加噪声，热启动也会导致更高的遗憾值。
*   **理论验证**：实验观察到的性能拐点与论文提出的理论先验误差 $B_0$ 的变化趋势高度吻合。

### 7. 优点
*   **理论与实证结合**：不仅通过实验展示了现象，还通过数学推导给出了热启动失效的理论解释。
*   **现实指导意义**：为从业者提供了“对齐诊断”的思路，即在部署前应先评估 LLM 先验的误差。
*   **发现“模型漂移”**：敏锐地观察到同一模型家族（如 GPT-3.5）在不同时间版本的更新会显著改变其作为先验知识的质量。

### 8. 不足与局限
*   **噪声模型简化**：注入的噪声是独立同分布的，而现实中 LLM 的错误可能具有复杂的上下文相关性。
*   **提示词敏感性**：实验主要基于固定模板，未深入探讨 Prompt Engineering 对先验质量的剧烈波动影响。
*   **伦理风险**：论文指出 LLM 可能携带人口统计学偏见，若直接用于热启动，可能导致推荐系统在初期就固化这些偏见，产生公平性问题。

（完）
