---
title: "V2A-DPO: Omni-Preference Optimization for Video-to-Audio Generation"
title_zh: V2A-DPO：面向视频转音频生成的全方位偏好优化
authors: "Nolan Chan, Timmy Gang, Yongqian Wang, Yuzhe Liang, Dingdong Wang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11089v1"
tags: ["query:rm-opt"]
score: 9.0
evidence: 用于视频转音频生成对齐的DPO框架
tldr: 本研究提出V2A-DPO框架，旨在通过直接偏好优化（DPO）提升视频转音频生成的质量。该框架引入了AudioScore评分系统，从语义一致性、时间对齐和感知质量三个维度评估音频，并利用自动化流水线生成大规模偏好数据。结合针对流匹配模型设计的课程学习策略，该方法显著提升了生成音频与人类偏好的对齐度，在VGGSound数据集上达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.11089v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration of the proposed V2A-DPO framework, including (a) our proposed AudioScore to rate the generated audio with multidimensional scores; (b) omni-preference pair data generation combining the automatically generated preference pairs based on AudioScore with a small amount of human-annotated preference pairs; (c) curriculum learning-empowered DPO to optimize V2A models on the complex and simple pairs gradually, which are split according to complexity score scorec.\", \"page\": 3, \"index\": 1, \"width\": 964, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2603.11089v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on the effect of the KL divergence constraint parameter β and preset threshold score∆ on the performance of optimized MMAudio using V2A-DPO.\", \"page\": 4, \"index\": 2, \"width\": 517, \"height\": 252}]"
motivation: 现有的视频转音频生成模型在语义一致性、时间对齐和感知质量方面仍难以完全符合人类偏好。
method: 提出了包含AudioScore评分系统、自动化偏好数据生成流水线以及针对流匹配模型的课程学习DPO优化策略。
result: 实验表明，经过V2A-DPO优化的模型在VGGSound数据集上超越了DDPO及预训练基准，MMAudio模型更是取得了SOTA性能。
conclusion: V2A-DPO通过全方位偏好优化有效提升了视频转音频生成的质量，证明了DPO在流匹配生成模型中的适用性。
---

## 摘要
本文介绍了 V2A-DPO，这是一种专为基于流（flow-based）的视频转音频（V2A）生成模型量身定制的新型直接偏好优化（DPO）框架，通过引入关键适配，有效地使生成的音频与人类偏好保持一致。我们的方法包含三项核心创新：(1) AudioScore——一个与人类偏好对齐的综合评分系统，用于评估合成音频的语义一致性、时间对齐度和感知质量；(2) 一个由 AudioScore 驱动的自动化流水线，用于生成大规模偏好对数据以进行 DPO 优化；(3) 一种专为基于流的生成模型定制的课程学习增强型 DPO 优化策略。在基准 VGGSound 数据集上的实验表明，使用 V2A-DPO 进行人类偏好对齐的 Frieren 和 MMAudio 模型，其表现优于使用去噪扩散策略优化（DDPO）优化的对应模型以及预训练基准模型。此外，经过 DPO 优化的 MMAudio 在多项指标上达到了最先进的性能，超越了已发表的 V2A 模型。

## Abstract
This paper introduces V2A-DPO, a novel Direct Preference Optimization (DPO) framework tailored for flow-based video-to-audio generation (V2A) models, incorporating key adaptations to effectively align generated audio with human preferences. Our approach incorporates three core innovations: (1) AudioScore-a comprehensive human preference-aligned scoring system for assessing semantic consistency, temporal alignment, and perceptual quality of synthesized audio; (2) an automated AudioScore-driven pipeline for generating large-scale preference pair data for DPO optimization; (3) a curriculum learning-empowered DPO optimization strategy specifically tailored for flow-based generative models. Experiments on benchmark VGGSound dataset demonstrate that human-preference aligned Frieren and MMAudio using V2A-DPO outperform their counterparts optimized using Denoising Diffusion Policy Optimization (DDPO) as well as pre-trained baselines. Furthermore, our DPO-optimized MMAudio achieves state-of-the-art performance across multiple metrics, surpassing published V2A models.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《V2A-DPO: Omni-Preference Optimization for Video-to-Audio Generation》** 的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频转音频（V2A）生成模型虽然在语义匹配上取得了进展，但仍面临三大挑战：
    1.  **风格控制受限**：模型往往局限于训练集的风格，缺乏灵活性。
    2.  **审美质量缺失**：现有模型忽视了音频的“美感”和“沉浸感”，难以通过显式奖励建模。
    3.  **评估体系孤立**：缺乏一个能同时整合语义一致性、时间对齐和感知质量的综合评分系统。
*   **研究背景**：随着视频生成技术的爆发，为无声视频配上高质量、同步且符合人类审美的音频成为迫切需求。

### 2. 方法论：V2A-DPO 框架
该论文提出了一个全方位的偏好优化框架，核心包含三个部分：
*   **AudioScore 评分系统**：
    *   整合了五个维度的自动化指标：ImageBind（视频-音频语义）、CLAP（文本-音频语义）、Synchformer（时间对齐度 DeSync）、PANNs-based IS（感知质量）和 PESQ（语音质量）。
    *   通过少量人工标注数据（2K条）训练一个简单的 MLP 模块，将上述五维指标映射为“好、中、差”三档概率，从而实现与人类偏好的对齐。
*   **自动化偏好对生成流水线**：
    *   利用预训练模型对同一视频生成多个样本，通过 AudioScore 选出概率最高的作为“赢家（winning）”，概率最低的作为“输家（losing）”。
    *   最终构建了包含 46K 自动生成对和 2K 人工标注对（侧重审美）的偏好数据集。
*   **课程学习增强型 DPO（Flow-DPO）**：
    *   **适配流匹配（Flow Matching）**：将 DPO 目标函数引入流匹配模型，通过调整预测向量场，使其向“偏好样本”靠近，远离“非偏好样本”。
    *   **课程学习策略**：根据 AudioScore 计算样本的“复杂度得分”。训练分为两个阶段：先学习差异明显的简单样本，再学习细微差异的复杂样本，以保证训练稳定性。

### 3. 实验设计
*   **数据集**：使用 **VGGSound** 基准数据集（包含 310 个类别的视频）。
*   **基准模型（Baselines）**：
    *   **预训练模型**：MMAudio 和 Frieren（作为 DPO 优化的底座）。
    *   **对比方法**：DDPO（基于强化学习的扩散策略优化）、Seeing&Hearing、FoleyCrafter、V2A-Mapper、V-AURA、ThinkSound 等。
*   **评估指标**：FD (PaSST)、KL (PANNs/PaSST)、IS（感知质量）、IB-score（语义对齐）、DeSync（时间同步误差）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   训练步数为 12K steps。
    *   全局 Batch Size 为 8。
    *   学习率为 5e-6，包含 1K 步的线性预热。
    *   推理时使用 Classifier-Free Guidance (CFG)，缩放因子为 4.5。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同规模的模型（1.03B 的 MMAudio 和 159M 的 Frieren）上验证了通用性。
    *   进行了针对 KL 约束参数 $\beta$ 和课程学习阈值 $\Delta$ 的消融实验。
*   **充分性评价**：实验设计较为充分，涵盖了分布匹配、音频质量、语义对齐和时间对齐四个核心维度。通过与 DDPO 的对比，证明了 DPO 在该任务上的优越性。对比了多种 SOTA 模型，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：V2A-DPO 显著提升了现有模型的表现。MMAudio 经过优化后，IS 指标提升了 10.4%，时间对齐误差（DeSync）降低了 20.5%。
*   **SOTA 地位**：优化后的 MMAudio 在绝大多数指标上超越了目前已发表的所有 V2A 模型。
*   **课程学习的必要性**：消融实验证明，如果不使用课程学习（即退化为普通 DPO），模型性能会显著下降，证明了由易到难学习偏好的重要性。

### 7. 优点与亮点
*   **首创性**：首次将 DPO 引入基于流匹配的视频转音频生成任务。
*   **综合评估**：AudioScore 解决了单一指标无法衡量综合偏好的问题，尤其是将“审美”这一主观因素通过人工标注引入了自动化流程。
*   **训练稳定性**：通过课程学习解决了 DPO 在处理复杂/模糊偏好对时可能出现的训练不稳定问题。

### 8. 不足与局限
*   **语义理解上限**：尽管在多项指标领先，但在语义对齐（IB-score）上略逊于 ThinkSound，这可能是因为对方使用了大语言模型（LLM）进行思维链（CoT）推理，而本方法更侧重于生成端的对齐。
*   **数据依赖**：AudioScore 的准确性高度依赖于基础模型（如 ImageBind, CLAP）的特征提取能力，若基础模型存在偏差，偏好对的质量也会受损。
*   **计算开销**：虽然推理不增加成本，但生成 50K 偏好对数据并进行 DPO 训练仍需要较大的算力支持。

（完）
