---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v2"
tags: ["query:pr-opt"]
score: 9.0
evidence: 协同提示词优化与策略优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“困难样本”探索效率低、奖励稀疏问题，本文提出P^2O框架。该框架结合了提示词优化与策略优化，通过GEPA算法为困难样本进化出更优提示词，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O在分布内和分布外基准测试中均表现优异，显著提升了模型的推理能力和泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21877v2/fig-001.webp\", \"caption\": \"Table 1. Comparative Evaluation on Mathematical Reasoning Benchmarks. We report the accuracy (%) of P2O against baselines. P2O consistently outperforms the baselines, achieving the most significant gains on challenging benchmarks such as AIME24 and AIME25. Best results are highlighted in bold.\", \"page\": 6, \"index\": 1, \"width\": 898, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2603.21877v2/fig-002.webp\", \"caption\": \"Figure 3. Training Dynamics of P2O. The plots illustrate the dynamics of Training Reward (Left) and Validation Accuracy (Right) throughout the optimization process using the Teacher-Ref variant on the DeepScaler-5K dataset. P2O maintains a higher reward compared to the baseline by dynamically cracking hard samples. Crucially, this reward advantage translates into robust gains in validation accuracy, confirming that the context distillation mechanism effectively transfers prompt-dependent success into intrinsic model capability.\", \"page\": 7, \"index\": 2, \"width\": 1003, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2603.21877v2/fig-003.webp\", \"caption\": \"Figure 5. Qualitative Analysis: Overcoming Local Optima in Geometric Reasoning.\", \"page\": 13, \"index\": 3, \"width\": 895, \"height\": 1300}, {\"url\": \"assets/figures/arxiv/2603.21877v2/fig-004.webp\", \"caption\": \"Figure 4. Prompt Optimization Effectiveness. Left: Optimized prompts (triangles) consistently yield gains over the standard policy (circles) on both Pass@1 and Pass@6 metrics, demonstrating that GEPA effectively assists the model in bridging the performance gap. Right: With this assistance, the model continuously conquers hard samples, resulting in a steady decline in the number of intractable instances throughout the training epochs. Data points are derived from the Teacher-Ref variant on the DeepScaler-5K dataset.\", \"page\": 8, \"index\": 4, \"width\": 1003, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2603.21877v2/fig-005.webp\", \"caption\": \"Figure 2. Overview of the P2O Framework. The training process is formulated as an alternating maximization procedure between two phases: (1) Policy Optimization with Context Distillation, where the policy πθ is updated to internalize reasoning patterns elicited by augmented inputs x̃; and (2) Evolutionary Prompt Optimization, where the prompt template set Z is evolved using GEPA to discover successful trajectories for the remaining hard samples (Dhard).\", \"page\": 4, \"index\": 5, \"width\": 1017, \"height\": 379}]"
motivation: 传统的强化学习在面对成功率接近零的困难样本时，因奖励极其稀疏而导致模型难以获得有效的监督信号。
method: 提出P^2O框架，利用遗传帕累托算法优化提示词以引导模型探索困难样本，并将优化后的推理成果蒸馏进模型参数。
result: "实验显示P^2O不仅在分布内数据集上表现卓越，在分布外基准测试中也实现了平均4.7%的性能提升。"
conclusion: 通过协同提示词与策略优化，P^2O有效解决了强化学习中的探索难题，增强了大型语言模型的推理与泛化能力。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，尤其是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有极高的信息价值，但实际上却使模型因缺乏监督信号而陷入停滞。为解决这一问题，我们提出了 P^2O，这是一个将提示词优化与策略优化协同结合的新颖框架。P^2O 在训练迭代中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，从而引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。广泛的实验表明，P^2O 不仅在分布内数据集上取得了卓越性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

这是一份关于论文《P^2O: Joint Policy and Prompt Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在针对大语言模型（LLM）推理能力的强化学习（RLVR，如 GRPO）中，存在**“困难样本探索瓶颈”**。
*   **研究背景**：当模型面对极难的推理题时，其初始成功率接近零。由于 RLVR 依赖稀疏的结果奖励（正确或错误），模型在这些样本上无法获得有效的梯度反馈（优势函数为零），导致模型只能在简单样本上过拟合，而无法攻克复杂难题。
*   **整体含义**：论文提出将“提示词优化”作为一种辅助手段，引导模型在训练期间探索出正确的推理路径，从而打破强化学习中的探索僵局。

### 2. 方法论：P^2O 框架
P^2O 采用**交替最大化（Alternating Maximization）**的策略，分为两个核心阶段：

*   **核心思想**：将提示词模板视为动态潜变量。如果原始输入 $x$ 无法产生正确答案，则寻找一个提示词 $z$，使得增强后的输入 $T(x, z)$ 能诱导出正确的推理轨迹。
*   **阶段一：带上下文蒸馏的策略优化**
    *   **困难样本挖掘**：在训练中识别出成功率低于阈值 $\tau$ 的样本。
    *   **上下文蒸馏（Context Distillation）**：这是关键技术。模型使用增强后的输入（带优化提示词）生成正确轨迹，但在计算策略梯度更新参数时，**输入端使用的是原始问题 $x$**。这样做的目的是让模型将提示词带来的推理能力“内化”到自身参数中，而非依赖提示词。
*   **阶段二：进化提示词优化**
    *   **GEPA 算法**：利用遗传-帕累托（Genetic-Pareto）算法。它使用一个“反射模型”（Reflection LLM）分析失败案例并生成改进的提示词候选。
    *   **帕累托选择与贪婪分配**：维护一个多样化的提示词池，通过帕累托前沿选择最优且多样的模板，并将其分配给下一轮训练中的困难样本。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DeepScaler-5K（5000个样本）和 DeepMath-5K（难度 $\ge 7$ 的5000个样本）。
    *   **测试集（Benchmark）**：AIME24、AIME25、AMC、MATH500、MINERVA、OLYMPIAD 等六个极具挑战性的数学竞赛基准。
*   **对比方法**：
    *   基础模型：Qwen3-0.6B/1.7B/4B。
    *   强化学习基准：标准的 GRPO 算法。
    *   P^2O 变体：P^2O Self-Ref（自反射）和 P^2O Teacher-Ref（使用 Kimi-K2 作为外部老师进行提示词进化）。

### 4. 资源与算力
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **已知参数**：训练使用了 Qwen3-4B 作为骨干模型，训练 5 个 Epoch，全局 Batch Size 为 128，最大生成长度 12k tokens，Rollout 采样数 $K=6$。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的 5K 训练集上进行了完整训练，并在 6 个主流数学基准上进行了验证。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：验证了如果不使用蒸馏（直接在带提示词的输入上训练），模型会产生依赖性，导致测试性能大幅下降。
    2.  **提示词多样性**：验证了在 Rollout 组内使用多样化提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为充分，涵盖了分布内和分布外的泛化性测试。对比了自进化和外部老师进化两种模式，客观地展示了不同策略在不同数据集上的表现差异。

### 6. 主要结论与发现
*   **性能提升**：P^2O 显著优于 GRPO 基准。在 DeepScaler-5K 上，平均准确率提升了 4.7%；在极难的 AIME 榜单上，提升幅度高达 12.3% 以上。
*   **打破局部最优**：实验证明，优化后的提示词能有效引导模型跳出奖励稀疏的“死区”，发现新的高质量推理路径。
*   **内化效应**：通过上下文蒸馏，模型在推理时无需任何额外提示词，即可展现出通过训练获得的增强能力。
*   **反射源的影响**：发现反射模型的选择具有任务相关性。在某些任务中，模型自反射的效果甚至优于更强的外部老师模型。

### 7. 优点与亮点
*   **协同效应**：首次将离散的提示词搜索与连续的参数优化在 RLVR 框架下深度结合。
*   **解决冷启动/探索难题**：为 RL 训练中“模型完全不会做”的样本提供了系统性的解决方案，而非简单的课程学习。
*   **推理内化**：通过蒸馏机制确保了推理能力的提升是模型本质的增强，而非推理侧的工程技巧。

### 8. 不足与局限
*   **计算开销**：虽然 GRPO 节省了 Critic 显存，但 P^2O 在训练过程中引入了额外的提示词进化步骤（涉及多次 LLM 调用和验证），会显著增加训练的总时间成本。
*   **样本规模**：实验主要集中在 5K 规模的小样本集上，在大规模数据集（如数十万样本）上的扩展性和成本效益尚待验证。
*   **领域局限**：目前仅在数学推理任务上进行了验证，对于缺乏明确可验证奖励（Verifiable Rewards）的开放域任务（如创意写作、对话）是否适用仍存疑。

（完）
