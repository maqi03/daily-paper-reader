---
title: "MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models"
title_zh: MoE-GRPO：在视觉语言模型中通过强化学习优化混合专家模型
authors: "Dohwan Ko, Jinyoung Park, Seoung Choi, Sanghyeok Lee, Seohyun Lee, Hyunwoo J. Kim"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24984v1"
tags: ["query:rm-opt"]
score: 9.0
evidence: 用于优化VLM专家路由的MoE-GRPO框架
tldr: 针对视觉语言模型（VLM）中混合专家（MoE）架构存在的确定性Top-K路由导致专家过拟合及组合非最优的问题，本文提出MoE-GRPO框架。该方法将专家选择建模为序列决策问题，利用组相对策略优化（GRPO）强化学习算法，结合模态感知路由引导，实现了更具多样性和自适应性的专家路由策略，在多模态任务中显著提升了模型性能与专家利用率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24984v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison between top-K routing and our MoEGRPO. (a) While the top-K routing deterministically selects K experts based on gating scores, (b) MoE-GRPO stochastically samples K experts across multiple rollouts and optimizes the expert selection policy through reward-based feedback.\", \"page\": 1, \"index\": 1, \"width\": 491, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2603.24984v1/fig-002.webp\", \"caption\": \"Figure 2. Overall pipeline of MoE-GRPO. Given an input image (or video) and a question, denoted as x, the rollout module gold samples G expert routing policies, i.e., {Ei}Gi=1 ∼ gold(E|x), where each policy Ei represents a sequence of expert selections across layers. Under each rollout Ei, the model generates an output token sequence yi, and a corresponding reward Ri is computed by the reward function. The relative reward of each rollout is evaluated within its group to derive the advantage value Âi, which guides the policy update toward higher-reward expert combinations. To jointly optimize token-level generation and layer-wise expert routing, the overall training objective of MoE-GRPO consists of two sub-objectives: Token-GRPO, which optimizes token-level generation quality, and Gate-GRPO, which refines layer-wise expert selection through the gating network.\", \"page\": 3, \"index\": 2, \"width\": 1036, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2603.24984v1/fig-003.webp\", \"caption\": \"Figure 6. A qualitative example and its routing probabilities. (a) illustrates the expert routing probabilities, with the selected experts highlighted in red boxes. (b) presents a qualitative example demonstrating that the learned expert selection policy of MoEGRPO yields a correct prediction, whereas the baseline Det-FT model produces an incorrect one.\", \"page\": 8, \"index\": 3, \"width\": 496, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2603.24984v1/fig-004.webp\", \"caption\": \"Table 1. Results on multi-modal understanding benchmarks. # activated and # total denote the number of activated and total parameters. The last column reports the average accuracy across all benchmarks, excluding MME.\", \"page\": 5, \"index\": 4, \"width\": 1026, \"height\": 325}]"
motivation: 传统的确定性Top-K路由机制限制了专家组合的探索空间，容易导致专家过拟合且难以达到最优性能。
method: 将专家选择视为序列决策过程，采用GRPO强化学习算法进行优化，并引入模态感知路由引导以提升训练稳定性。
result: 在多模态图像和视频基准测试中，MoE-GRPO通过促进专家选择的多样性，性能一致优于标准的Top-K路由及其变体。
conclusion: 通过强化学习优化专家路由，可以有效缓解MoE模型的专家过拟合问题，并实现更优的任务级专家专业化分工。
---

## 摘要
混合专家模型（MoE）已成为降低 Transformer 架构计算开销的一种有效方法，它通过为每个 token 稀疏地激活一部分参数，同时保持较高的模型容量。这一范式最近已被扩展到视觉语言模型（VLM），从而在降低计算成本的同时实现了可扩展的多模态理解。然而，广泛采用的确定性 top-K 路由机制可能会忽略更优的专家组合，并导致专家过拟合。为了解决这一局限性并提高专家选择的多样性，我们提出了 MoE-GRPO，这是一个基于强化学习（RL）的框架，用于优化基于 MoE 的 VLM 中的专家路由。具体而言，我们将专家选择建模为一个序列决策问题，并使用群体相对策略优化（GRPO）对其进行优化，使模型能够通过探索和基于奖励的反馈来学习自适应专家路由策略。此外，我们引入了一种模态感知的路由器引导机制，通过阻止路由器探索在给定模态下极少被激活的专家，从而增强训练的稳定性和效率。在多模态图像和视频基准测试上的大量实验表明，MoE-GRPO 通过促进更多样化的专家选择，始终优于标准的 top-K 路由及其变体，从而缓解了专家过拟合，并实现了任务级的专家专业化。

## Abstract
Mixture-of-Experts (MoE) has emerged as an effective approach to reduce the computational overhead of Transformer architectures by sparsely activating a subset of parameters for each token while preserving high model capacity. This paradigm has recently been extended to Vision-Language Models (VLMs), enabling scalable multi-modal understanding with reduced computational cost. However, the widely adopted deterministic top-K routing mechanism may overlook more optimal expert combinations and lead to expert overfitting. To address this limitation and improve the diversity of expert selection, we propose MoE-GRPO, a reinforcement learning (RL)-based framework for optimizing expert routing in MoE-based VLMs. Specifically, we formulate expert selection as a sequential decision-making problem and optimize it using Group Relative Policy Optimization (GRPO), allowing the model to learn adaptive expert routing policies through exploration and reward-based feedback. Furthermore, we introduce a modality-aware router guidance that enhances training stability and efficiency by discouraging the router from exploring experts that are infrequently activated for a given modality. Extensive experiments on multi-modal image and video benchmarks show that MoE-GRPO consistently outperforms standard top-K routing and its variants by promoting more diverse expert selection, thereby mitigating expert overfitting and enabling a task-level expert specialization.

---

## 论文详细总结（自动生成）

这是一份关于论文《MoE-GRPO: Optimizing Mixture-of-Experts via Reinforcement Learning in Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的混合专家模型（MoE）在视觉语言模型（VLM）中通常采用**确定性的 Top-K 路由机制**。这种机制存在两个主要缺陷：一是限制了专家组合的探索空间，可能错过更优的路由路径；二是容易导致模型过拟合到一小部分专家上，降低了模型的泛化能力。
*   **研究背景**：随着 Transformer 架构的扩展，MoE 成为平衡计算效率与模型容量的关键。然而，如何学习一个“最优专家选择策略”而非简单的启发式选择，仍是一个未被充分探索的领域。

### 2. 方法论：MoE-GRPO 框架
*   **核心思想**：将 MoE 的专家选择过程建模为一个**序列决策问题**，并利用强化学习算法 **GRPO（Group Relative Policy Optimization）** 来优化路由策略。
*   **关键技术细节**：
    *   **双重优化目标**：
        1.  **Token-GRPO**：优化输出 token 序列的生成质量，确保模型输出符合预期。
        2.  **Gate-GRPO**：直接优化每一层门控网络（Gating Network）的决策，通过奖励信号引导路由器选择能带来更高整体奖励的专家组合。
    *   **模态感知路由引导（Modality-Aware Router Guidance）**：为了缩小 RL 的搜索空间并提高稳定性，研究者计算了专家对视觉和文本 token 的偏好得分，在探索阶段屏蔽掉该模态下极少激活的专家（底部 25%），从而实现更高效的策略采样。
    *   **奖励机制**：采用基于准确率的验证奖励（Verifiable Rewards），对正确答案给予正向反馈。

### 3. 实验设计
*   **数据集与场景**：
    *   **多模态理解**：涵盖图像（MME, MMBench, MMStar, AI2D 等）和视频（VideoMME, MLVU, MVBench 等）共 9 个主流基准测试。
    *   **泛化性测试**：在 CLIP-MoE 架构上进行跨数据集评估（10 个目标数据集）和领域泛化实验（ImageNet 变体）。
*   **对比方法**：
    *   **Det-FT**：标准的确定性 Top-K 微调。
    *   **Stoch-FT-Multi/Noise**：引入多项式采样或高斯噪声的随机路由变体。
    *   **其他路由算法**：Expert Choice 路由、基于最优传输（Sinkhorn）的路由。
    *   **其他 RL 算法**：DAPO、SAPO。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA RTX Pro 6000 Blackwell Max-Q GPU**。
*   **训练配置**：训练步数为 25K 步，Batch Size 为 4，学习率为 1e-6。
*   **时长**：总训练时间约为 **1 天**。
*   **数据量**：从 OneThinker 中采样了 100K 条多模态指令微调数据。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的基准测试对比（9 个 VLM 榜单）、跨领域泛化实验（10+ 数据集）以及详尽的消融实验。
*   **充分性与公平性**：
    *   消融实验涵盖了 Token-GRPO 与 Gate-GRPO 的必要性、路由引导机制的有效性以及不同 RL 算法的鲁棒性。
    *   所有基准模型均在相同的负载均衡损失（Load-balancing loss）下训练，确保了对比的公平性。
    *   通过可视化专家利用率分布（熵值从 1.05 提升至 1.82），客观证明了方法对专家多样性的提升。

### 6. 主要结论与发现
*   **性能提升**：MoE-GRPO 在 7/9 的多模态基准测试中优于所有基准，平均准确率提升约 1.7% - 2.3%。
*   **缓解过拟合**：在跨数据集任务中，MoE-GRPO 显著优于确定性微调（Det-FT），证明了 RL 探索能有效防止模型对特定专家组合的依赖。
*   **专家专业化**：MoE-GRPO 诱导了更明显的**任务级专家分工**（JSD 散度从 0.06 提升至 0.20），使不同任务倾向于调用不同的专家组合。

### 7. 优点与亮点
*   **首创性**：首次将专家路由优化定义为序列决策问题并应用 GRPO 强化学习进行端到端优化。
*   **训练稳定性**：提出的模态感知引导机制巧妙地解决了 RL 在大规模离散动作空间中搜索效率低下的问题。
*   **即插即用**：该框架不依赖于特定的 MoE 架构，可广泛应用于各种基于 MoE 的视觉语言模型。

### 8. 不足与局限
*   **计算开销**：虽然推理成本不变，但训练阶段需要进行多次 Rollout（G=8），相比标准微调，训练时的计算资源需求更高。
*   **奖励函数单一**：目前主要依赖于二元（0/1）的准确率奖励，对于更复杂的开放式生成任务，可能需要更细粒度的奖励模型（Reward Model）。
*   **模态限制**：实验主要集中在视觉和文本，对于音频、深度等更多模态的扩展效果尚待验证。

（完）
