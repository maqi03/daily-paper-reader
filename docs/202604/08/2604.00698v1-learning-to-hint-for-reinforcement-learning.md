---
title: Learning to Hint for Reinforcement Learning
title_zh: 强化学习中的提示学习
authors: "Yu Xia, Canwen Xu, Zhewei Yao, Julian McAuley, Yuxiong He"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00698v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 通过自适应提示解决GRPO中的优势坍缩问题
tldr: 针对强化学习（如GRPO）中因样本全错导致优势函数坍缩、缺乏学习信号的问题，本文提出HiLL框架。该框架通过联合训练提示生成器（Hinter）和推理器（Reasoner），在线生成自适应提示以恢复学习信号。HiLL引入“提示依赖度”概念，通过转移加权奖励优化提示生成，确保提示下的成功经验能有效迁移至无提示场景。实验证明HiLL在多个基准测试中显著优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00698v1/fig-001.webp\", \"caption\": \"Figure 2: All-incorrect ratio (left two) and hint reliance (right two) over training steps. Both HiLL variants substantially reduce the fraction of degenerate all-incorrect groups compared to GRPO. Transfer weighting (HiLL vs. HiLLw/o TW) keeps hint reliance consistently lower throughout training.\", \"page\": 8, \"index\": 1, \"width\": 812, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00698v1/fig-002.webp\", \"caption\": \"Table 2: Example hints generated by HiLL and HiLLw/o TW on the same question. HiLL provides conceptual strategies while HiLLw/o TW tends to set up the computation directly.\", \"page\": 9, \"index\": 2, \"width\": 830, \"height\": 339}, {\"url\": \"assets/figures/arxiv/2604.00698v1/fig-003.webp\", \"caption\": \"Table 1: Main results on in-distribution and out-of-distribution benchmarks. Best results are in bold and second-best are underlined. HiLL consistenctly outperforms GRPO and hint-based baselines.\", \"page\": 7, \"index\": 3, \"width\": 830, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.00698v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of our HiLL framework. Given a question q with an all-incorrect group, the hinterHϕ takes the question, a failed rollout τk, and the reference solution z⋆ as input, and generates M candidate hints. The reasoner πθ re-samples G rollouts under each hinted input q+hj . Each hint is then scored by signal creation (Sec. 4.2) and signal transfer (Sec. 4.3). The best hinted group is selected reasoner GRPO update, while all candidate hints form the group for the hinter GRPO update.\", \"page\": 3, \"index\": 4, \"width\": 829, \"height\": 398}]"
motivation: 解决强化学习中因题目过难导致所有采样结果均为错误、进而产生零学习信号的优势坍缩问题。
method: 提出HiLL框架，通过在线生成自适应提示并利用转移加权奖励来联合优化提示生成器和推理器。
result: 在多个基准测试中，HiLL的表现一致优于传统的GRPO算法及现有的固定提示基线方法。
conclusion: 自适应且具备迁移意识的提示学习能有效解决强化学习中的冷启动和信号缺失问题，提升推理任务的性能。
---

## 摘要
组相对策略优化（GRPO）广泛应用于具有可验证奖励的强化学习，但它经常面临优势崩溃（advantage collapse）的问题：当一个组内的所有采样序列（rollouts）获得相同的奖励时，该组产生的相对优势为零，从而导致学习信号缺失。例如，如果一个问题对于推理器而言过于困难，所有采样的序列可能都是错误的并获得零奖励。最近的研究通过为此类难题添加提示或辅助支架来解决这一问题，从而使推理器产生多样化的结果并恢复非零更新。然而，现有的提示通常是固定的，无法适应当前的推理器，且在有提示输入下产生的学习信号并不一定能提升测试时所使用的无提示策略。为此，我们提出了强化学习中的提示学习（HiLL），这是一个在强化学习过程中联合训练提示器（hinter）策略和推理器（reasoner）策略的框架。对于每个难题，提示器会根据当前推理器的错误采样序列在线生成提示，使提示生成能够适应推理器不断演变的错误。我们进一步引入了提示依赖度（hint reliance），用于衡量正确提示轨迹对提示的依赖程度。我们推导出了一个可迁移性结果，表明较低的提示依赖度意味着从有提示成功到无提示成功的迁移能力更强，并利用该结果定义了用于训练提示器的迁移加权奖励。因此，HiLL 倾向于生成不仅能恢复信息丰富的 GRPO 组，还能产生更有可能改进原始无提示策略信号的提示。在多个基准测试上的实验表明，HiLL 一致优于 GRPO 和以往基于提示的基准方法，证明了自适应且具备迁移感知能力的提示学习在强化学习中的价值。代码见 https://github.com/Andree-9/HiLL。

## Abstract
Group Relative Policy Optimization (GRPO) is widely used for reinforcement learning with verifiable rewards, but it often suffers from advantage collapse: when all rollouts in a group receive the same reward, the group yields zero relative advantage and thus no learning signal. For example, if a question is too hard for the reasoner, all sampled rollouts can be incorrect and receive zero reward. Recent work addresses this issue by adding hints or auxiliary scaffolds to such hard questions so that the reasoner produces mixed outcomes and recovers a non-zero update. However, existing hints are usually fixed rather than adapted to the current reasoner, and a hint that creates learning signal under the hinted input does not necessarily improve the no-hint policy used at test time. To this end, we propose Hint Learning for Reinforcement Learning (HiLL), a framework that jointly trains a hinter policy and a reasoner policy during RL. For each hard question, the hinter generates hints online conditioned on the current reasoner's incorrect rollout, allowing hint generation to adapt to the reasoner's evolving errors. We further introduce hint reliance, which measures how strongly correct hinted trajectories depend on the hint. We derive a transferability result showing that lower hint reliance implies stronger transfer from hinted success to no-hint success, and we use this result to define a transfer-weighted reward for training the hinter. Therefore, HiLL favors hints that not only recover informative GRPO groups, but also produce signals that are more likely to improve the original no-hint policy. Experiments across multiple benchmarks show that HiLL consistently outperforms GRPO and prior hint-based baselines, demonstrating the value of adaptive and transfer-aware hint learning for RL. The code is available at https://github.com/Andree-9/HiLL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Hint for Reinforcement Learning》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**强化学习与可验证奖励（RLVR）**在提升大语言模型（LLM）推理能力时的核心瓶颈——**优势坍缩（Advantage Collapse）**。
*   **背景**：在 GRPO 等算法中，模型通过对比一组采样序列的奖励来计算优势。如果题目太难，模型所有采样均错误（奖励全为0），则相对优势为零，导致模型无法获得学习信号。
*   **核心问题**：现有的“提示（Hint）”方法多为静态或外部生成的，存在两个缺陷：
    1.  **缺乏自适应性**：提示无法根据推理器当前的错误模式动态调整。
    2.  **缺乏迁移性**：如果提示过于直接（如直接给出关键步骤），模型可能学会“走捷径”依赖提示，而在测试（无提示）时依然无法解题。

### 2. 方法论：HiLL 框架
HiLL（Hint Learning for RL）提出了一个**联合训练提示器（Hinter）和推理器（Reasoner）**的协同进化框架。

*   **核心思想**：将提示生成本身建模为一个可学习的 RL 任务。当推理器在某题上全错时，提示器介入生成提示，帮助推理器产生“有对有错”的混合结果，从而恢复 GRPO 的学习信号。
*   **关键技术细节**：
    *   **失败条件输入**：提示器 $H_\phi$ 的输入包含：原题 $q$、推理器当前的失败采样 $\tau_k$、以及参考答案 $z^*$。这使得提示能针对性地纠正推理器的特定错误。
    *   **提示依赖度（Hint Reliance）**：定义为 $\rho = \log \pi_\theta(\tau | q+h) - \log \pi_\theta(\tau | q)$。它衡量了正确轨迹对提示的依赖程度。
    *   **转移加权奖励（Transfer-Weighted Reward）**：提示器的奖励由两部分组成：
        1.  **信号创建**：鼓励产生非退化的（有对有错的）采样组。
        2.  **信号迁移**：通过 $\exp(-\rho/T)$ 对奖励加权。如果正确轨迹在无提示下也具有较高概率（即依赖度低），则奖励更高。
*   **算法流程**：推理器先采样 $\rightarrow$ 识别全错组 $\rightarrow$ 提示器生成多个候选提示 $\rightarrow$ 推理器在提示下重新采样 $\rightarrow$ 计算提示奖励并选择最佳提示 $\rightarrow$ 同时更新推理器和提示器。

### 3. 实验设计
*   **数据集**：使用 OpenR1-Math-220k 的 15k 提示子集进行训练。
*   **Benchmark**：
    *   **数学推理（分布内）**：AIME24/25、AMC23、MATH-500、Minerva、OlympiadBench。
    *   **通用推理（分布外/泛化）**：GPQA-diamond、MMLU-Pro。
*   **对比方法**：
    *   **Base**：初始模型。
    *   **GRPO**：标准 RL。
    *   **LUFFY**：引入外部高质量轨迹。
    *   **Scaf-GRPO**：使用外部教师模型提供固定提示。
    *   **SAGE**：自生成提示（无迁移优化）。
    *   **HiLL w/o TW**：消融实验，去掉转移加权奖励。

### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA B200 GPUs**。
*   **软件框架**：基于 `verl` 强化学习框架和 `vLLM` 推理引擎。
*   **模型规模**：
    *   推理器：Llama-3.2-3B-Instruct 和 Qwen2.5-7B-Instruct。
    *   提示器：Qwen3-4B-Instruct。
*   **训练细节**：训练 500 步，Batch Size 为 128，每题采样 $G=8$ 个序列。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同规模、不同系列的基座模型（Llama 和 Qwen）上进行了验证，涵盖了 8 个主流基准测试。
*   **消融与分析**：
    *   进行了**消融实验**（HiLL vs HiLL w/o TW），证明了转移加权的重要性。
    *   进行了**超参数敏感性分析**（如温度 $T$ 对信号创建与迁移的权衡）。
    *   进行了**定性分析**，对比了提示的长度、数学表达式密度及具体案例。
*   **客观性**：实验对比了多种最新的提示增强 RL 方法，且在相同的训练数据和算力预算下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **性能领先**：HiLL 在所有数学和通用推理任务上均优于标准 GRPO 和其他提示基线。
*   **迁移性验证**：引入“提示依赖度”约束后，提示器学会生成更**简洁、更具概念性**的提示（而非直接给答案），这显著提升了推理器在无提示测试时的表现。
*   **自适应进化**：随着推理器变强，提示器会自动针对新的“能力边界”难题生成提示，形成动态的课程学习。
*   **泛化能力**：虽然只在数学数据上训练，但在 GPQA 和 MMLU-Pro 上的提升证明了该方法增强了模型的底层逻辑推理能力。

### 7. 优点
*   **理论支撑**：推导出了可迁移性边界（Proposition 1），为提示依赖度提供了数学依据。
*   **解决冷启动**：有效解决了 RL 训练初期难题无法提供梯度的“死锁”问题。
*   **高效利用算力**：虽然增加了单步计算量，但它将算力精准投放在原本无用的“全错组”上，提高了样本效率。

### 8. 不足与局限
*   **计算开销**：HiLL 引入了额外的提示生成和二次采样步骤。在 Llama-3B 上，单步训练耗时约为标准 GRPO 的 **3.8 倍**（Qwen-7B 上为 2.6 倍）。
*   **依赖参考答案**：提示器在训练阶段需要访问参考答案（Ground Truth Solution）来生成提示，这限制了其在完全无监督或无标准答案场景下的应用。
*   **提示器初始化**：提示器的性能可能受其初始模型能力的影响，若提示器太弱，可能无法生成有效的引导。

（完）
