---
title: Unifying Group-Relative and Self-Distillation Policy Optimization via Sample Routing
title_zh: 通过样本路由统一组相对策略优化与自蒸馏策略优化
authors: "Gengsheng Li, Tianyu Yang, Junfeng Fang, Mingyang Song, Mao Zheng, Haiyun Guo, Dan Zhang, Jinqiao Wang, Tat-Seng Chua"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02288v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 将组相对策略优化 (GRPO) 与自我蒸馏统一
tldr: 本研究针对大语言模型强化学习中GRPO信用分配粗糙和SDPO后期训练不稳定的问题，提出了样本路由策略优化（SRPO）。SRPO通过将正确样本路由至GRPO进行奖励对齐，将失败样本路由至SDPO进行逻辑层纠错，并结合熵感知动态权重抑制不可靠信号。实验表明，SRPO在保持长程稳定性的同时实现了快速性能提升，在多个基准测试中显著超越基准模型，并降低了计算成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02288v1/fig-001.webp\", \"caption\": \"Table 3: Hyperparameters for GRPO, SDPO, and SRPO. For GRPO and SDPO, we use the configurations from Hübotter et al. (2026). For SRPO, the GRPO-branch and SDPO-branch loss parameters are inherited from the respective baselines; only the learning rate and the dynamic-weighting temperature β differ. Entries marked “–” indicate parameters not applicable to that method.\", \"page\": 15, \"index\": 1, \"width\": 829, \"height\": 1259}, {\"url\": \"assets/figures/arxiv/2604.02288v1/fig-002.webp\", \"caption\": \"Figure 1: Training dynamics and diagnostic analysis on Chemistry with Qwen3-8B. (a) SDPO improves faster than GRPO in early training, but is later overtaken and collapses, whereas SRPO achieves both rapid initial improvement and stable long-horizon optimization. (b) Restricting SDPO updates to incorrect samples retains most of its overall benefit, whereas applying SDPO only to correct samples degrades performance and destabilizes training, supporting the necessity of sample routing. (c) The self-teacher’s token-level entropy rises during training, indicating that the distillation signal becomes increasingly dominated by uncertain predictions. Curves show a 5-step rolling mean and shaded bands denote ±1 std.\", \"page\": 2, \"index\": 2, \"width\": 748, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.02288v1/fig-003.webp\", \"caption\": \"Table 1: Main results on five benchmarks at three training budgets. Each entry reports the highest achieved avg@16 accuracy (%) within the corresponding wall-clock budget. The last three columns report the mean over the five benchmarks. Within each model scale, the best result in each column is in bold and the second-best is underlined.\", \"page\": 7, \"index\": 3, \"width\": 818, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2604.02288v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of SRPO. Given a prompt x, the policy πθ generates a group of on-policy rollouts. A correctness check routes each rollout to one of two branches: correct samples are sent to the GRPO branch (top), where group-relative advantages provide a reward-aligned policy update; incorrect samples with available teacher information are sent to the SDPO branch (bottom), where a feedback-conditioned self-teacher produces logit-level distillation targets via KL(P ∥ stopgrad(Q)) for dense corrective supervision.\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2604.02288v1/fig-005.webp\", \"caption\": \"Table 5: One representative example from each benchmark. Science Q&A examples show the question stem and four answer options; the Tool Use example shows the user query and the expected structured tool call (API specification and answer omitted for brevity; see Appendix B.3 for the full template).\", \"page\": 17, \"index\": 5, \"width\": 824, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.02288v1/fig-006.webp\", \"caption\": \"Figure 4: Response length and per-step compute time for Qwen3-8B. (a) Response length on Chemistry: GRPO remains consistently long, SDPO drops rapidly, and SRPO stays moderate. Curves show a 5-step rolling mean and shaded bands denote ±1 std. (b) Average seconds per training step, averaged across five benchmarks and measured over the 1h, 5h, and 10h windows. SRPO incurs a modest overhead relative to GRPO in the early stage of training, but becomes faster than both GRPO and SDPO over longer training horizons.\", \"page\": 9, \"index\": 6, \"width\": 739, \"height\": 250}]"
motivation: 现有的GRPO缺乏细粒度信用分配，而SDPO在训练后期因对正确样本的过度蒸馏和教师信号退化而容易崩溃。
method: 提出SRPO框架，根据样本正确性进行路由分发，并引入熵感知动态权重机制来优化自蒸馏目标的可靠性。
result: "在五个基准测试中，SRPO在Qwen3-8B上比GRPO和SDPO平均性能分别提升3.4%和6.3%，且计算成本降低达17.2%。"
conclusion: SRPO成功统一了组相对优化与自蒸馏的优势，为大语言模型提供了一种高效、稳定且低成本的后训练强化学习方案。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为大语言模型后训练的标准范式。虽然组相对策略优化（GRPO）被广泛采用，但其粗粒度的信用分配会统一惩罚失败的生成序列，缺乏有效解决特定偏差所需的词元级关注。自蒸馏策略优化（SDPO）通过提供更密集、更具针对性的 Logit 级监督来解决这一问题，从而促进早期快速提升，但在长时间训练中经常发生崩溃。我们将这种后期不稳定性归因于两个内在缺陷：对已经正确的样本进行自蒸馏会引入优化歧义，且自教师信号的可靠性会逐渐退化。为解决这些问题，我们提出了样本路由策略优化（SRPO），这是一个统一的在线策略框架，它将正确样本路由至 GRPO 的奖励对齐强化，将失败样本路由至 SDPO 的针对性 Logit 级纠正。SRPO 进一步结合了熵感知动态加权机制，以抑制高熵、不可靠的蒸馏目标，同时强调高置信度的目标。在五个基准测试和两个模型规模上的评估表明，SRPO 既实现了 SDPO 的早期快速提升，又具备 GRPO 的长程稳定性。它持续超越了两个基准模型的峰值性能，在 Qwen3-8B 上的五个基准平均得分比 GRPO 提高了 3.4%，比 SDPO 提高了 6.3%，同时产生了适中的响应长度，并将每步计算成本降低了高达 17.2%。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has become a standard paradigm for post-training large language models. While Group Relative Policy Optimization (GRPO) is widely adopted, its coarse credit assignment uniformly penalizes failed rollouts, lacking the token-level focus needed to efficiently address specific deviations. Self-Distillation Policy Optimization (SDPO) addresses this by providing denser, more targeted logit-level supervision that facilitates rapid early improvement, yet it frequently collapses during prolonged training. We trace this late-stage instability to two intrinsic flaws: self-distillation on already-correct samples introduces optimization ambiguity, and the self-teacher's signal reliability progressively degrades. To resolve these issues, we propose Sample-Routed Policy Optimization (SRPO), a unified on-policy framework that routes correct samples to GRPO's reward-aligned reinforcement and failed samples to SDPO's targeted logit-level correction. SRPO further incorporates an entropy-aware dynamic weighting mechanism to suppress high-entropy, unreliable distillation targets while emphasizing confident ones. Evaluated across five benchmarks and two model scales, SRPO achieves both the rapid early improvement of SDPO and the long-horizon stability of GRPO. It consistently surpasses the peak performance of both baselines, raising the five-benchmark average on Qwen3-8B by 3.4% over GRPO and 6.3% over SDPO, while simultaneously yielding moderate response lengths and lowering per-step compute cost by up to 17.2%.

---

## 论文详细总结（自动生成）

这是一份关于论文《Unifying Group-Relative and Self-Distillation Policy Optimization via Sample Routing》的结构化深入总结：

### 1. 核心问题与研究动机
论文聚焦于大语言模型（LLM）在带有可验证奖励的强化学习（RLVR）后训练阶段的效率与稳定性问题。
*   **背景**：目前主流方法如 **GRPO**（组相对策略优化）虽然稳定，但信用分配粗糙（对整个序列统一奖惩），导致收敛慢、效率低；而 **SDPO**（自蒸馏策略优化）通过 Logit 级监督提供密集引导，早期提升快，但在训练后期极易发生性能崩溃。
*   **核心发现**：作者诊断出 SDPO 不稳定的两个主因：
    1.  **优化歧义**：对已正确的样本进行自蒸馏会强迫模型在等效的正确路径间做无意义选择。
    2.  **信号退化**：随着训练进行，自教师（Self-teacher）的预测熵值升高，提供的蒸馏信号变得模糊且不可靠。

### 2. 方法论：样本路由策略优化 (SRPO)
SRPO 的核心思想是“因材施教”，根据样本的实时表现动态选择最优的优化路径。
*   **样本级路由机制**：
    *   **正确样本**：路由至 **GRPO 分支**。利用序列级奖励进行强化，确保模型锚定在奖励对齐的目标上，避免 Logit 级的过度干预。
    *   **失败样本**（且存在正确兄弟样本作为教师）：路由至 **SDPO 分支**。利用自教师提供的 Logit 级密集监督进行精准纠错。
*   **熵感知动态加权 (DW-SDPO)**：
    *   在 SDPO 分支中，根据教师分布的熵值计算词元级权重。
    *   **公式逻辑**：权重与熵值成负相关（$w \propto \exp(-\beta H)$）。高置信度（低熵）的词元获得更高权重，而模糊（高熵）的信号被抑制，从而缓解后期信号退化问题。
*   **统一目标函数**：将两分支的损失函数根据样本覆盖的词元数进行归一化合并，无需额外的混合超参数，实现了训练过程中的自动平滑过渡。

### 3. 实验设计
*   **数据集/场景**：涵盖五个具有挑战性的基准测试：
    *   **科学推理**：Chemistry（化学）、Physics（物理）、Biology（生物）、Materials（材料），均源自 SciKnowEval。
    *   **工具调用**：Tool Use（基于 ToolAlpaca）。
*   **模型规模**：使用了 Qwen3-4B 和 Qwen3-8B 两个尺寸的指令微调模型。
*   **对比基准 (Baselines)**：
    *   **GRPO**：强化版的组相对策略优化实现。
    *   **SDPO**：纯自蒸馏策略优化。
    *   **Advantage Mix**：在优势函数层面直接混合两者的消融变体。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU**（单节点，总显存 768GB）。
*   **训练时长**：实验对比了 1小时、5小时和 10小时三个时间窗口下的性能表现。
*   **软件框架**：基于 `verl` 库，使用 PyTorch FSDP2 进行分布式训练，推理后端采用 SGLang。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模、5 个不同领域的基准测试上进行了完整训练。
*   **消融实验**：针对“路由策略” vs “优势混合”以及“动态加权”的有效性进行了专门的消融对比。
*   **充分性评价**：实验设计较为充分且客观。作者不仅关注最终精度（avg@16），还深入分析了训练曲线、响应长度变化以及每步计算耗时。通过跨领域和跨规模的验证，证明了方法的普适性。

### 6. 主要结论与发现
*   **性能卓越**：SRPO 在所有基准上均超越了 GRPO 和 SDPO 的峰值。在 Qwen3-8B 上，5 榜平均分比 GRPO 高 3.4%，比 SDPO 高 6.3%。
*   **长程稳定**：SRPO 成功解决了 SDPO 的后期崩溃问题，在 10 小时的长时训练中依然保持增长。
*   **计算高效**：虽然早期因自教师计算有额外开销，但随着正确率提升，路由至 SDPO 的样本减少，且 SRPO 生成的序列长度比 GRPO 更短，最终在 10 小时节点上比 GRPO 节省了 **17.2%** 的每步计算时间。

### 7. 优点与亮点
*   **架构优雅**：通过简单的路由逻辑统一了两种范式，避免了复杂的超参数调优。
*   **诊断深入**：对 SDPO 崩溃原因的分析（优化歧义与信号退化）具有很强的启发性。
*   **多维优化**：不仅提升了准确率，还兼顾了推理成本（响应长度）和训练速度，具有很高的实用价值。

### 8. 不足与局限
*   **教师依赖**：SDPO 分支仍依赖于同组内必须存在至少一个正确样本（Sibling Rollout）。如果模型在某些极难题目上全军覆没，SRPO 将退化为纯 GRPO。
*   **反馈维度**：目前主要利用正确/错误这种二元反馈，尚未充分利用更丰富的环境反馈（如代码执行的报错信息）。
*   **模型范围**：实验主要集中在 Qwen3 系列，尚未在其他架构（如 Llama 或不同训练阶段的模型）上广泛验证。

（完）
