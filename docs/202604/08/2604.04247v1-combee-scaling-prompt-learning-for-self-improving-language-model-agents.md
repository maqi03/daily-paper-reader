---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：面向自我改进语言模型智能体的可扩展提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:pr-opt"]
score: 10.0
evidence: 为自我改进的语言模型智能体扩展提示词学习
tldr: 针对自改进大语言模型智能体在处理大规模并行轨迹时效率低且质量易下降的问题，本文提出了 Combee 框架。Combee 通过引入并行扫描、增强打乱机制以及动态批大小控制器，实现了提示学习的高效扩展。该框架能够从大量并行执行的智能体轨迹中聚合知识，在显著提升学习速度的同时保证了提示词的质量。实验表明，Combee 在多个基准测试中比现有方法快 17 倍，且维持了同等或更高的准确率，为大规模智能体自进化提供了有力支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04247v1/fig-001.webp\", \"caption\": \"Figure 2: Context overload from naive scaling. As batch size increases, the aggregator LLM produces monotonically fewer and less useful context updates, directly degrading final accuracy across benchmarks.\", \"page\": 3, \"index\": 1, \"width\": 739, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-002.webp\", \"caption\": \"Figure 4: Combee achieves superior quality-delay trade off on GEPA for finance benchmarks.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-003.webp\", \"caption\": \"Figure 5: Combee achieves superior quality-delay trade off on ACE for finance benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 797, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-004.webp\", \"caption\": \"Figure 3: Overall design of Combee (top) vs. naive scaling (bottom). Combee follows a Map-Shuffle-Reduce paradigm: the Map phase dispatches n parallel agents to execute queries and reflect; the Shuffle phase applies augmented shuffling; and the Reduce phase hierarchically combines reflections via parallel scan aggregation. In contrast, naive scaling feeds all reflections directly into a single prompt update, causing context overload.\", \"page\": 5, \"index\": 4, \"width\": 732, \"height\": 552}]"
motivation: 现有的提示学习方法在处理高并发智能体轨迹时，面临扩展性差、学习效率低以及模型质量退化的问题。
method: 提出 Combee 框架，采用并行扫描、增强打乱机制和动态批大小控制器来优化大规模并行提示学习过程。
result: 在 AppWorld 和 Terminal-Bench 等多个基准测试中，Combee 实现了高达 17 倍的加速，并保持或提升了预测准确率。
conclusion: Combee 成功解决了自改进智能体在扩展提示学习时的效率与质量平衡难题，是实现大规模智能体进化的有效方案。
---

## 摘要
提示学习的最新进展使得大语言模型智能体能够在不改变参数的情况下，从推理时的上下文中获取任务相关知识。例如，现有方法（如 ACE 或 GEPA）可以根据之前的智能体运行记录学习系统提示，以提高准确性。然而，这些方法主要关注单智能体或低并行度设置，这从根本上限制了它们从大量收集的智能体轨迹（agentic traces）中高效学习的能力。为了适应从大量智能体轨迹或并行智能体执行中学习的增长趋势，并行运行提示学习将是高效且有益的。然而，由于缺乏原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时，从它们的聚合轨迹中学习，且不会造成质量损失。为实现这一目标，Combee 利用了并行扫描（parallel scans）并采用了增强的洗牌机制（augmented shuffle mechanism）；Combee 还引入了动态批大小控制器来平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 在保持相当或更好准确性以及同等成本的前提下，比之前的方法实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Combee**，一个旨在解决大语言模型（LLM）智能体在“自我改进”过程中，如何高效且高质量地扩展并行提示学习（Prompt Learning）的框架。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：提示学习（如 ACE 或 GEPA）允许智能体通过“生成-反思-更新”循环，从推理过程中的轨迹和反馈中提取知识并存入“剧本（Playbook）”或系统提示中，实现无需参数微调的自我改进。
*   **核心问题**：现有的提示学习方法在**高并行度**下会遭遇“**上下文过载（Context Overload）**”问题。当尝试同时从大量智能体轨迹中学习时，负责聚合信息的 LLM 会因为处理过长的反思上下文而进行“有损压缩”，保留通用模式却丢弃了对性能至关重要的特定高价值信息。
*   **研究意义**：Combee 的提出是为了在保证学习质量的前提下，大幅提升智能体从大规模交互数据中进化的速度。

### 2. 方法论：核心思想与关键技术
Combee 采用了 **Map-Shuffle-Reduce** 范式，包含三个核心组件：
*   **并行扫描聚合（Parallel Scan Aggregation）**：借鉴并行计算中的前缀和思想，将 $n$ 个轨迹的反思分成 $\sqrt{n}$ 个小组。先在小组内进行局部聚合，再将局部更新分层合并。这种分层结构避免了单次调用 LLM 时输入过多的反思内容，解决了上下文过载问题。
*   **增强打乱机制（Augmented Shuffling）**：在将反思分发到聚合树之前，将每个反思信号复制多份（默认 $p=2$）并随机打乱。这增加了关键信息被聚合器捕捉到的概率，提高了系统的鲁棒性。
*   **动态批大小控制器（Dynamic Batch Size Controller）**：通过预先测试不同批大小（Batch Size）下的延迟，拟合幂律延迟曲线 $T_{epoch}(bs) = A \cdot bs^{-\alpha}$。系统会自动选择边际收益最高的批大小，在训练速度（延迟）和学习质量之间取得平衡。

### 3. 实验设计
*   **数据集与场景**：
    *   **智能体基准**：AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行软件工程任务）。
    *   **领域特定任务**：FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法（Baselines）**：
    *   基础模型（Base LLM / ReAct）。
    *   原始提示学习方法：ACE 和 GEPA（在不同固定批大小下）。
    *   传统缓解策略：基于摘要（Summarization）的方法、基于 Top-K 检索的方法。
*   **基础模型**：主要使用 DeepSeek-V3.1，并在 GPT-OSS 120B 上进行了鲁棒性验证。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 硬件型号和数量，因为实验主要通过 API（如 Together AI 提供的 DeepSeek-V3.1）进行。
*   **训练时长**：论文详细记录了训练时间。例如，在 AppWorld 上，ACE 顺序学习需要 86 分钟，而 Combee 在批大小为 40 时仅需 7 分钟（加速约 12 倍）；在 Terminal-Bench 上实现了高达 17 倍的加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个不同类型的基准测试，涉及 API 调用、命令行操作、金融 NLP 等多个维度。
*   **消融实验**：对“增强打乱”和“动态批大小控制器”进行了专门的消融分析，验证了各组件的有效性。
*   **充分性评价**：实验设计较为充分，不仅对比了准确率，还对比了训练时间、Token 成本和剧本大小。通过在不同模型（DeepSeek 和 GPT-OSS）上运行，证明了框架的通用性。对比实验考虑了多种批大小，体现了客观性。

### 6. 主要结论与发现
*   **打破质量-速度权衡**：Combee 证明了在高并行度下，通过合理的聚合策略可以维持甚至超过顺序学习的准确率，同时大幅缩短训练时间。
*   **信息保留能力强**：定性分析显示，Combee 生成的剧本保留了更多具体的、针对边缘情况的策略，而朴素的并行方法生成的剧本往往流于表面。
*   **高效性**：在多个任务中，Combee 均达到了帕累托前沿（Pareto Frontier），即在相同的训练时间内准确率最高，或在相同准确率下耗时最短。

### 7. 优点
*   **框架无关性**：Combee 可以无缝集成到 ACE、GEPA 等多种现有的“生成-反思-更新”框架中。
*   **解决实际痛点**：针对大规模智能体部署中产生的海量轨迹，提供了切实可行的并行学习方案。
*   **自动化程度高**：动态批大小控制器减少了人工调参的负担。

### 8. 不足与局限
*   **同步限制**：目前的并行扫描设计假设每一轮迭代是同步执行的，未来可以探索异步更新机制以进一步提升异构环境下的吞吐量。
*   **模型依赖**：虽然在两个模型上做了测试，但分层聚合的效果仍可能受限于底层聚合器 LLM 的指令遵循能力。
*   **应用范围**：主要针对文本形式的提示学习，对于需要频繁更新模型权重的强化学习（RL）场景，该框架的适用性尚未讨论。

（完）
