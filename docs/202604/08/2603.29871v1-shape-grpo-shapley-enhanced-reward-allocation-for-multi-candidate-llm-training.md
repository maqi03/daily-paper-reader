---
title: "ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training"
title_zh: ShapE-GRPO：面向多候选大语言模型训练的 Shapley 增强奖励分配
authors: "Rui Ai, Yu Pan, David Simchi-Levi, Chonghuan Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29871v1"
tags: ["query:rm-opt"]
score: 10.0
evidence: 针对GRPO的沙普利增强奖励分配，旨在优化训练信号
tldr: 针对大语言模型在生成候选集（如推荐、头脑风暴）时，传统强化学习（如GRPO）将集合奖励平均分配给所有候选者导致训练信号模糊的问题，本文提出ShapE-GRPO。该方法利用合作博弈论中的Shapley值，将集合层面的奖励分解为候选者特有的细粒度信号。实验证明，该方法在保持计算效率的同时，显著提升了模型性能并加速了训练收敛。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29871v1/fig-001.webp\", \"caption\": \"Figure 1: When the user considers the ratings for the three suggestions to be 5.0, 4.0, and 3.0, respectively, our ShapE-GRPO performs candidate-level (with broadcasting to token-level) reward allocation as above. However, GRPO only uses a single +5.0 sequence-level reward shared across tokens.\", \"page\": 2, \"index\": 1, \"width\": 827, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-002.webp\", \"caption\": \"Figure 4: Response reward as a function of the number of candidates across different datasets using QWEN3-8B.\", \"page\": 18, \"index\": 2, \"width\": 826, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.29871v1/fig-003.webp\", \"caption\": \"Table 2: Main training hyperparameters used in our experiments.\", \"page\": 23, \"index\": 3, \"width\": 714, \"height\": 231}]"
motivation: 传统的GRPO在处理集合生成任务时，将相同的集合奖励分配给所有候选者，导致表现差的候选者“搭便车”，产生训练噪声。
method: 引入Shapley值理论，将集合奖励分解为针对每个候选者的细粒度奖励信号，并确保计算复杂度在多项式时间内。
result: ShapE-GRPO在多个数据集上均优于标准GRPO，且训练收敛速度更快。
conclusion: 通过Shapley值进行细粒度奖励分配，能有效解决多候选者训练中的信号噪声问题，提升LLM在集合生成任务中的表现。
---

## 摘要
在推荐、头脑风暴和代码建议等用户-代理交互场景中，大语言模型（LLM）通常生成候选推荐集合，其目标是最大化整个集合的集体效用，而非独立地最大化单个候选。然而，现有的强化学习后训练范式，如群体相对策略优化（GRPO），通常将相同的集合级标量奖励分配给集合中的每个候选。这导致了训练信号的噪声，表现为表现较差的候选者“搭便车”，利用单个优秀同伴产生的高奖励，从而导致次优的探索。为了解决这一问题，我们提出了 Shapley 增强的 GRPO（ShapE-GRPO）。通过利用集合级效用的置换不变性，我们从合作博弈论中推导出一种 Shapley 增强公式，将集合级奖励分解为细粒度的、针对特定候选的信号。我们证明了该公式在保持多项式时间复杂度的计算效率的同时，保留了 Shapley 值的基本公理。实验表明，ShapE-GRPO 在多种数据集上始终优于标准的 GRPO，并在训练过程中加速了收敛。

## Abstract
In user-agent interaction scenarios such as recommendation, brainstorming, and code suggestion, Large Language Models (LLMs) often generate sets of candidate recommendations where the objective is to maximize the collective utility of the entire set rather than individual candidates independently. However, existing reinforcement learning post-training paradigms, such as Group Relative Policy Optimization (GRPO), typically assign the same set-level scalar reward to every candidate in the set. This leads to noisy training signals where poor candidates free-ride on the high reward produced by a single strong peer, resulting in suboptimal exploration. To address this, we propose Shapley-Enhanced GRPO (ShapE-GRPO). By leveraging the permutation-invariant nature of set-level utility, we derive a Shapley-enhanced formulation from cooperative game theory to decompose set-level rewards into granular, candidate-specific signals. We show that our formulation preserves the fundamental axioms of the Shapley value while remaining computationally efficient with polynomial-time complexity. Empirically, ShapE-GRPO consistently outperforms standard GRPO across diverse datasets with accelerated convergence during training.

---

## 论文详细总结（自动生成）

这是一份关于论文《ShapE-GRPO: Shapley-Enhanced Reward Allocation for Multi-Candidate LLM Training》的结构化总结：

### 1. 核心问题与研究动机
在推荐系统、代码建议或头脑风暴等场景中，LLM 通常一次性生成多个候选方案（Candidate Set）。此时的目标是最大化**集合的整体效用**（例如：只要其中一个方案被用户采纳即可）。
*   **现有问题：** 传统的强化学习算法（如 GRPO）通常将集合层面的单一奖励（Scalar Reward）分配给集合中的所有候选者。
*   **痛点：** 这会导致“搭便车”现象（Free-riding），即一个极差的候选者因为同组中有一个极优秀的候选者而获得了高奖励。这种模糊的训练信号增加了噪声，降低了探索效率，导致模型难以学习到真正有效的生成策略。

### 2. 方法论：ShapE-GRPO
论文提出了一种基于合作博弈论中 **Shapley 值（Shapley Value）** 的奖励分配机制，将集合奖励公平地分解到每个候选者身上。
*   **核心思想：** 将每个候选者视为博弈中的“玩家”，集合的总奖励为博弈的“价值”。通过计算每个候选者对集合奖励的边际贡献，实现细粒度的奖励分配。
*   **关键技术细节：**
    *   **置换不变性：** 利用推荐场景中候选者顺序不影响总效用的特性，简化了 Shapley 值的计算。
    *   **奖励分解公式：** 论文推导出在 $max$ 奖励函数（即集合效用取决于表现最好的候选者）下，Shapley 值的闭式解。
    *   **Token 级广播：** 计算出候选者级别的 Shapley 奖励后，将其广播至该候选者包含的所有 Token 上，而推理过程（Reasoning part）则保留原始的集合奖励。
    *   **计算效率：** 证明了在特定奖励函数下，Shapley 值的计算复杂度从指数级 $O(2^K)$ 降低到了多项式级 $O(K^2)$ 甚至 $O(K)$，其中 $K$ 是候选者数量。

### 3. 实验设计
*   **数据集/场景：**
    1.  **ACLSum（摘要生成）：** 科学论文摘要，使用 GPT-5.2 作为裁判进行连续评分。
    2.  **DS-1000（代码建议）：** 数据科学编程任务，采用二元奖励（通过测试用例为 1，否则为 0）。
    3.  **Netflix（电影推荐）：** 基于真实用户历史评分构建的新基准，分为“有历史”和“无历史”两种设置。
*   **对比方法（Baselines）：**
    *   **标准 GRPO：** 集合内所有候选者共享相同奖励。
    *   **Winner-Takes-All (WTA)：** 仅给表现最好的候选者分配奖励，其余为 0。
    *   **Pretrained Model：** 未经强化学习微调的原始模型。

### 4. 资源与算力
*   **硬件：** 使用了两块 **NVIDIA GH200 GPU**。
*   **模型：** 主要实验基于 **Qwen3-8B**，补充实验使用了 **Llama 3.1-8B-Instruct**。
*   **训练细节：** 训练框架为 `verl`，设置了 1 个 Epoch，学习率为 1e-6，KL 系数为 0.01。具体训练时长未明确标注，但提到 ShapE-GRPO 相比 GRPO 能在更少的步数内（约 160 步 vs 240 步）达到收敛。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了三个不同领域的任务（文本、代码、推荐），并使用了两种主流的 8B 参数模型进行验证。
*   **充分性：** 实验设计较为全面，不仅对比了标准基准，还引入了 WTA 这种启发式分配方法作为对照，证明了 Shapley 值的优越性并非仅仅来自“只奖励最好者”，而是来自“合理的贡献分配”。
*   **客观性：** 论文通过训练曲线展示了 ShapE-GRPO 在收敛速度和训练稳定性上的优势，实验结果具有一致性。

### 6. 主要结论与发现
*   **性能提升：** ShapE-GRPO 在所有任务上均显著优于标准 GRPO 和 WTA。
*   **收敛加速：** 细粒度的奖励信号减少了训练噪声，使模型能够更快、更稳定地收敛。
*   **上下文利用：** 在 Netflix 推荐任务中，ShapE-GRPO 显著增强了模型利用长上下文（用户历史评分）的能力。
*   **计算可行性：** 候选者级别的 Shapley 计算引入的额外开销微乎其微，完全适用于大规模后训练。

### 7. 优点与亮点
*   **理论严谨：** 将博弈论中的公平分配原则引入 LLM 强化学习，解决了集合生成的信用分配（Credit Assignment）问题。
*   **高效实现：** 巧妙利用任务特性将原本复杂的 Shapley 计算简化，解决了实际工程应用中的算力瓶颈。
*   **通用性：** 方法不依赖于特定的奖励模型或模型架构，具有很强的普适性。

### 8. 不足与局限
*   **候选者识别依赖：** 该方法依赖于能够清晰地从模型输出中提取出 $K$ 个独立的候选者（通常依赖结构化 Prompt），对于非结构化输出的适用性较弱。
*   **奖励函数限制：** 虽然理论上支持通用奖励函数，但其高效计算的闭式解主要针对 $max$ 或线性组合等特定形式。
*   **模型规模：** 实验主要集中在 8B 规模的模型上，尚未在超大规模模型（如 70B+）上验证其扩展性。
*   **多样性风险：** 强化的 Shapley 奖励可能会导致模型过度关注某些高贡献特征，虽然实验中通过 Prompt 鼓励多样性，但长期训练是否会导致输出模式坍缩仍需观察。

（完）
