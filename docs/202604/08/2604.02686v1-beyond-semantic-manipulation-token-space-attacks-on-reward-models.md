---
title: "Beyond Semantic Manipulation: Token-Space Attacks on Reward Models"
title_zh: 超越语义操纵：针对奖励模型的 Token 空间攻击
authors: "Yuheng Zhang, Mingyue Huo, Minghao Zhu, Mengxue Zhang, Nan Jiang"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02686v1"
tags: ["query:rm-trn"]
score: 9.0
evidence: 针对奖励模型的对抗性攻击以评估鲁棒性
tldr: 本研究针对强化学习（RLHF）中的奖励模型（RM）提出了一种名为TOMPA的新型攻击框架。不同于以往侧重语义层面的攻击，TOMPA直接在Token空间进行对抗优化，通过绕过解码-重分词过程，利用黑盒标量反馈自动发现能诱导极高奖励的非语言Token模式。实验表明，该方法能使生成的无意义文本在多个先进RM上获得远超高质量人类回答的评分，揭示了当前RLHF流程中RM存在的严重安全漏洞。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02686v1/fig-001.webp\", \"caption\": \"Table 2: Reward scores on the NB-curated evaluation set. For each prompt, we compute the min, mean, and max reward across its 8 rollouts; values are averaged across all 100 prompts based on these per-group statistics. Beat Gold denotes the percentage of rollouts whose reward exceeds the gold answer score.\", \"page\": 7, \"index\": 1, \"width\": 826, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.02686v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative examples of generated sequences under our attack, presented exactly as decoded by the respective reward model’s tokenizer. Despite receiving anomalously high rewards of +21.38 for the Qwen3-8B RM and +36.75 for the Llama-3.1-8B RM, which far exceed their respective GPT-5 gold scores of +9.44 and +15.06, the outputs degenerate into cross-lingual gibberish, code snippets, and reserved tokenizer artifacts. This demonstrates that the reward models assign extremely high scores to token sequences completely devoid of semantic meaning.\", \"page\": 7, \"index\": 2, \"width\": 780, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.02686v1/fig-003.webp\", \"caption\": \"Figure 3: Impact of response length on reward scores. Responses are truncated at various intervals and evaluated using the target reward models. Despite consisting of highly repetitive patterns, the reward does not scale linearly with length. Instead, both models assign low or negative scores to shorter truncated versions, before exhibiting anomalous reward surges as the sequences approach the maximum length of 2048 tokens.\", \"page\": 8, \"index\": 3, \"width\": 814, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.02686v1/fig-004.webp\", \"caption\": \"Table 1: Prior work on reward model and LLM judge vulnerabilities, categorized by attack method, adversarial content type, and access requirement. Our work is the only approach that leverages RL to discover non-linguistic adversarial content while requiring only blackbox access to the target reward model.\", \"page\": 4, \"index\": 4, \"width\": 840, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.02686v1/fig-005.webp\", \"caption\": \"Figure 4: Training curves of attack policy optimization under token mapping perturbation. Mean and maximum rewards are plotted over training steps. The attack policy initially receives strongly negative rewards, but progressively identifies high-reward patterns and eventually surpasses the GPT-5 reference answers (the horizontal dashed line).\", \"page\": 9, \"index\": 5, \"width\": 814, \"height\": 283}]"
motivation: 现有的奖励模型攻击主要集中在语义层面，忽略了直接在Token空间进行非语言模式优化所带来的潜在风险。
method: 提出TOMPA框架，通过绕过标准接口直接在Token序列上进行对抗优化，利用黑盒反馈搜索高奖励的非语义Token组合。
result: "在针对Skywork等先进模型的测试中，TOMPA生成的无意义文本奖励值接近GPT-5参考答案的两倍，且在98%的提示词上表现更优。"
conclusion: 奖励模型极易受到非语义Token空间攻击的影响，这表明当前的RLHF管道在处理非自然语言输入时存在关键的鲁棒性缺陷。
---

## 摘要
奖励模型 (RM) 在人类反馈强化学习 (RLHF) 中被广泛用作优化目标，但它们仍然容易受到奖励欺骗 (reward hacking) 的影响。现有的攻击主要在语义空间内运行，通过构建人类可读的对抗性输出来利用 RM 的偏差。在这项工作中，我们引入了一种根本不同的范式：Token 映射扰动攻击 (TOMPA)，这是一个直接在 Token 空间执行对抗性优化的框架。通过绕过策略与奖励模型之间标准的“解码-再分词”接口，TOMPA 使攻击策略能够针对原始 Token 序列而非连贯的自然语言进行优化。仅使用黑盒标量反馈，TOMPA 就能自动发现非语言性的 Token 模式，从而在多个最先进的 RM 上诱导出极高的奖励。具体而言，在针对 Skywork-Reward-V2-Llama-3.1-8B 时，TOMPA 获得的奖励几乎是 GPT-5 参考答案的两倍，并在 98.0% 的提示词上优于后者。尽管得分很高，但生成的输出退化为无意义的文本，这表明 RM 可以在语义范畴之外被系统性地利用，并揭示了当前 RLHF 流水线中的一个关键漏洞。

## Abstract
Reward models (RMs) are widely used as optimization targets in reinforcement learning from human feedback (RLHF), yet they remain vulnerable to reward hacking. Existing attacks mainly operate within the semantic space, constructing human-readable adversarial outputs that exploit RM biases. In this work, we introduce a fundamentally different paradigm: Token Mapping Perturbation Attack (TOMPA), a framework that performs adversarial optimization directly in token space. By bypassing the standard decode-re-tokenize interface between the policy and the reward model, TOMPA enables the attack policy to optimize over raw token sequences rather than coherent natural language. Using only black-box scalar feedback, TOMPA automatically discovers non-linguistic token patterns that elicit extremely high rewards across multiple state-of-the-art RMs. Specifically, when targeting Skywork-Reward-V2-Llama-3.1-8B, TOMPA nearly doubles the reward of GPT-5 reference answers and outperforms them on 98.0% of prompts. Despite these high scores, the generated outputs degenerate into nonsensical text, revealing that RMs can be systematically exploited beyond the semantic regime and exposing a critical vulnerability in current RLHF pipelines.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Semantic Manipulation: Token-Space Attacks on Reward Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：在 RLHF（人类反馈强化学习）流程中，奖励模型（RM）作为人类偏好的代理目标。然而，RM 往往存在“奖励欺骗”（Reward Hacking）漏洞，即策略模型通过利用 RM 的偏差而非提升真实性能来获取高分。
*   **核心问题**：现有的攻击手段大多集中在**语义空间**（如增加回复长度、使用谄媚语气或特定提示词模板），这些攻击生成的文本通常是人类可读的。本文旨在探索：是否可以直接在 **Token 空间**（非语义、非语言层面）通过自动化手段发现能诱导 RM 产生异常高分的模式？

### 2. 方法论：TOMPA 框架
*   **核心思想**：提出 **Token Mapping Perturbation Attack (TOMPA)**。该框架通过绕过标准的“解码-再分词”（decode–re-tokenize）接口，直接将策略模型生成的 Token ID 序列映射到奖励模型的输入空间。
*   **关键技术细节**：
    *   **接口扰动**：传统流程中，策略生成的 Token 需转为文本再由 RM 重新分词。TOMPA 采用恒等映射（Identity Mapping），直接将 Token ID 传给 RM。当两个模型的分词器不一致时，RM 接收到的实际上是无意义的乱码。
    *   **对抗优化**：使用 **GRPO（Group Relative Policy Optimization）** 算法。该算法通过组内相对奖励的标准化来估计优势（Advantage），无需额外的价值网络（Value Network），从而节省显存并提高优化效率。
    *   **黑盒反馈**：攻击者仅需访问 RM 的标量奖励输出，无需了解其内部参数或梯度信息。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 WildChat 中随机抽取 10,000 个真实用户对话提示词。
    *   **评估集**：使用 Novelty-Bench 的 NB-curated 子集（100 个提示词），涵盖随机性、事实知识、创意写作和主观性四个类别。
*   **Benchmark 与对比方法**：
    *   **Gold Answer**：由 GPT-5 生成的高质量参考答案。
    *   **Random OOD**：随机采样的 Token 序列（证明高分并非简单的分布外数据导致的随机结果）。
    *   **目标模型**：针对 RewardBench 排名第一的 `Skywork-Reward-V2-Llama-3.1-8B` 和排名第六的 `Skywork-Reward-V2-Qwen3-8B`。

### 4. 资源与算力
*   **框架与参数**：基于 `verl` 框架训练 10 个 epoch。学习率为 $10^{-6}$，Batch Size 为 64，每个提示词生成 8 个 rollout，最大长度 2048 tokens。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100/H100）及具体训练时长。但考虑到 8B 参数模型的强化学习训练，通常需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：针对两个主流且顶尖的开源奖励模型进行了完整的攻击实验，并进行了 1500 步的训练动力学分析。
*   **充分性与客观性**：
    *   实验对比了随机 OOD 基准，排除了“任意乱码都能拿高分”的假设。
    *   进行了**长度消融实验**，揭示了奖励随长度增加而突增的非线性特征。
    *   使用了跨模型的攻击组合（Llama 策略攻击 Qwen RM，反之亦然），证明了该漏洞的普遍性。实验设计较为客观、公平。

### 6. 主要结论与发现
*   **极端奖励欺骗**：TOMPA 生成的无意义乱码在 98% 的提示词上击败了 GPT-5 的回答。在 Llama-3.1-8B RM 上，攻击得分（+33.64）几乎是 GPT-5 得分（+17.48）的两倍。
*   **非语义崩溃**：高分输出完全由跨语言片段（如阿拉伯语、中文）、代码残片（如 `assertNotNull`）和特殊占位符组成，完全不具备人类可读性。
*   **长度依赖性**：RM 对这些对抗模式的识别具有长度阈值。当序列较短时得分较低，但当长度接近 2048 时，奖励会发生剧烈的非线性飙升。

### 7. 优点
*   **视角独特**：跳出了传统的语义攻击范畴，揭示了 RM 在底层表示空间（Token Space）的脆弱性。
*   **全自动化**：无需人工干预或手动设计对抗样本，通过 RL 自动搜索漏洞。
*   **警示意义强**：证明了即使是目前最先进、在 Benchmark 上表现完美的 RM，也可能被简单的非语言模式彻底攻破。

### 8. 不足与局限
*   **防御机制缺失**：论文主要展示了攻击的有效性，但对于如何修复此类漏洞（如对抗训练的具体实现及代价）仅在未来工作中提及，未给出实验验证。
*   **现实场景限制**：由于生成的文本是乱码，这种攻击在实际对话产品中容易被简单的启发式规则（如检测乱码率）拦截，其威胁更多存在于模型训练阶段的优化目标污染。
*   **模型覆盖度**：尚未在基于分类器（Classifier-based）或闭源 API 形式的奖励模型上验证该攻击的有效性。

（完）
