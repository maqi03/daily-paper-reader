---
title: Learning to Translate from Soft to Hard LLM Prompts
title_zh: 从软提示到硬提示的翻译学习
authors: "Pitipat Kongsomjit, Suryansh Goyal, Jacob Whitehill"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27642v1"
tags: ["query:pe"]
score: 9.0
evidence: 将优化的软提示翻译为可移植的自然语言提示以提升模型性能
tldr: 软提示调优通过优化连续向量嵌入适配下游任务，虽参数高效却牺牲了可解释性，用户难以解读其内涵。受最近解释软提示的进展启发，本文训练一个从软提示到硬提示（自然语言）的专用翻译模型，以期获得高保真翻译。在多个Dataset of Datasets上，定量比较和人工评估一致表明，该翻译器产生的文本提示流畅通顺、语义准确，大幅优于现有的免训练方法InSPEcT。更重要的是，所生成的硬提示具有跨模型可移植性：在小规模开源模型上优化的软提示经翻译后，部署于大规模闭源API模型时，性能全面超越原始软提示，并在部分场景下优于少样本学习，为软提示的高效迁移与应用开辟新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27642v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 360, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2605.27642v1/fig-002.webp\", \"caption\": \"\", \"page\": 7, \"index\": 2, \"width\": 1800, \"height\": 1200}]"
motivation: 软提示调优缺乏可解释性，限制了理解和复用，亟需方法将软提示转化为可读的自然语言提示。
method: 训练一个软提示到自然语言的专用翻译模型，通过监督学习优化翻译质量，生成硬文本提示。
result: 在多个DoDs上，翻译模型输出的文本提示流畅准确，优于InSPEcT；且翻译后提示在闭源大模型上性能超过原始软提示。
conclusion: 本工作提升了软提示可解释性，并展示了软提示向可移植硬提示转化的潜力，为跨模型迁移提供新思路。
---

## 摘要
软提示调优是一种参数高效的方法，用于让大型语言模型适应特定任务，但缺乏可解释性。基于最近解释软提示的工作（Ramati 等，2024），我们探索了训练一个专用的软提示到自然语言翻译模型如何能产生更高的翻译质量。特别是在多个数据集的数据集（DoDs）上的定量和定性比较中，我们证明我们的翻译器能产生流畅、准确的言语表达，优于现有的免训练方法如 InSPEcT。除了推进可解释性，我们的工作还提示了一个有前景的下游应用：在小规模开源模型上优化的软提示可以被翻译成可移植的文本提示，当部署到更大的闭源 API 模型上时，其性能超过原始软提示，并且在某些情况下，甚至超过少样本学习。

## Abstract
Soft prompt tuning is a parameter-efficient method for adapting LLMs to specific tasks, but suffers from a lack of interpretability. Building on recent work on interpreting soft prompts (Ramati et al., 2024), we explore how training a dedicated soft prompt to natural language translation model can yield higher translation quality. In particular, in both quantitative and qualitative comparisons on multiple Datasets of Datasets (DoDs), we demonstrate that our translator produces fluent, accurate verbalizations that outperforms existing training-free methods like InSPEcT. In addition to advancing interpretability, our work suggests a promising downstream application: soft prompts optimized on small, open-source models can be translated into portable text prompts that, when deployed on larger closed-API models, exceed the performance of the original soft prompt and, in some cases, even few-shot learning.

---

## 论文详细总结（自动生成）

### 1. 核心问题与研究动机
- **研究背景**：软提示调优（soft prompt tuning）通过在输入序列前添加可学习的连续嵌入来高效微调大语言模型（LLM），参数效率极高，但缺乏可解释性。已有的直接投影或训练优化方法要么不可靠，要么会牺牲任务性能。
- **核心挑战**：如何将不可读的软提示精确翻译成自然语言描述，使其具有可解释性和跨模型可移植性。
- **整体含义**：作者旨在构建一个专用的“软提示到硬提示”翻译模型，不仅提升可解释性，还使得在小模型上训练出的软提示可转化为可移植文本提示，用于更大更强的闭源模型，从而实现性能跃升。

### 2. 方法论
- **核心思想**：将软提示翻译视为序列到序列的语言生成任务。通过构造“数据集的数据集”（Dataset of Datasets, DoD）获取大量（软提示，硬提示）配对数据，以此监督训练一个翻译模型。
- **翻译器架构与训练**：
  - 基座模型采用与软提示训练相同的 LLM：`meta-llama/Llama-3.1-8B-Instruct`。
  - 使用 LoRA 进行参数高效微调（rank=4，dropout=0.1，学习率 1e-4，批量大小 16，AdamW 优化器），将软提示嵌入作为输入，自回归生成自然语言硬提示，并以交叉熵损失进行训练。
  - 软提示本身使用 PEFT 等方法在各自下游任务数据集上训练获得。
- **数据集构造（DoDs）**：
  - **分类 DoD**：包含 5500 个数据集（2.75M 句子），每个数据集有 5 个紧密语义聚类标签，由 LLM 生成标签和相关句子。
  - **通用 DoD**：基于 Super-NaturalInstructions 数据集预处理得到，包含 539 个训练任务和 96 个验证任务，每个任务含 500 个示例。作者还通过 LLM 进行了数据增强（10 倍到 100 倍）来简化和扩充指令。
- **评估指标**：分类任务使用 Recall 和 F1-Score；通用任务使用 ROUGE-L 和余弦相似度；还引入了 Mean Percentile Rank（MPR）来衡量任务可区分性。

### 3. 实验设计与对比方法
- **对比基准**：
  - 主要对比免训练的 InSPEcT 方法（通过 Patchscopes 在多对层之间进行激活修补，需要网格搜索最佳层对）。
  - 还对比了直接使用软提示的小模型基线、不使用提示的大模型基线，以及少样本学习。
- **评估场景与数据集**：
  1. **InSPEcT 原始数据集**：SST-2, SST-5, AG-News, Subj, TREC——评估分类 DoD 翻译器的泛化效果。
  2. **分类 DoD**：测试翻译器在合成分类任务上的性能。
  3. **通用 DoD**：测试翻译器在多样化语言任务上的效果。
  4. **分布外泛化**：在真实世界任务（真假酒店评论检测、AI 生成 vs. 人类写作判别）上定性评估。
  5. **下游实用性验证**：将翻译出的硬提示提供给闭源大模型（GPT-4o-mini）推理，比较任务性能。

### 4. 资源与算力
- **文献明确提到**：使用 `meta-llama/Llama-3.1-8B-Instruct` 作为基座模型，LoRA 微调翻译器。
- **未提及的内容**：论文中未给出 GPU 型号、数量、训练时长等具体算力消耗数据。

### 5. 实验数量与充分性评估
- **实验规模**：
  - 构造了两种大型 DoD（分类 DoD 有 5500 个数据集，通用 DoD 有 539 个训练任务）。
  - 在 InSPEcT 的 5 个基准数据集、分类 DoD 测试集、通用 DoD 测试集上均进行了定量评估。
  - 进行了训练集大小消融（12%, 25%, 50%, 100%）和数据增强消融（1x, 10x, 100x）。
  - 探索了预填充（prefill）对风格对齐的影响。
  - 评估了下游实用性（与少样本学习的对比）。
- **充分性与客观性**：实验覆盖了大量不同任务类型，对比了主要基线方法，并考虑了层对选择等关键超参数优化，整体设计较为客观充分。但通用 DoD 训练集只有 539 个任务，规模不算大，翻译器仍有提升空间。

### 6. 主要结论与发现
- **翻译质量**：专用翻译模型在所有数据集上均优于训练免费的 InSPEcT，生成的硬提示语法流畅通顺，与真实任务描述有较高一致性，且对软提示任务准确率不敏感。
- **实用性**：从软提示翻译出的硬提示可成功应用于闭源大模型，性能超过原始软提示，甚至在某些复杂任务上优于少样本学习。
- **泛化性**：翻译器能够推广到训练时未见过的真实世界任务，展示了较强的“隐式语言”翻译能力。
- **风格对齐**：通过简单的预填充可改善翻译器过于简洁的风格缺失问题，暗示未来可进一步优化。

### 7. 优点与亮点
- **可解释性增强**：首次系统性地训练专用翻译模型，将神秘连续嵌入转为可读语言，大幅超越免训练方法。
- **跨模型可移植性**：提出了“小模型训练软提示 → 翻译为文本 → 大模型使用”的新工作流，兼具隐私与性能优势。
- **数据集贡献**：构建了大规模、高质量的分类 DoD 和通用 DoD，为软提示翻译研究提供坚实基础。
- **实验全面**：涵盖了分类、通用 NLP 任务、分布外测试、实用性验证等多个维度，并与多种基线进行了详细对比。

### 8. 不足与局限
- **模型绑定**：翻译器依赖特定基座模型（Llama-3.1-8B-Instruct），若软提示在其他模型上训练则需重新训练翻译器，缺乏跨模型通用性。
- **训练数据量**：通用 DoD 训练集仅有 539 个任务，虽然效果已经不错，但仍有提升空间。
- **风格对齐问题**：生成的硬提示有时过于简洁，丢失关键细节，目前仅用预填充缓解，未引入深层风格对齐机制。
- **下游应用验证有限**：下游实用性测试仅使用 GPT-4o-mini，未涵盖更多种闭源大模型，且仅基于通用 DoD 的部分任务。
- **未提及**：没有探索翻译器对对抗性软提示或恶意软提示的解释安全性。

（完）
