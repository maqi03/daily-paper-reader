---
title: Prompt Optimization for LLM Code Generation via Reinforcement Learning
title_zh: 基于强化学习的LLM代码生成提示优化
authors: "Ali Mohammadi Esfahani, Nafiseh Kahani, Samuel A. Ajila"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19102v1"
tags: ["query:pe"]
score: 10.0
evidence: 基于强化学习的提示优化，使用PPO结合遗传词汇变异和语义重写，由单元测试奖励信号引导。
tldr: "大型语言模型（LLMs）的代码生成性能对提示词高度敏感，但手动设计提示难以达到最优，现有自动优化方法考虑代码正确性不足。本文提出基于强化学习的提示优化框架，将迭代改进提示建模为序列决策，利用PPO智能体在混合动作空间（直接生成、遗传突变、语义重写）中搜索，并通过单元测试反馈塑造奖励。在MBPP+等多个基准上，该方法显著提升了三种主流模型的Pass@1分数，超越了现有方法，证明了测试驱动强化学习可有效提升代码生成正确性，为自动化提示优化提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19102v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 4684, \"height\": 1433}]"
motivation: 手动设计提示难以保证LLM代码生成的正确性，需要自动化测试驱动的提示优化方法。
method: 将提示优化视为序列决策，PPO代理在直接生成、遗传变异和语义重写组成的混合动作空间中搜索，利用单元测试奖励信号。
result: "在MBPP+上，三个主流LLM的严格Pass@1分别达到57.58%、64.80%和85.50%，均超越现有方法，并推广到多个基准。"
conclusion: 强化学习结合测试奖励能有效优化提示，显著提升代码生成的功能正确性，开辟了自动提示工程新路径。
---

## 摘要
大型语言模型（LLMs）能够根据自然语言生成代码，但其性能对提示的表述高度敏感。我们提出了一种基于强化学习的框架，将提示优化建模为序列决策问题。一个近端策略优化（PPO）智能体利用混合动作空间迭代改进提示，该空间结合了直接生成、遗传词汇变异和语义重写，并由单元测试反馈形成的塑形奖励引导。我们在MBPP+、HumanEval+和APPS数据集上评估了该框架，使用CodeT5+、CodeLLaMA和DeepSeek-Coder作为冻结的代码生成器。在包含500个任务的MBPP+测试集上，PPO智能体分别取得了57.58%、64.80%和85.50%的严格Pass@1得分，优于EPiC、Reflexion和Random-Hybrid。Soft-Pass@1分别达到67.90%、73.10%和88.20%。在HumanEval+和APPS上，所有骨干模型也观察到了类似的改进。结果表明，基于塑形测试驱动奖励的强化学习能够提高基于LLM的代码生成的功能正确性。

## Abstract
Large Language Models (LLMs) can generate code from natural language, but their performance is highly sensitive to prompt formulation. We propose a reinforcement-learning-based framework that models prompt refinement as a sequential decision-making problem. A Proximal Policy Optimization (PPO) agent iteratively improves prompts using a hybrid action space that combines direct generation, genetic lexical mutation and semantic rewriting, guided by shaped rewards derived from unit-test feedback. We evaluate the framework on MBPP+, HumanEval+, and APPS using CodeT5+, CodeLLaMA, and DeepSeek-Coder as frozen code generators. On the 500-task MBPP+ test set, the PPO agent achieves strict Pass@1 scores of 57.58%, 64.80%, and 85.50%, respectively, outperforming EPiC, Reflexion, and Random-Hybrid. Soft-Pass@1 reaches 67.90%, 73.10%, and 88.20%, respectively. Similar improvements are observed on HumanEval+ and APPS across all backbone models. The results demonstrate that reinforcement learning with shaped test-driven rewards improves functional correctness in LLM-based code generation.

---

## 论文详细总结（自动生成）

### 1. 研究背景与核心问题
- **核心问题**：大型语言模型（LLM）在代码生成任务中的性能高度依赖提示（prompt）的措辞与结构，但手工设计最优提示耗时且泛化性差，而现有自动化提示优化方法普遍缺乏对**功能正确性**的持续、密集反馈，难以适应需要迭代精化的复杂代码生成场景。
- **整体含义**：论文将提示优化重新建模为**序列决策问题**，提出用强化学习（RL）来自动化地、自适应地组合多种异构的提示变换策略，并利用**单元测试的部分正确性信号**作为塑形奖励，从而显著提高生成代码的功能正确性。

### 2. 方法论
- **核心思想**：构建一个基于**近端策略优化（PPO）** 的智能体，通过迭代观察当前提示的语义嵌入（状态），从包含三种动作的离散动作空间中采样一种提示变换，根据生成代码通过测试的比例获得塑形奖励，最终学习一种能够动态调度提示优化策略的策略网络。
- **关键技术细节**：
  - **状态表示**：使用 MiniLM 句子转换器将当前提示编码为 **384 维向量**，作为 PPO 策略的输入。状态仅包含提示信息，不包含生成代码或执行回溯信息。
  - **动作空间（混合动作空间）**：
    - **直接生成**：不修改提示，利用解码随机性探索。
    - **遗传词汇变异**：受 EPiC 启发，对提示进行词元层面的变异（交叉、打乱、变异概率0.2），基于测试通过率进行锦标赛选择。
    - **语义重写**：受 Reflexion 启发，使用预训练模型在固定元提示指导下对提示进行高层语义重构，并通过正则表达式过滤保留关键约束。
  - **奖励函数**：采用基于测试通过率 \(\rho_{i,t}\) 的**塑形奖励**：
    \[
    R_{i,t} =
    \begin{cases}
    1.0 & \text{所有测试通过} \\
    \rho_{i,t} & \text{部分测试通过} \\
    -1 & \text{零通过} \\
    -2 & \text{执行失败}
    \end{cases}
    \]
  - **训练流程**：每个编程任务为一个回合，智能体最多进行 \(T_i\) 步优化。每步生成代码、执行测试、计算奖励并存储转移，回合结束后用 PPO 更新策略。PPO 的在线学习过程被详细描述为算法1。
- **公式与算法**：文中以算法块形式给出了训练循环和评估协议，评估时使用严格 Pass@1 和辅助指标 SoftPass@1。

### 3. 实验设计
- **数据集/场景**：
  - **MBPP+**（974个任务，374训练/500测试）
  - **HumanEval+**（80%/20% 训练-测试划分）
  - **APPS**（80%/20% 划分）
  均需要生成可执行代码并通过单元测试评估。
- **评估指标**：
  - **严格 Pass@1**：只有通过所有测试才算成功（主要指标）。
  - **SoftPass@1**：累积各步通过概率之补，用于测量中间进展（辅助诊断指标）。
- **骨干代码生成模型**：CodeT5+、CodeLLaMA、DeepSeek-Coder（冻结、不开源商业模型）。
- **对比方法**：
  - **直接生成（Direct Generation）**：单步静态提示基线。
  - **EPiC**：基于遗传算法的进化式搜索。
  - **Reflexion**：基于语言反馈的自我反思优化。
  - **Random-Hybrid**：随机均匀选择三种动作，作用相同的混合动作空间但无策略学习，用于消融学习的作用。
  - 还包括不同奖励设计的对比（稀疏二元奖励 vs. 塑形奖励）。

### 4. 资源与算力
- 论文**未明确报告**训练所用的 GPU 型号、数量、训练时长或具体计算开销，仅在“威胁与局限性”中提到语义重写和 PPO 训练增加了计算成本（需要重复重写、生成、执行和奖励计算）。

### 5. 实验数量与充分性
- **实验组数**：
  - 三个基准 × 三种骨干模型 × 多种方法对比，构成至少 **3×3×5** 种主要配置。
  - 同时包含单步动作分析（RQ1）、奖励塑形消融（RQ2）、多步 RL 综合对比（RQ3）以及 Random-Hybrid 消融。
  - 对 MBPP+ 的 500 个任务的结果还进行了统计显著性检验（McNemar 检验和 t 检验）。
- **充分性与公平性**：
  - 实验覆盖从小规模代码任务（MBPP+/HumanEval+）到高难度竞赛题（APPS），模型选择覆盖不同规模和能力的编码 LLM。
  - 通过与 Random-Hybrid 对比，剥离了混合动作空间本身带来的收益，证明学习调度带来的增益，增强了实验公平性。
  - 统计检验增强了结论的可靠性，消融实验（奖励塑形 vs. 二元）揭示了塑形信号的重要性。

### 6. 主要结论与发现
- 在 MBPP+、HumanEval+ 和 APPS 三个基准上，**PPO 多步优化全面超越 EPiC、Reflexion 和 Random-Hybrid**。
- 以 MBPP+ 为例，PPO 在三种模型上的严格 Pass@1 分别达到 57.58%、64.80% 和 85.50%，均获得大幅提升。
- **奖励塑形**（基于部分正确性）是有效学习的关键，二元稀疏奖励训练不稳定且效果差。
- **学习到的动作调度**（PPO）显著优于随机混合动作（Random-Hybrid），证明增益来自策略，而非单纯的动作空间扩展。
- 即使单步语义重写也能带来明显改进，说明结构化提示澄清的通用益处。

### 7. 优点
- **问题新颖**：首次将基于测试的部分正确性信号与 RL 结合的**塑形奖励**引入代码生成提示优化的序列决策框架。
- **方法集成度高**：通过动作空间统一了基因突变、语义重写和直接探索，智能体学习何时使用何种操作。
- **实验扎实**：多数据集、多模型、多基线对照，包含重要的消融基线（Random-Hybrid）和统计检验。
- **可复现性考虑**：使用开源模型、提供训练与评估算法伪码，增强了透明度和复现可能。

### 8. 不足与局限
- **状态表征受限**：状态仅含提示嵌入，未纳入生成代码、失败测试或执行回溯等信息，限制了进行更深层调试的可能性。
- **计算开销未量化**：未报告 RL 训练所需的具体算力，难以评估实际应用的时间/资源成本。
- **威胁到泛化性**：评估限于固定测试集，真实世界编程任务的复杂性和多样性可能未被充分捕获；代码生成模型始终冻结，进步完全依赖提示优化。
- **语义重写依赖**：语义重写效果受限于外部重写模型与元提示设计，可能引入偏差或不符合任务意图的改写。

（完）
