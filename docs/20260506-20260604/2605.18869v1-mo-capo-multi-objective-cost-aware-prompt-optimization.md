---
title: "MO-CAPO: Multi-Objective Cost-Aware Prompt Optimization"
title_zh: MO-CAPO：多目标成本感知提示优化
authors: "Jan Büssing, Moritz Schlager, Timo Heiß, Tom Zehle, Matthias Feurer"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.18869v1"
tags: ["query:pe"]
score: 10.0
evidence: 多目标提示优化，在最大化输出质量的同时最小化推理成本
tldr: 现有自动提示优化方法大多只优化性能，忽视推理成本等多目标，且多目标优化效率低。MO-CAPO提出一种联合优化性能与成本的新算法，并通过预算分配实现成本高效的搜索。在多个任务和LLM上，MO-CAPO能发现多样、鲁棒的帕累托前沿，以更低预算获得有竞争力的性能，支持从业者按需选择性能-成本权衡的提示。
source: arxiv
selection_source: fresh_fetch
motivation: 现有提示优化仅关注性能，忽略成本；多目标方法依赖NSGA-II但优化效率低，且缺乏考虑部署成本的目标。
method: 提出MO-CAPO算法，联合优化性能和包含LLM推理完整计算概况的部署成本目标，并通过预算分配实现成本高效的搜索。
result: 在4项任务和3个LLM上，MO-CAPO在12例中的8例上以噪声R2指标优于NSGA-II基线，以更低预算获得有竞争力的性能，并揭示了多样的权衡方案。
conclusion: MO-CAPO能高效发现兼顾性能与成本的多样提示集，支持从业者灵活选择，且首次在评估中考虑了多目标优化的泛化和鲁棒性。
---

## 摘要
大型语言模型（LLMs）在广泛任务中表现强劲，但对提示设计高度敏感，这催生了自动提示优化的需求。现有方法主要关注性能，忽略了推理成本或延迟等竞争目标。同时，现有的多目标提示优化工作依赖于现成的NSGA-II，忽视了优化效率。为此，我们提出MO-CAPO，一种新颖的多目标提示优化算法，在联合优化性能和推理成本的同时，利用预算分配实现成本高效优化。我们进一步提出了一种面向部署的成本目标，刻画了LLM推理的完整计算轮廓。我们在四个任务和三个LLM上评估了我们的方法，并与基于NSGA-II的多目标方法以及先进的单目标提示优化器进行了比较。结果表明，MO-CAPO在保持成本效率的同时，持续识别出强健、多样且近似的帕累托前沿。在噪声R2指标上，其在12个案例中的8个上优于NSGA-II基线，并且通常以低得多的预算就实现了有竞争力的性能。发现的解集涵盖了单目标优化器忽略的多样化性能 - 成本权衡，而性能最优的候选方案仍与单目标解决方案具有竞争力。此外，我们首次对多目标机器学习实验进行了评估，通过噪声R2和近似间隙考虑泛化性和鲁棒性，从而能更实际地评估解决方案质量。MO-CAPO使从业者能够从高效发现的多提示集中进行选择，这些提示提供性能与成本之间的不同权衡。

## Abstract
Large language models (LLMs) achieve strong performance across a wide range of tasks but are highly sensitive to prompt design, motivating the need for automatic prompt optimization. Existing methods predominantly focus on performance alone, ignoring competing objectives such as inference cost or latency. At the same time, existing work on multi-objective prompt optimization relies on off-the-shelf NSGA-II, ignoring optimization efficiency. As a remedy, we introduce MO-CAPO, a novel multi-objective prompt optimization algorithm that jointly optimizes performance and inference cost while leveraging budget allocation for cost-efficient optimization. We further propose a deployment-oriented cost objective that captures the full computational profile of LLM inference. We evaluate our approach across four tasks and three LLMs and compare it to an NSGA-II-based multi-objective method and state-of-the-art single-objective prompt optimizers. Results show that MO-CAPO consistently identifies strong, robust, and diverse Pareto front approximations while maintaining cost-efficiency. It outperforms the NSGA-II baseline on 8 out of 12 cases in terms of the noisy R2 metric and achieves competitive performances often already at a considerably lower budget. The discovered solution sets span diverse performance-cost trade-offs that are omitted by single-objective optimizers, yet the top-performance candidates remain competitive with single-objective solutions. Additionally, we conduct the first evaluation of multi-objective machine learning experiments that considers generalization and robustness through noisy R2 and approximation gap, enabling a more realistic assessment of solution quality. MO-CAPO enables practitioners to select from an efficiently discovered set of multiple prompts offering different trade-offs between performance and cost.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义
*   **研究背景**：大型语言模型（LLM）的性能高度依赖于提示词的设计，因此自动提示优化至关重要。
*   **核心问题**：现有的提示优化工作主要存在两大缺陷。**第一**，它们绝大多数只聚焦于优化性能（如准确率）这一单一目标，完全忽略了推理成本、延迟等同样关键的竞争性目标。**第二**，少数考虑多目标的工作主要依赖现成的NSGA-II算法，未对优化过程本身的成本效率进行设计，而提示评估本身消耗大量计算资源。
*   **整体含义**：本文旨在解决**多目标成本感知的提示优化**问题，即如何**既高效又经济地**找到一组能平衡**预测性能**和**推理成本**的帕累托最优提示解集，供用户按需选择。

### 论文提出的方法论

*   **核心思想**：在单目标优化器CAPO的基础上进行扩展，将其升级为真正的多目标优化算法，并引入预算分配机制以提升优化效率。
*   **部署导向的成本目标函数**：
    *   提出一个新的成本目标函数，不只考虑提示长度（输入令牌数），而是**对输入和输出令牌进行加权求和**：$f_2(p; D) = w_{in}tok_{in}(p; D) + w_{out}tok_{out}(p; D)$。
    *   权重$w_{in}$和$w_{out}$可根据LLM的API定价（如从OpenRouter获取）或本地部署的延迟与资源消耗来设定，更真实地反映了现代LLM（尤其是推理模型）的完整计算开销。
*   **MO-CAPO算法流程**：
    *   **初始化**：构建初始提示种群，在一个随机抽样的数据块上评估其性能与成本，识别出非支配解集作为现任解集（Pinc）。
    *   **进化循环**：使用二进制锦标赛选择父代（优先选择现任解，其次根据拥挤距离或弱支配关系），通过LLM驱动的交叉和变异算子产生后代。
    *   **核心创新：成本感知的强化（Intensification）机制**：
        *   新的后代作为“挑战者”进入强化流程。挑战者在所有现任解均已评估的数据块子集上逐步增加评估。
        *   **提前终止**：每次新增评估后，挑战者会与当前最接近的现任解进行比较。如果挑战者被该现任解支配，则立即终止评估，节省预算。挑战者虽不被加入Pinc，但保留在种群中作为遗传材料。
        *   **完全评估与更新**：如果挑战者在所有共享数据块上均未被支配，则被加入种群和Pinc，并重新评估当前Pinc的非支配性。
    *   **环境选择**：当种群超出预设大小时，优先淘汰非现任解（评估次数少者先淘汰），若只剩现任解则依据拥挤距离淘汰，以保持种群多样性和帕累托前沿的覆盖。

### 实验设计
*   **基准模型与任务**：
    *   **LLM模型**：Mistral-3.2-24B, Qwen3-30B, GPT-OSS-120B。模型同时充当评估LLM和用于修改提示的元LLM。
    *   **数据集**：AG News（新闻分类）、Subj（主客观分类）、GSM8K（数学推理）、MBPP（代码生成）。
*   **对比方法**：
    *   **多目标基线**：NSGA-II-PO（使用NSGA-II的提示优化器）和未优化的初始指令。
    *   **单目标SOTA方法**：CAPO（主基线）、GEPA、EvoPromptGA。
*   **评估指标**：
    *   **多目标指标**：噪声R2（nR2）、乐观/悲观超体积（HV）、近似间隙（Gap）、经验成就面（EAS）。
    *   **单目标指标**：准确率。
    *   **效率指标**：候选解评估数量、首次结果产出时的预算消耗、达到80%性能所需时间（TT80）。
*   **成本权重设定**：尽管使用本地部署的LLM，但实验中的代价权重$w_{in}$和$w_{out}$取自OpenRouter平台的平均API美元定价，以使成本目标值可解释为“每100万次调用的潜在API成本”。

### 资源与算力
*   **文中明确提及**：所有计算在GPU集群上完成。每个实验配置单次运行**使用1张至少40GB显存的GPU**，具体型号为**NVIDIA A100或H100**。
*   **未明确说明**：论文未提及完成所有实验所使用的GPU总卡时数或优化单次运行的绝对时长。

### 实验数量与充分性
*   **实验规模**：实验设计非常全面，覆盖了**3个模型 × 4个任务**的12种组合。
*   **实验类型**：
    1.  **基准对比**：多目标方法（MO-CAPO）与多目标基线、单目标SOTA的全面性能与效率对比。
    2.  **消融研究**：
        *   **强化机制**：直接与无强化机制的NSGA-II-PO比较，量化效率提升。
        *   **成本权重**：系统性地将$w_{in}$和$w_{out}$设为零，分析各自对提示长度和性能的影响。
        *   **其他组件**：分析了父代选择、锦标赛中弱支配准则的作用。
    3.  **超参数敏感性分析**：考察了数据块大小、种群大小、每代交叉数等关键参数。
    4.  **定性分析**：通过提示组合分析（少样本示例数量、输入/输出令牌成本占比）展示了最终解集的多样性。
*   **实验充分性评估**：实验数量充足，对比维度丰富，考虑了不同任务类型和模型。每种配置使用3个独立随机种子控制数据划分和算法随机性，并通过统计图表（临界差异图）或标准偏差评估结果显著性，整体过程客观、公平。

### 论文的主要结论与发现
*   **多目标性能领先**：MO-CAPO在12个设定中的**8个上取得了最佳nR2指标**，优于NSGA-II-PO。
*   **优化效率极高**：强化机制使其能更快发现有竞争力的解集。相比无强化机制的基线，MO-CAPO在产出第一个解集时的预算消耗**最高减少了88%**，并且通常仅需不到总预算的1/3就能达到80%的最终性能。
*   **解集多样且实用**：MO-CAPO能发现覆盖不同性能-成本权衡的帕累托前沿，包含单目标优化器忽略的低成本高性价比方案，同时其最优性能解与SOTA单目标方法（如CAPO, GEPA）同样具有竞争力。
*   **算法组件有效性**：
    *   成本目标中的**输入令牌权重对控制提示长度至关重要**，而输出令牌权重的影响相对间接。
    *   算法对主要超参数的变化表现出较好的鲁棒性。

### 优点
*   **算法创新与效率**：首次将预算分配的强化机制引入多目标提示优化，解决了优化过程本身成本高昂的关键痛点。
*   **贴近实际的成本模型**：提出的加权成本目标函数比仅计算提示长度的做法更精确地反映了LLM部署的真实计算成本。
*   **评估协议的严谨性**：在提示优化领域首次系统性地引入和应用噪声R2、乐观/悲观HV等泛化鲁棒性指标，为多目标评估设立了新标准。
*   **实践价值高**：为用户直接提供一组多样化的帕累托最优提示，允许其在部署时根据实时成本和性能需求灵活切换。

### 不足与局限
*   **成本模型的局限性**：实验采用API均价作为成本代理，可能无法完全等同于实际计算时间或能量消耗。对于思考模型产生的大量内部推理令牌，文中虽提及但未进行专门实验。
*   **实验覆盖范围**：
    *   未涵盖当前顶尖的大型商业模型（如GPT-5.2 Pro或Claude Opus 4.6，文中仅列出其价格作为示例），可能限制其在最高端模型上的表现验证。
    *   目标维度仅限两维（性能 vs. 成本），未探索更高维度的目标（如公平性、安全性）。

（完）
