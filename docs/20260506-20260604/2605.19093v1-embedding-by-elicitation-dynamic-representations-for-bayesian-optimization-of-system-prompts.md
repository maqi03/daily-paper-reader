---
title: "Embedding by Elicitation: Dynamic Representations for Bayesian Optimization of System Prompts"
title_zh: 基于诱导的嵌入：用于系统提示贝叶斯优化的动态表征
authors: "Zhiyuan Jerry Lin, Benjamin Letham, Samuel Dooley, Maximilian Balandat, Eytan Bakshy"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19093v1"
tags: ["query:pe"]
score: 9.0
evidence: 利用LLM启发式嵌入进行系统提示的贝叶斯优化
tldr: 系统提示优化仅能使用聚合标量分数，无法获取逐例标签或错误信息，使得传统优化难以应对离散变长文本空间。本文提出ReElicit，一种基于嵌入诱导的贝叶斯优化框架。LLM根据任务描述与历史提示-分数对，动态提炼出紧凑可解释的特征空间，并将每个提示映射为向量。高斯过程代理模型学习评分与特征向量的关系，采集函数选取目标向量，LLM再由此生成或精炼新提示。每次获取新评价后，特征空间被重新诱导以适应知识积累。在十项系统提示优化任务中，总评估次数仅30次，ReElicit在所有仅依赖聚合评分的优化方法中表现最佳，从而展示了LLM作为自适应语义表示构建器支撑自然语言贝叶斯优化的潜力。
source: arxiv
selection_source: fresh_fetch
motivation: 系统提示调优仅能获取聚合标量分数，难以定位提示缺陷，传统黑盒优化无法高效应对离散大文本空间且样本受限。
method: 基于嵌入诱导：LLM动态构建可解释特征空间并映射提示；高斯过程建模评分，采集函数选择目标特征；LLM据此生成精炼提示，空间随评估重诱导。
result: 在10项系统提示优化任务、总计30次评估预算下，ReElicit在所有仅依赖聚合评分的基线中取得最佳性能。
conclusion: 这项工作表明，通过动态嵌入诱导，LLM能自适应构建语义表示来支撑贝叶斯优化，超越单纯提示生成，为复杂文本优化问题提供新范式。
---

## 摘要
系统提示是现代人工智能系统中的核心控制机制，塑造着跨对话、任务和用户群体的行为。然而，当反馈仅以聚合指标而非逐样本标签、错误或评注的形式提供时，它们难以调优。我们将这种聚合反馈设定研究为离散、变长文本上的样本受限黑箱优化。我们提出了ReElicit，一个基于“诱导嵌入”的贝叶斯优化框架。给定任务描述、先前评估过的提示和标量分数，大语言模型诱导出一个紧凑且可解释的特征空间，并将提示映射其中。借助概率高斯过程代理，采集函数随后选择目标特征向量，由大语言模型实现并细化为可部署的系统提示。随着新评估的到来重新诱导特征空间，使表征能够适应观察到的提示-分数历史。我们使用离线基准准确率作为受控的聚合代理来评估这一设定：优化器每提示观察到一个标量分数，且无逐样本标签、错误或评注。在十个系统提示优化任务中，总评估预算为30次，ReElicit在代表性的仅聚合提示优化基线中取得了最强的聚合性能概况。这些结果表明，大语言模型可以作为自适应语义表征构建器，而不仅仅是提示生成器，用于对自然语言产物的贝叶斯优化。

## Abstract
System prompts are a central control mechanism in modern AI systems, shaping behavior across conversations, tasks, and user populations. Yet they are difficult to tune when feedback is available only as aggregate metrics rather than per-example labels, failures, or critiques. We study this aggregate feedback setting as sample-constrained black-box optimization over discrete, variable-length text. We introduce ReElicit, a Bayesian optimization framework based on \emph{embedding by elicitation}. Given a task description, previously evaluated prompts, and scalar scores, an LLM elicits a compact, interpretable feature space and maps prompts into it. Leveraging a probabilistic Gaussian process surrogate, an acquisition function then selects target feature vectors, which the LLM realizes and refines into deployable system prompts. Re-eliciting the feature space as new evaluations arrive lets the representation adapt to the observed prompt-score history. We evaluate the setting using offline benchmark accuracy as a controlled aggregate proxy: the optimizer observes one scalar score per prompt and no per-example labels, errors, or critiques. Across ten system prompt optimization tasks with a 30 total evaluation budget, ReElicit achieves the strongest aggregate performance profile among representative aggregate-only prompt-optimization baselines. These results suggest that LLMs can serve as adaptive semantic representation builders, not only prompt generators, for Bayesian optimization over natural-language artifacts.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：系统提示是现代大语言模型的核心控制手段，但调优时往往只能获取聚合的标量指标（如任务完成率、满意度、安全事件率），而欠缺逐样本标签、错误信息或文本评注。在该反馈受限的设置下，如何高效地在离散、变长文本空间中进行样本受限的黑箱优化，是本文要解决的难题。
- **整体含义**：论文将系统提示调优建模为“仅观察聚合标量反馈的黑箱优化”，并提出一种利用大语言模型动态构建任务相关语义特征空间的贝叶斯优化框架，从而在极少评估次数下搜索高性能提示。

### 2. 方法论：ReElicit 框架
- **核心思想**：*嵌入诱导*（embedding by elicitation）——让大语言模型根据任务描述和已有的提示‑分数历史，自行提炼一组紧凑、可解释的语义特征轴，并将每个提示映射为该空间中的低维向量；随后在该连续空间上用高斯过程替代模型和采集函数进行贝叶斯优化，最后再让大语言模型将优化所得的目标特征向量“实现”为新的部署级系统提示。
- **算法流程**：
  - **特征定义**：给定历史提示集与对应标量分数，大语言模型分析高、低分提示的语义差异，提出一组互不冗余、可解释的数值特征，形成特征集 \(F_t\)。
  - **特征提取**：大语言模型将已有提示按 \(F_t\) 映射到 \([0,1]^{d_t}\)，得到嵌入矩阵 \(Z_t\)；通过交叉验证选择预测误差最小的特征集。
  - **代理建模与采集**：在选定的嵌入空间中拟合高斯过程，使用对数期望改进等采集函数选择一批目标特征向量。
  - **提示实现与精炼**：大语言模型依据历史提示、特征定义和目标向量，先生成候选提示，再通过特征差距反馈迭代精炼（feature‑gap refinement），直到与目标向量的 \(ℓ_2\) 距离低于阈值或达到迭代上限。
  - **动态重诱导**：每次获得新评估数据后重复上述过程，使特征空间随证据动态适应，逐步拉近与理想任务相关语义表示的距离。
- **理论支撑**：假设存在一个“神谕语义嵌入” \(g^*\)，真实目标函数在该空间中被近似，由此导出优化误差界 \(\epsilon = \delta + 2BL\eta_t\)，其中 \(\eta_t\) 是当前嵌入与神谕嵌入的差异，表明动态重诱导通过减小表征误差来收紧近优性的保证。

### 3. 实验设计
- **数据集与场景**：采用10个系统提示优化任务，包括GSM8K（数学推理）、MMLU（多学科知识）和BIG‑Bench Hard中的8个子任务（布尔表达式、因果判断、歧义消解、形式谬误、倒置形容词、表格企鹅、讽刺句检测、对象追踪）。每个任务使用固定规模的子集（GSM8K与MMLU各500题，BBH各250题），评估分仅作为聚合标量返回，优化器无法观察到任何个体标签或错误信息。
- **基准方法**：对比了四种仅依赖聚合反馈的硬提示搜索方法：
  - **APE样式采样**：无历史，直接让大语言模型生成多样化提示。
  - **OPRO**：基于分数排序的历史提示生成改进候选。
  - **PromptBreeder**：基于种群的进化算法（突变与重组）。
  - **TextGrad样式精炼**：分析历史轨迹后生成改进变体。
- **评估协议**：总评估预算固定为30次目标模型调用（5个提示/批次×6批次，第一项为共享种子集）。所有方法共享相同任务上下文、初始种子数据和反馈接口，保证公平对比。

### 4. 资源与算力
- 文中未提供显式的GPU型号、数量或训练时长。使用的优化器大语言模型为 Llama 3.3 70B Instruct，目标大语言模型为 Llama 3.1 8B Instruct。实验采用贪婪解码、温度0.0的目标模型评估，以及温度0.7的优化器侧调用。计算资源主要用于大语言模型推理和少量高斯过程拟合（BoTorch），但细节未明确量化。可以认为其计算负担主要来自大语言模型调用，文中未宣称大规模训练或昂贵计算。

### 5. 实验数量与充分性
- **实验数量**：每个任务独立运行30个不同的随机种子，报告均值与95%置信区间。除主结果外，还有：
  - 组件分析：特征提取重复性、跨迭代CKA相似性、GP交叉验证MSE、特征差距与改进概率关系。
  - 消融实验：无精炼（No Refinement）、无贝叶斯优化而随机采样（No BO）、静态特征（Static Features）、独立提取（Independent Extraction）。
  - 定性案例：特征演化的具体示例（如表4）。
- **充分性与公平性**：主实验覆盖10个任务、30个种子，统计显著性通过置信区间和两两胜负率（表2）检验，消融进一步分析各组件贡献，整体设计与对比公平。

### 6. 主要结论与发现
- ReElicit在所有对比方法中取得最强的整体性能概况（最高两两胜出率），在10个任务中均为最优或与最优无显著差异。
- 动态诱导的特征空间具有高稳定性（重复提取标准差小）同时又能适应新证据（跨迭代CKA均值为0.81），且能提升高斯过程替代模型的拟合效果（交叉验证MSE低于静态特征）。
- 特征差距精炼和贝叶斯优化目标选择是关键贡献组件：去除这两者会导致性能显著下降，而冻结特征空间或独立提取的影响相对较小。
- 理论分析表明，更准确地逼近理想语义嵌入能够收紧近优性保证，从而为动态重诱导提供了理论动机。

### 7. 优点
- **新颖的方法融合**：将大语言模型用作自适应的语义表示构建器，而非仅作为提示生成器，实现了在连续空间上对离散文本的贝叶斯优化。
- **组件完善与诊断深入**：不仅验证最终性能，还通过特征稳定性、代理拟合、精炼质量等多维度诊断，清晰揭示各设计选择的作用。
- **理论与实验相呼应**：提供了可解释的表征误差界，并在实验中验证了动态重诱导、精炼等组件的价值。
- **公平的对比设置**：所有方法共享相同的反馈接口和评估预算，聚焦于“仅聚合反馈”这一实际但研究不足的设定。

### 8. 不足与局限
- **代理指标的局限性**：评估使用离线基准准确率，而非真实的部署侧指标（如用户满意度、安全事件率），可能高估方法在动态、主观且延迟的真实场景中的效果。
- **额外的优化器侧开销**：ReElicit在优化过程中需要多次调用大语言模型用于特征诱导、提取、生成和精炼，相比传统方法增加了总调用成本（尽管论文强调目标评估往往是主要开销）。
- **任务范围的局限性**：仅限于GSM8K、MMLU和BBH等相对同质化的基准任务，未覆盖需要维护多轮对话状态、策略平衡、安全约束或高度开放的系统提示场景。
- **与其他嵌入方法的对比不足**：未与直接使用通用文本嵌入（如降维后的BERT嵌入）进行BO的方法详细比较，仅基于维度过高和解码不可行的论点排除了这些选项，但缺乏实证削弱。
- **理论假设严格**：理论依赖于存在平滑的神谕语义嵌入和误差界，实际中可能难以严格满足。
- **可解释性依赖大语言模型**：特征定义的质量和一致性依赖于大语言模型本身的能力，可能因模型受训数据或模型类型产生偏差。

（完）
