---
title: "iPOE: Interpretable Prompt Optimization via Explanations"
title_zh: iPOE：基于解释的可解释提示优化
authors: "Jiahui Li, Sean Papay, Roman Klinger"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18113v1"
tags: ["query:pe"]
score: 10.0
evidence: 通过解释引导的提示优化以最大化模型输出质量
tldr: "传统的提示优化方法通常依赖离散搜索，虽然能找到高性能提示，却无法解释为何特定修改带来提升，优化过程不透明。iPOE创新地借鉴了人类标注指南设计经验，利用自动或人工标注解释生成初始指南，再通过删除、添加、洗牌、合并等操作迭代优化，最终产出包含指南的提示，使得决策过程可解释、可追溯。在四个不同数据集的评估中，iPOE大幅优于无指南提示（最高提升31%）和随机指南提示（最高提升35%），并且使用LLM自动生成解释也能达到与人工解释相当的效果。iPOE为提示优化提供了可解释的新范式，降低了技术门槛，对需要领域知识的复杂任务尤其有帮助。"
source: arxiv
selection_source: fresh_fetch
motivation: 传统提示优化多依赖离散搜索，结果缺乏可解释性，令人难以把握性能增益的原因，尤其阻碍了在需专业知识的任务中的信任与应用。
method: 从自动或人工标注解释中提取初始指南，并通过删除、添加、洗牌、合并等操作迭代优化，生成可解释的提示指南。
result: "在四个数据集上，iPOE较无指南提示最多提升31%，较随机指南提示最多提升35%，且LLM解释可替代人工解释。"
conclusion: iPOE赋予提示优化可解释性，显著降低应用门槛，并验证了LLM解释替代人类解释的可行性，为专业领域提示优化开辟新途径。
---

## 摘要
提示优化通常被构架为一个离散搜索问题，旨在为大型语言模型（LLM）找到高性能且稳健的指令。然而，搜索结果可能无法揭示特定提示更改为何以及在哪里带来了性能提升。这与人们如何被指导完成标注任务形成对比。在人类标注任务中，研究人员精心设计标注指南，从而提高标注一致性。本文旨在将这两种方法结合起来，并提出iPOE，一种通过解释实现的新型可解释提示优化策略。我们通过自动生成的指南来引导提示优化过程，这些指南来源于标注决策的解释（可以是自动生成或人工提供）。这套指南还通过一系列操作进一步优化，包括删除、添加、打乱顺序和合并。最终得到的提示包含指导标注的指南，使得LLM的决策过程和优化过程变得透明。因此，它也能支持非专业人士进行提示优化，特别是在需要专业知识的挑战性领域。在四个数据集上的实验中，我们发现iPOE相较于无指南的提示和随机选择指南的提示，性能提升分别最高可达31%和35%。此外，LLM生成的解释可以替代该方法中的人工解释。

## Abstract
Prompt optimization has often been framed as a discrete search problem to find high-performing and robust instructions for an LLM. However, the search result might not make it transparent why and where specific prompt changes lead to performance gains. This is in contrast to how humans are instructed for annotation tasks. Here, researchers carefully design annotation guidelines, leading to enhanced annotation consistency. Our paper aims at joining these two approaches and introduces iPOE, a novel interpretable prompt optimization strategy via explanations. We guide the prompt optimization process by automatically created guidelines from explanations of annotation decisions (either automatically generated or from humans). This set of guidelines is furthermore optimized by as series of operations, including removing, adding, shuffling, and merging. The resulting prompt includes guidelines that instruct the annotation, making the decision process of the LLM and the optimization transparent. It therefore supports also laypeople in the area of prompt optimization, particularly in challenging domains requiring expertise. In our experiments on four datasets, we find that iPOE can improves over prompts without guidelines and with random selected guidelines by up to $31\%$ and $35\%$, respectively. Moreover, LLM explanations can replace human explanations in the proposed method.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究动机**：现有提示优化多被视为离散搜索问题，虽然能提升性能，但优化结果（为什么这些修改有效、具体哪些改动带来收益）不透明，缺乏可解释性。
- **与人类标注的对比**：在人工标注中，研究者会精心设计并迭代优化标注指南以提升一致性；而直接将这些人类指南用于 LLM 提示，效果往往不佳（因为 LLM 理解方式不同），且人工撰写高质量指南本身门槛高。
- **整体含义**：论文提出将“迭代优化指南”的思路引入 LLM 提示优化中，通过自动生成或人工撰写的标注解释来生成指南，并进一步优化指南集，使得提示既高性能又可解释，决策过程透明化。

### 2. 方法论

- **核心思想**：利用解释（人工标注解释或 LLM 生成解释）构建“指南池”，然后通过迭代操作（添加、删除、替换、合并、打乱顺序）动态优化当前的指南集，最终生成包含指南的可解释提示。
- **算法流程（文字说明）**：
  - **指南池生成**：使用 LLM 从带解释的训练数据中提取规则式指南，形成候选池 \(R\)。
  - **初始化**：当前指南集 \(G = \emptyset\)。
  - **迭代优化**：
    - 从 \(R\) 中采样一小部分指南 \(g\)。
    - 对 \(g\) 分别执行所有操作（添加、删除、替换、合并、打乱），得到候选 \(G'\)。
    - 评估每个候选提示 \(P \oplus o(G,g)\) 在训练集上的性能 \(F\)。
    - 选择性能最高的操作结果作为新的 \(G\)（仅当相比当前 \(G\) 的性能有提升时才更新）。
    - 重复直至预设最大迭代次数 \(I\)。
- **采样策略**：提供无控制（随机采样）和标签控制（按标签均匀采样，确保每个标签都有指南）两种变体。
- **操作定义**：添加（追加指南）、删除（随机或按标签移除）、替换（移除后添加新指南）、合并（用 LLM 将同标签的多条指南合并为一条）、打乱（调整指南顺序）。

### 3. 实验设计

- **数据集**：4 个英文数据集，涵盖三个领域：
  - **crowd-enVent**（情绪分类，提供自然语言解释）。
  - **HealthFC**（健康事实核查，提供自然语言解释）。
  - **HealthVer**（健康事实核查，无人工解释，用于跨分布评估）。
  - **HateXplain**（仇恨言论检测，提供特征归因解释）。
- **基准方法**：
  - 普通提示（无指南）。
  - 随机选择指南集（大小等于标签数）。
- **模型**：3 个可使用部署的 LLM：LLama-3.1-8B-Instruct、Qwen3-4B-Instruct-2507、Qwen3-30B-A3B-Instruct-2507。
- **对比维度**：
  - 指南来源：人工解释 vs. LLM 生成解释。
  - 优化策略：有/无标签控制。
  - 解释类型：自然语言解释 vs. 特征归因（在 HateXplain 上对比）。
  - 域外评估：在 HealthVer 上使用 HealthFC 的指南进行跨分布测试。

### 4. 资源与算力

- **硬件**：Nvidia A40 GPU。
- **总计估算**：约 15860 GPU 小时。
- **解码策略**：解释和指南生成采用随机采样（top_p=0.9, temperature=0.6, top_k=50），标签预测采用贪婪解码以减少不确定性。
- **优化迭代设定**：LLama-8B 和 Qwen3-4B 最多 500 次迭代，Qwen3-30B 最多 300 次（受算力限制）。

### 5. 实验数量与充分性

- **实验组数**：4 个数据集 × 3 个模型 × 多种配置（4 种 iPOE 变体 + 2 种基线），组合约 70+ 组对比实验，同时包含跨数据集、跨解释类型的分析。
- **充分性**：数据集涵盖多领域和两种解释类型；消融对比涵盖解释来源、采样控制、域外迁移；记录训练与验证的学习曲线；定性分析给出案例。整体实验设计较为全面，但受算力限制仅进行了一轮优化，且无人类评估验证可解释性的主观感受。

### 6. 主要结论与发现

- **iPOE 显著优于基线**：相比无指南提示最多提升 31%，比随机指南提示最多提升 35%。标签控制影响较小。
- **LLM 解释可替代人工解释**：使用 LLM 生成解释产出的指南，性能与人工解释相当，大幅降低对人工标注解释的依赖。
- **域外迁移有效**：从 HealthFC 生成的指南应用于 HealthVer 同样有效，表明指南可支持同域下的分布外推理。
- **不同解释类型均可生效**：特征归因和自然语言解释均可生成有效的指南。

### 7. 优点

- **可解释性**：优化过程以指南的形式展现，使提示的优化逻辑和模型的决策依据透明化，降低提示工程的技术门槛。
- **灵活且自动化**：可利用 LLM 自动生成解释和指南，减少对专业标注者的依赖。
- **方法论合理**：借鉴人类标注指南的迭代优化经验，并适配 LLM 的运作方式。
- **实验扎实**：多数据集、多模型、多配置的对比验证，并包含跨域和跨解释类型的分析。

### 8. 不足与局限

- **算力与迭代限制**：只进行了一轮优化（单次迭代上限固定），多轮优化可能进一步提升稳定性。
- **解释数据稀缺**：特征归因等人力解释数据有限，限制了部分对比的广度。
- **缺乏人类评估**：虽声称指南提升了可解释性，但未通过用户研究验证非专业人士能否真正理解和使用优化过程。
- **收敛与稳定性**：学习曲线显示优化过程中性能仍有波动，未探索更高效的收敛策略。
- **提示鲁棒性**：未与针对前缀提示的扰动方法进行对比，对提示微小变化的鲁棒性未充分检验。

（完）
