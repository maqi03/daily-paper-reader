---
title: "PEML: Parameter-efficient Multi-Task Learning with Optimized Continuous Prompts"
title_zh: "PEML: 参数高效的多任务学习与优化的连续提示"
authors: "Anjir Ahmed Chowdhury, Syed Zawad, Xiaolong Ma, Xu Dong, Feng Yan"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14055v1"
tags: ["query:pe"]
score: 9.0
evidence: 优化连续提示以实现多任务LLM微调
tldr: "多任务大语言模型部署需同时高效适配模型权重与优化提示，但现有参数高效微调方法（如LoRA和Prefix Tuning）通常只侧重其一，限制多任务性能。PEML提出联合神经架构搜索优化连续提示与低秩适配的方案，自动化协同优化。在GLUE、SuperGLUE、MMLU等基准上，PEML平均准确率提升6.67%，单任务最高提升10.75%，显著优于MTL-LoRA等前沿方法。该工作揭示了多任务学习中提示与模型协同优化的关键作用，为资源高效的单模型多任务部署提供新途径。"
source: arxiv
selection_source: fresh_fetch
motivation: 现有PEFT方法如LoRA侧重权重适配而忽视提示优化，Prefix Tuning提示架构简单，缺乏协同优化限制了多任务适应能力。
method: 通过神经架构搜索自动优化连续提示结构，结合模型权重的低秩适配，实现参数高效的多任务联合优化。
result: "在GLUE等四个基准上，PEML平均准确率提升最高6.67%，单任务最高提升10.75%，显著优于MTL-LoRA、MultiLoRa等方法。"
conclusion: 协同优化连续提示与模型低秩适配是提升多任务学习性能的关键，PEML为此提供了高效且显著提升的方案。
---

## 摘要
参数高效微调（PEFT）被广泛用于使大型语言模型（LLMs）适应各种任务。最近，对单个LLM进行多任务微调的需求日益增长，因为由于任务间共享的共同特征，微调所需的数据总体较少。更重要的是，LLMs资源需求大，部署单个模型用于多任务有助于资源整合，并且与为每个任务部署单独的大型模型相比，消耗的资源显著减少。现有的PEFT方法如LoRA和Prefix Tuning旨在使LLMs适应特定任务。LoRA及其变体专注于调整模型本身以适配任务，忽视了多任务学习中提示调优的重要性，而Prefix Tuning仅采用简单的架构来优化提示，这限制了多任务适应的能力。为了实现多任务学习的高效微调，协同优化提示优化和模型适应至关重要。在这项工作中，我们提出了参数高效多任务学习（PEML），它采用神经架构工程方法来优化连续提示，同时对模型权重进行低秩适应。我们通过创建一个自动化框架来优化连续提示和调整模型权重，实现了PEML的原型。我们在GLUE、SuperGLUE、大规模多任务语言理解和常识推理基准上，将PEML与最先进的多任务学习方法MTL-LoRA、MultiLoRa、C-Poly和MoE进行了评估。评估结果显示平均准确率提升高达6.67%，个别任务的峰值增益高达10.75%。

## Abstract
Parameter-Efficient Fine-Tuning (PEFT) is widely used for adapting Large Language Models (LLMs) for various tasks. Recently, there has been an increasing demand for fine-tuning a single LLM for multiple tasks because it requires overall less data for fine-tuning thanks to the common features shared among tasks. More importantly, LLMs are resource demanding and deploying a single model for multiple tasks facilitates resource consolidation and consumes significantly less resources compared to deploying individual large model for each task. Existing PEFT methods like LoRA and Prefix Tuning are designed to adapt LLMs to a specific task. LoRA and its variation focus on aligning the model itself for tasks, overlooking the importance of prompt tuning in multi-task learning while Prefix Tuning only adopts a simple architecture to optimize prompts, which limits the adaption capabilities for multi-task. To enable efficient fine-tuning for multi-task learning, it is important to co-optimize prompt optimization and model adaptation. In this work, we propose a Parameter-Efficient Multi-task Learning (\PM), which employs a neural architecture engineering method for optimizing the continuous prompts while also performing low-rank adaption for model weights. We prototype PEML by creating an automated framework for optimizing the continuous prompts and adapting model weights. We evaluate PEML against state-of-the-arts multi-task learning methods MTL-LoRA, MultiLoRa, C-Poly, and MoE, on the GLUE, SuperGLUE, Massive Multitask Language Understanding, and commonsense reasoning benchmarks. The evaluation results present an average accuracy improvement of up to 6.67%, with individual tasks showing peak gains of up to 10.75%.

---

## 论文详细总结（自动生成）

好的，以下是对论文《PEML: Parameter-efficient Multi-Task Learning with Optimized Continuous Prompts》的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：随着大型语言模型（LLMs）规模的增长，传统的全参数微调成本高昂。参数高效微调（PEFT）虽能解决此问题，但现有PEFT方法（如LoRA和Prefix Tuning）主要为单任务设计。
*   **核心问题**：在多任务学习场景下，现有方法存在明显局限。LoRA及其变体侧重于模型权重的低秩适配，**忽视了连续提示的优化**；而Prefix Tuning等提示方法则采用**简单的固定架构**，限制了多任务适应能力。更重要的是，它们缺乏一种**协同优化**模型适配与提示对齐的机制，导致多任务间知识共享不足，且部署时需频繁切换适配器，效率低下且资源管理复杂。
*   **整体含义**：本文提出PEML框架，旨在通过**协同优化连续提示的结构（提示对齐）和模型权重的低秩适应（模型适配）**，以一种参数高效的方式实现更优的多任务学习性能，并简化部署流程。

### 2. 论文提出的方法论

*   **核心思想**：将神经架构搜索（NAS）引入连续提示优化，与LoRA低秩适配进行**联合优化**。通过自动化搜索出最优的共享连续提示架构，并与低秩矩阵同时训练，使提示与模型能更好地协同工作。
*   **关键技术细节**：
    *   **整体架构**：PEML集成两部分（见图1）：
        1.  **低秩适配**：在注意力模块的Key和Value投影层中引入LoRA矩阵（\`B, A\`）。
        2.  **连续提示优化**：使用**PrefixNAS**自动搜索出一个最优的、可学习的连续前缀向量（\`P_k, P_v\`），并将其拼接到Key和Value序列前，以引导模型在不同任务上的行为。
    *   **PrefixNAS**：
        *   **搜索空间**：候选操作包括不同的激活函数（如ReLU, GELU）、Dropout率和归一化层的组合。
        *   **搜索策略**：采用基于梯度下降的**可微分架构搜索**，通过连续松弛（Continuous Relaxation）对候选操作加权求和，训练架构参数α。
        *   **最终选择**：训练收敛后，选择权重最大的操作构成最终的离散前缀架构。
    *   **联合优化问题**：将多任务学习形式化为一个联合损失函数的优化问题，该函数包含任务损失和架构正则化项。LoRA参数（\`B, A\`）和PrefixNAS的架构参数（\`α\`）在训练的每次迭代中**同步更新**。
    *   **超参数优化**：采用双层优化，内层使用PrefixNAS优化架构参数α，外层使用Tree-structured Parzen Estimator (TPE)优化学习率、前缀长度等超参数。

### 3. 实验设计

*   **数据集/场景**：评估覆盖了四个主流的自然语言理解基准测试：
    *   **通用语言理解**：GLUE (SST-2, COLA, STS-B等7个任务)
    *   **高级多句推理**：SuperGLUE (BoolQ, RTE, WIC等6个任务)
    *   **大规模多任务语言理解**：MMLU
    *   **常识推理**：PIQA, SIQA, Winogrande, OBQA, HellaSwag, ARC, BoolQ
*   **基础模型**：在三种主流LLM架构上进行了验证：**T5-Large (770M)**、**FLAN-T5-Large**、**LLaMA-7B** 和 **LLaMA2-7B**。
*   **对比方法**：与多个领域的**最先进（SOTA）** 多任务PEFT方法进行比较，包括：
    *   多任务LoRA扩展：**MTL-LoRA, MultiLoRA**
    *   模块化/技能组合方法：**C-Poly**
    *   混合专家方法：**MoE-LoRA (MoE)**
    *   基础PEFT方法：**LoRA, AdaLoRA**，以及它们与简单Embedding层（PreEmbedd）的组合。

### 4. 资源与算力

*   **硬件**：所有实验使用 **8 块 NVIDIA A100 40GB GPU** 进行分布式训练。
*   **算力开销**：
    *   **神经架构搜索**：对于LLaMA变体，搜索约需2小时（即16 GPU小时）；对于T5-large变体，搜索约需30分钟（即4 GPU小时）。
    *   **总训练成本**：以LLaMA2-7B的GLUE任务为例，训练、超参数调优和NAS搜索的总时间约为**18.5 GPU小时**。
    *   **资源对比**：论文通过实验指出，其方法在可比甚至更少的计算时间内，取得了优于MTL-LoRA等需要大量人工调参的方法的性能。同时，PEML避免了MultiLoRA因并行模块导致的**显存（VRAM）线性增长**问题。

### 5. 实验数量与充分性

*   **实验数量**：实验设计非常全面，覆盖了**4个不同性质的数据集系列、4种不同规模和架构的基础模型**，并与**超过6种主流PEFT和多任务PEFT方法**进行比较。
*   **充分性与客观性**：
    *   **主要结果**：提供了在GLUE, SuperGLUE, MMLU和常识推理上的性能对比表，清晰展示了PEML的优势。
    *   **消融与敏感性分析**：对PrefixNAS的关键设计（层数、搜索空间操作、优化顺序）、离散化稳定性、任务采样权重等进行了详尽的消融研究（见表7.8和图3-5），证明了各部分设计的有效性。
    *   **公平性考量**：确保对比方法的秩和参数总量在同一水平（如与MultiLoRA的对比），并从其他论文直接引用基线结果，遵循了标准的评估协议。
    *   **额外分析**：还进行了低数据场景、跨基准架构迁移、异构任务、不同LLM对比（DeepSeek, Qwen2）等实验，丰富了论据。

### 6. 论文的主要结论与发现

*   **性能显著提升**：PEML在多个基准上表现最佳。例如，在GLUE上用LLaMA2-7B取得了**91.1%** 的平均分，高出最强基线MTL-LoRA **1.1个百分点**；在SuperGLUE上用FLAN-T5-Large超出C-Poly **1.1个百分点**；平均准确率提升高达**6.67%**，单任务峰值提升达**10.75%**。
*   **协同优化的优越性**：实验证明，简单结合LoRA和固定前缀（如LoRA-PreEmbedd）几乎无提升，而PEML通过**联合、自动化地**搜索最优提示架构，证明了协同优化提示与模型是多任务PEFT成功的关键。
*   **架构的泛化性与鲁棒性**：PrefixNAS搜索到的架构能在相似任务间有效迁移，减少了搜索成本。同时，该方法对低数据场景也表现出鲁棒性。

### 7. 优点

*   **创新性强**：首次将神经架构搜索方法引入多任务学习的连续提示优化中，并与LoRA结合进行端到端联合优化，填补了“提示-模型协同适配”的空白。
*   **性能突出**：在多个权威基准上一致地、显著地超越了现有的SOTA方法。
*   **设计高效且优雅**：生成一个**统一的、共享的适配器**，避免了多任务部署时切换适配器的开销和延迟，且不会像MultiLoRA那样导致显存线性增加。
*   **论证扎实**：提供了充足的主实验、消融实验、成本分析和收敛性理论分析，结论可信度高。
*   **自动化程度高**：PrefixNAS和集成的TPE超参数搜索，减少了人工设计提示架构和调参的负担。

### 8. 不足与局限

*   **一次性搜索成本**：NAS过程本身会引入额外的计算开销（尽管作者认为这是一次性的，且与基线方法所需的大量调参成本可比）。
*   **任务性能的折中**：在个别任务（例如HellaSwag）上，PEML的绝对性能低于某些强基线（如MTL-LoRA），这是多任务学习中广泛存在的“跷跷板”现象。虽然可以通过调节任务权重缓解，但这会牺牲整体平均性能。
*   **模型/任务范围限制**：实验主要在编码器-解码器（T5）和仅解码器（LLaMA）的自然语言理解任务上进行，其在生成任务或更大规模模型上的表现有待验证。
*   **增加额外参数**：为了统一多任务，前缀模块引入了额外参数，增加了总参数量（约2%-8%）。

（完）
