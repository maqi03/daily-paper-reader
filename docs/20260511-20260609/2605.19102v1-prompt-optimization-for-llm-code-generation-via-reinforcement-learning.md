---
title: Prompt Optimization for LLM Code Generation via Reinforcement Learning
title_zh: 基于强化学习的大语言模型代码生成提示优化
authors: "Ali Mohammadi Esfahani, Nafiseh Kahani, Samuel A. Ajila"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19102v1"
tags: ["query:prompt-evol"]
score: 9.0
evidence: 利用RL与测试反馈迭代进化代码生成提示
tldr: "大型语言模型（LLMs）的代码生成高度依赖提示词质量，现有手动设计提示的方式难以保证最优。为此，本文提出基于强化学习的Prompt优化框架，将其建模为序贯决策问题，利用PPO智能体在混合动作空间（直接生成、遗传变异、语义重写）中迭代改进，并借助单元测试反馈塑造奖励。在MBPP+、HumanEval+和APPS上使用CodeT5+、CodeLLaMA和DeepSeek-Coder评估，该方法在MBPP+严格Pass@1分别达到57.58%、64.80%和85.50%，全面超越EPiC等基线。结果表明强化学习结合测试驱动奖励能显著提升LLM代码生成的功能正确性，为自动提示工程提供了有效新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19102v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1234, \"height\": 371, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.19102v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1255, \"height\": 1017, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.19102v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 624, \"height\": 1044, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19102v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 623, \"height\": 1170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19102v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19102v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1258, \"height\": 566, \"label\": \"Table\"}]"
motivation: LLM代码生成性能高度依赖提示词设计，手动调优效率低且易陷入次优，需要自动化优化方法。
method: 提出基于强化学习的提示优化方法，使用PPO智能体通过直接生成、遗传变异和语义重写等混合动作迭代改进提示，并以单元测试反馈为塑造奖励。
result: "在MBPP+等基准上，PPO智能体实现严格Pass@1最高85.50%（DeepSeek-Coder），全面优于EPiC、Reflexion等基线。"
conclusion: 强化学习结合测试驱动的塑造奖励能显著提升LLM代码生成的功能正确性，为自动提示工程提供有效途径。
---

## 摘要
大型语言模型可以从自然语言生成代码，但其性能对提示的表述高度敏感。我们提出一个基于强化学习的框架，将提示精炼建模为顺序决策问题。一个近端策略优化（PPO）代理利用混合动作空间迭代改进提示，该空间结合了直接生成、遗传词汇变异和语义重写，并由单元测试反馈产生的形状奖励引导。我们在MBPP+、HumanEval+和APPS数据集上，使用CodeT5+、CodeLLaMA和DeepSeek-Coder作为冻结的代码生成器来评估该框架。在包含500个任务的MBPP+测试集上，PPO代理分别达到57.58%、64.80%和85.50%的严格Pass@1分数，优于EPiC、Reflexion和Random-Hybrid。Soft-Pass@1分别达到67.90%、73.10%和88.20%。在HumanEval+和APPS上，所有骨干模型也观察到类似的改进。结果表明，带有形状测试驱动奖励的强化学习能提高基于LLM的代码生成的功能正确性。

## Abstract
Large Language Models (LLMs) can generate code from natural language, but their performance is highly sensitive to prompt formulation. We propose a reinforcement-learning-based framework that models prompt refinement as a sequential decision-making problem. A Proximal Policy Optimization (PPO) agent iteratively improves prompts using a hybrid action space that combines direct generation, genetic lexical mutation and semantic rewriting, guided by shaped rewards derived from unit-test feedback. We evaluate the framework on MBPP+, HumanEval+, and APPS using CodeT5+, CodeLLaMA, and DeepSeek-Coder as frozen code generators. On the 500-task MBPP+ test set, the PPO agent achieves strict Pass@1 scores of 57.58%, 64.80%, and 85.50%, respectively, outperforming EPiC, Reflexion, and Random-Hybrid. Soft-Pass@1 reaches 67.90%, 73.10%, and 88.20%, respectively. Similar improvements are observed on HumanEval+ and APPS across all backbone models. The results demonstrate that reinforcement learning with shaped test-driven rewards improves functional correctness in LLM-based code generation.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **核心问题**：大语言模型（LLM）在根据自然语言描述生成代码时，输出质量高度依赖提示（prompt）的表述方式。现有的提示工程方法多为静态设计（如思维链），缺乏对中间反馈的自适应能力，难以通过迭代精炼来提升代码的功能正确性。
- **整体含义**：本文旨在将提示优化建模为一个**序贯决策问题**，利用强化学习（尤其是PPO）让一个智能体根据单元测试的反馈，动态地选择“直接生成”、“词汇级变异”或“语义重写”等操作，不断改进提示，从而提升冻结的代码生成模型的通过率。

## 2. 论文提出的方法论
- **核心思想**：将每一道编程题视为一个 episode，智能体观察当前提示的语义嵌入（状态），从混合动作空间中选择一个提示变换操作，生成新提示后送入冻结的代码模型生成代码，再依据单元测试的通过比例计算形状奖励，以此训练策略网络。
- **关键技术细节**：
  - **状态表示**：使用 MiniLM 将当前提示文本映射为 384 维向量，作为策略网络的输入。
  - **混合动作空间**：
    - *直接生成*：保持当前提示不变，仅利用解码随机性尝试不同输出。
    - *词汇变异*：基于遗传算法，对提示进行交叉、索引重排和变异（变异概率0.2），通过锦标赛选择保留通过测试数较多的变异体。
    - *语义重写*：利用一个预训练模型，根据固定的元提示对当前提示进行结构化重写，并用正则表达式过滤保留任务关键词。
  - **奖励函数**：使用**形状奖励**替代稀疏二元奖励。奖励根据单元测试的通过比例 \(\rho_{i,t}\) 给出：全部通过给 1.0，部分通过给 \(\rho_{i,t}\)，全部失败给 -1，执行出错（如语法错误）给 -2。
  - **训练算法**：采用 PPO 进行策略更新，利用裁剪目标稳定学习。每回合结束后用存储的 transition 做小批量更新。
- **核心公式/流程**（文字说明）：
  - 步骤 t 的状态 \(s_{i,t}\) 是提示 \(p_{i,t}\) 的嵌入。
  - 策略 \(\pi_\theta\) 输出动作概率，采样动作 \(a_{i,t}\) 并应用变换算子 \(T\) 得到 \(p_{i,t+1}\)。
  - 生成代码 \(f_{i,t}\)，执行得到每个测试点的正确性指示 \(c_{i,t,k}\)，计算通过率 \(\rho_{i,t} = \frac{1}{K_i}\sum_{k=1}^{K_i} c_{i,t,k}\)。
  - 奖励 \[ R_{i,t} = 
    \begin{cases} 
    1.0, & \rho_{i,t}=1.0 \\
    \rho_{i,t}, & 0<\rho_{i,t}<1.0 \\
    -1, & \rho_{i,t}=0 \\
    -2, & \text{执行失败}
    \end{cases} \]
  - 基于累积奖励更新策略。

## 3. 实验设计
- **数据集/场景**：
  - MBPP+（测试集500题，训练集374题）
  - HumanEval+（80%/20%训练测试划分）
  - APPS（同上划分）
  - 所有数据集均包含可执行的单元测试。
- **基准模型（冻结的代码生成器）**：CodeT5+、CodeLLaMA、DeepSeek-Coder，均不开源调优。
- **对比方法**：
  - *静态/单步基线*：直接生成、仅词汇变异、仅语义重写（用于RQ1）。
  - *迭代基线*：EPiC（基于进化搜索）、Reflexion（基于语言反馈）、Random-Hybrid（与PPO共享相同动作空间但均匀随机选择动作，用于消融学习调度的影响）。
- **评价指标**：
  - 严格 Pass@1：所有测试用例均通过才算成功。
  - Soft-Pass@1：累积各步通过率的几何补集，作为辅助诊断指标，衡量精炼过程中的部分功能进步。

## 4. 资源与算力
- 文中**未明确说明**训练所使用的 GPU 型号、数量、总训练时长或单次运行的能耗。
- 仅能推断训练过程涉及重复的代码生成（冻结模型前传）、执行、奖励计算与 PPO 策略更新，计算成本高于简单 baseline，但无量化数值。

## 5. 实验数量与充分性
- 共设计了 **三个研究问题（RQ1‑RQ3）**，覆盖从单步到多步、从稀疏奖励到形状奖励、从无学习随机调度到PPO的对比。
- 在 **3 个数据集 × 3 个代码骨干模型** 上进行了系统评估，表 1 对比了单步变换效果（3×3种配置），表 2 对比了多步迭代方法（PPO 与 EPiC、Reflexion、Random‑Hybrid，共 3×3 组）。
- 还包含 **奖励设计的消融**（形状奖励 vs. 稀疏二元奖励），以及对部分任务案例的定性展示（图 3）。
- 进行了 **统计显著性检验**（McNemar 检验用于 Pass@1，配对 t 检验用于 SoftPass@1），均报告了显著性和效应量。
- **实验较为充分、客观且公平**：动作空间在 Random‑Hybrid 和 PPO 中保持一致，有效分离了“使用哪些动作”与“是否学习何时使用”的贡献；冻结代码模型避免训练数据泄露影响对比。

## 6. 论文的主要结论与发现
- PPO 智能体在 MBPP+ 上对三种代码模型分别取得 **57.58%、64.80%、85.50%** 的严格 Pass@1，全面超越 EPiC、Reflexion 和 Random‑Hybrid。
- Random‑Hybrid 性能显著弱于 PPO，说明 **性能提升并非仅来自混合动作空间，更来自学习到的动作调度策略**。
- 形状奖励较稀疏二元奖励能提供更密集的学习信号，稳定训练并支持多步探索。
- Soft‑Pass@1 的增益表明，学习到的策略能够累积中间功能进步，最终转化为完全正确的程序。
- 方法对代码生成骨干模型的架构差异具有一定鲁棒性，在 MBPP+、HumanEval+ 和 APPS 上均观察到了一致的相对优势（尽管 APPS 绝对性能因任务更复杂而偏低）。

## 7. 优点
- **方法创新**：首次将词汇变异、语义重写与直接生成统一为一个可学习的序贯决策框架，通过 RL 自适应调度。
- **奖励设计**：不从二元正确性出发，利用单元测试通过比例提供密集的形状奖励，更有利于策略学习。
- **对比公平**：引入 Random‑Hybrid 作为动作空间控制基线，剥离了“动作组合”与“学习调度”的贡献，结论更具说服力。
- **实验扎实**：覆盖多个代码模型和不同难度数据集，包含单步、多步、消融和统计检验，分析维度较完整。

## 8. 不足与局限
- **状态信息单薄**：状态仅用提示嵌入，未包含生成代码内容、失败测试输出或异常追踪，限制了更细致的诊断性精炼。
- **计算开销未量化**：未报告训练所需的GPU资源、时间或成本，难以评估实际部署的门槛。
- **依赖外部模块**：语义重写依赖第三方预训练模型和固定的元提示，其质量可能影响整体优化上限。
- **泛化边界未探索**：仅在 Python 编程任务上测试，未扩展到其他编程语言或非代码生成任务。
- **评估依赖预定义测试**：测试用例的完备性直接影响 Pass@1 的可靠性，且真实场景可能需要更复杂的正确性判定。
- **受限模型选择**：仅测试了开源、小中型代码模型，未与 GPT‑4 等闭源系统比较，也未验证策略能否迁移到更强的代码模型。

（完）
